{
  "slug": "yield-from",
  "title": "yield from and generator delegation",
  "roadmap": "python-swe",
  "kind": "concept",
  "tier": "tier2",
  "metadata": {
    "difficulty": "medium",
    "estimated_minutes": 30,
    "importance": 7,
    "interview_frequency": "medium",
    "prerequisites": ["iterator-protocol", "iterable-protocol"],
    "unlocks": ["generator-send"],
    "project_usage": ["asyncio", "FastAPI"]
  },
  "overview": {
    "what": "The `yield from` expression delegates part of a generator's operations to another iterable (called the subgenerator). It acts as a transparent two-way pipe: instead of iterating with a `for` loop and yielding items one by one, `yield from` automatically yields all values from the sub-iterable. Think of it like connecting a hose to an existing tap instead of carrying water by buckets.",
    "why": "It removes boilerplate when one generator needs to yield all elements from another, making recursive generators clean and enabling advanced coroutine patterns.",
    "where_used": ["Recursive data structure traversal", "Coroutines (asyncio foundation)", "Chunked data processing"]
  },
  "why_learning_this": [
    "Refactoring large generators into smaller reusable subgenerators",
    "Understanding the foundation of Python's async/await syntax"
  ],
  "common_mistakes": [
    {
      "title": "Confusing yield with yield from",
      "explanation": "Using `yield list_obj` yields the entire list as a single item, whereas `yield from list_obj` yields each element of the list individually."
    }
  ],
  "recall_questions": [
    {
      "q": "What does `yield from` do when applied to an iterable like a list?",
      "answer": "It automatically yields each element of the iterable one by one, delegating the iteration process.",
      "tier": "tier2"
    },
    {
      "q": "How does `yield from` improve code readability over a standard `yield`?",
      "answer": "It replaces a manual `for` loop that iterates over a sub-iterable and yields items individually, reducing boilerplate.",
      "tier": "tier2"
    }
  ],
  "practice_tasks": [
    {
      "title": "Flatten a Nested List",
      "prompt": "Write a generator function `flatten` that takes a list of lists and uses `yield from` to yield each individual item from the inner lists without using a nested for-loop.",
      "starter_code": "def flatten(list_of_lists):\n    # Your code here\n    pass",
      "solution": "def flatten(list_of_lists):\n    for sublist in list_of_lists:\n        yield from sublist"
    }
  ],
  "code_walkthrough": {
    "code": "def sub_gen():\n    yield 1\n    yield 2\n\ndef main_gen():\n    yield 'start'\n    yield from sub_gen()\n    yield 'end'\n\nfor item in main_gen():\n    print(item)",
    "focus": "yield from sub_gen()"
  },
  "aha_moment": {
    "code": "def get_letters():\n    yield from 'AB'\n\ndef get_numbers():\n    yield from [1, 2]\n\ndef combined():\n    yield from get_letters()\n    yield from get_numbers()\n\nprint(list(combined()))",
    "prediction": "What does this print?",
    "common_guess": "['AB', [1, 2]]",
    "why": "`yield from` delegates to the string iterable ('A', 'B') and then the list iterable (1, 2), effectively chaining them together into ['A', 'B', 1, 2]."
  },
  "challenge": {
    "title": "Recursive Generator",
    "prompt": "Write a generator `traverse_tree` that recursively yields all node values in a binary tree from left to right. The tree is represented as a dictionary: `{'val': 1, 'left': {...}, 'right': {...}}`. Assume missing children are `None`. Use `yield from` for the recursive calls.",
    "solution": "def traverse_tree(node):\n    if node is None:\n        return\n    yield from traverse_tree(node.get('left'))\n    yield node['val']\n    yield from traverse_tree(node.get('right'))"
  },
  "sources": [
    "https://docs.python.org/3/reference/expressions.html#yieldexpr"
  ]
}
