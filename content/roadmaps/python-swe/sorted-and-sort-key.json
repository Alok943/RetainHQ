{
  "slug": "sorted-and-sort-key",
  "title": "Sorting with sorted and key=",
  "roadmap": "python-swe",
  "kind": "concept",
  "tier": "tier1",
  "metadata": {
    "difficulty": "medium",
    "estimated_minutes": 35,
    "importance": 9,
    "interview_frequency": "high",
    "prerequisites": ["lists", "lambda-functions"],
    "unlocks": [],
    "project_usage": [
      "ordering database results",
      "sorting log entries by timestamp"
    ]
  },
  "glossary": [
    {
      "term": "stable sort",
      "definition": "A sorting algorithm that preserves the original relative order of elements that have equal sort keys. Python's `sort()` and `sorted()` are guaranteed to be stable."
    }
  ],
  "overview": {
    "what": "Python offers two ways to sort data:\n\n1. **`list.sort()`**: Mutates the list in place. It returns `None`.\n2. **`sorted(iterable)`**: Returns a *new* sorted list. The original iterable is unchanged.\n\nBoth functions accept two optional keyword arguments:\n- `reverse=True`: Sorts descending instead of ascending.\n- `key=...`: A function (or lambda) that takes one element and returns a value to sort by.\n\nBy default, sorting compares elements directly (numbers by value, strings alphabetically). But often you need to sort complex objects. By providing a `key` function, you tell Python exactly what aspect of the element to compare.\n\nFor example, to sort a list of strings by their length: `sorted(words, key=len)`.\nTo sort a list of intervals `[[1, 3], [2, 6], [8, 10]]` by their start time (the first element): `sorted(intervals, key=lambda x: x[0])`.\nTo sort a dictionary's items by value instead of key: `sorted(d.items(), key=lambda item: item[1])`.\n\nPython's sort is **stable**. If two elements have the same key, they stay in the exact order they were originally in. This is extremely useful if you need to sort by multiple criteria (e.g. first sort by age, then sort by name).",
    "why": "Sorting is a fundamental building block. Many algorithms (like merging overlapping intervals or finding the k-th largest element) begin by sorting the input. Understanding `key=lambda...` means you can cleanly sort tuples, objects, or arrays by any arbitrary rule without writing complex comparator logic.",
    "where_used": [
      "Sorting intervals by start time",
      "Sorting pairs by the second element",
      "Finding top-K elements"
    ]
  },
  "why_learning_this": [
    "You'll constantly need to sort lists of lists, tuples, or dicts in interviews.",
    "Using `.sort()` when you meant `sorted()` (or vice-versa) is a classic bug that deletes your data or mutates inputs you weren't supposed to touch."
  ],
  "common_mistakes": [
    {
      "title": "Assigning the result of list.sort()",
      "explanation": "`result = arr.sort()` leaves `result` as `None`. `.sort()` mutates in place and doesn't return the list."
    },
    {
      "title": "Writing a custom comparator instead of a key",
      "explanation": "In older languages, you pass a function that compares two elements `f(a, b)`. In Python 3, you pass a `key` function that transforms one element into a sortable value `f(x)`."
    },
    {
      "title": "Mutating function arguments unnecessarily",
      "explanation": "If a function receives a list, calling `arr.sort()` modifies the caller's list. Unless instructed to modify in place, use `sorted(arr)` to return a new list."
    }
  ],
  "recall_questions": [
    {
      "q": "What is the difference between `a.sort()` and `sorted(a)`?",
      "answer": "`a.sort()` mutates the list in place and returns None. `sorted(a)` leaves `a` unchanged and returns a new sorted list.",
      "tier": "tier1"
    },
    {
      "q": "How do you sort a list of strings in descending order?",
      "answer": "`sorted(strings, reverse=True)`",
      "tier": "tier1"
    },
    {
      "q": "What does it mean that Python's sort is 'stable'?",
      "answer": "Elements with equal keys retain their original relative order after sorting.",
      "tier": "tier2"
    }
  ],
  "practice_tasks": [
    {
      "title": "Sort by the second value",
      "prompt": "You are given a list of `(name, age)` tuples. Modify the `sort_by_age` function to return a new list sorted by the person's age (the second element in each tuple), ascending.",
      "starter_code": "def sort_by_age(people):\n    # Modify the key to sort by age (index 1)\n    return sorted(people)\n\ndata = [('Alice', 30), ('Bob', 25), ('Charlie', 35)]\nprint(sort_by_age(data))",
      "solution": "def sort_by_age(people):\n    return sorted(people, key=lambda x: x[1])\n\ndata = [('Alice', 30), ('Bob', 25), ('Charlie', 35)]\nprint(sort_by_age(data))"
    }
  ],
  "code_walkthrough": {
    "code": "intervals = [[8, 10], [1, 3], [2, 6]]\nintervals.sort(key=lambda x: x[0])\nprint(intervals)",
    "focus": "The lambda function `lambda x: x[0]` tells the sort algorithm: 'for each interval `x`, look at `x[0]` (the start time) to determine its order.' The list is mutated in place to `[[1, 3], [2, 6], [8, 10]]`."
  },
  "aha_moment": {
    "code": "words = ['banana', 'apple', 'cherry', 'kiwi']\nby_len = sorted(words, key=len)\nprint(by_len)",
    "prediction": "What is the order of the output?",
    "common_guess": "Alphabetical order",
    "why": "It prints `['kiwi', 'apple', 'banana', 'cherry']`. The `key` function is `len`, so Python calculates the length of each word and sorts by those numbers (4, 5, 6, 6). Notice 'banana' and 'cherry' both have length 6 — because the sort is stable, 'banana' stays before 'cherry' as it was in the original list."
  },
  "sources": [
    "https://docs.python.org/3/howto/sorting.html"
  ],
  "understanding_checks": [
    {
      "type": "predict-output",
      "code": "items = [('a', 3), ('b', 1), ('c', 2)]\nres = items.sort(key=lambda x: x[1])\nprint(res)",
      "question": "What is printed?",
      "answer": "None",
      "why": "`items.sort()` mutates the list in place and returns `None`. To get the sorted list, you would just print `items`."
    },
    {
      "type": "find-bug",
      "code": "records = [(1, 'B'), (2, 'A')]\nrecords.sort(key=lambda x: x[1])\nprint(records)",
      "question": "Is there a bug in this code?",
      "answer": "No, this is perfectly valid.",
      "why": "This correctly sorts the list of tuples in place by the second element ('A', 'B'), modifying `records` to `[(2, 'A'), (1, 'B')]`."
    },
    {
      "type": "predict-output",
      "code": "nums = [5, 2, 9, 1]\nnums.sort(reverse=True)\nprint(nums[0])",
      "question": "What does this print?",
      "answer": "9",
      "why": "The list is sorted in descending order (`reverse=True`), so the largest number (9) becomes the first element."
    }
  ]
}
