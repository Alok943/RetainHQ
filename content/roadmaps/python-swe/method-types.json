{
  "slug": "method-types",
  "title": "Instance, Class, and Static Methods",
  "roadmap": "python-swe",
  "kind": "concept",
  "tier": "tier2",
  "metadata": {
    "difficulty": "medium",
    "estimated_minutes": 30,
    "importance": 8,
    "interview_frequency": "high",
    "prerequisites": [
      "functions",
      "decorators"
    ],
    "unlocks": [],
    "project_usage": [
      "FastAPI",
      "Django",
      "Pydantic"
    ]
  },
  "glossary": [
    {
      "term": "constructors",
      "definition": "Special methods used to create and initialize a new instance of a class, e.g. `def __init__(self, name): self.name = name`."
    },
    {
      "term": "namespace",
      "definition": "A conceptual space or container that holds a set of names, ensuring that all names are unique and don't conflict with each other, e.g. `Config.clean_key` lives in the `Config` namespace."
    }
  ],
  "overview": {
    "what": "Python classes support three distinct types of methods based on what state they need to access. The key mental model is to look at the first parameter to determine the method's scope.\n\n- Instance methods: The default method type. They operate on specific object instances and take `self` as their first argument.\n- Class methods: Decorated with `@classmethod`. They operate on the class itself by taking `cls` as their first argument. They return a new instance when used as alternative constructors.\n- Static methods: Decorated with `@staticmethod`. They act like regular functions that take neither `self` nor `cls`. They live inside a class's namespace purely for organizational purposes.\n\nA common edge case involves modifying class state through an instance method.\n\n  class Counter:\n    count = 0\n\n    def increment(self):\n      # Trap: Creates an instance attribute, shadowing the class attribute!\n      self.count += 1\n\nWriting `self.count += 1` reads the class attribute `count`, adds `1`, and creates a new instance attribute `count` instead of updating `Counter.count` (covered fully in class-attributes). To modify class state safely, use a `@classmethod` and update `cls.count`.\n\nThink of `self` as a single employee, `cls` as the department manager, and `@staticmethod` as the office calculator.",
    "why": "They allow you to logically group related behaviors. Instead of having a bunch of disconnected functions, you can bundle utility functions (`@staticmethod`) and alternative constructors (`@classmethod`) directly onto the class they relate to.",
    "where_used": [
      "Alternative constructors like `dict.fromkeys()`",
      "Utility functions attached to models in `Pydantic`"
    ]
  },
  "why_learning_this": [
    "Building clean alternative constructors (e.g., `User.from_json()`)",
    "Organizing utility functions inside related classes instead of polluting the module namespace"
  ],
  "common_mistakes": [
    {
      "title": "Forgetting the decorator",
      "explanation": "If you forget `@classmethod`, the method is just a plain instance method. Calling it on the class raises a `TypeError` because there's no instance to fill `self`, and calling it on an instance passes the instance instead of the class."
    },
    {
      "title": "Using staticmethod when classmethod is needed",
      "explanation": "Using `@staticmethod` to build a constructor means you have to hardcode the class name inside the method. If the class is subclassed, the static method will still create instances of the parent class, whereas `@classmethod` dynamically uses the subclass `cls`."
    }
  ],
  "recall_questions": [
    {
      "q": "What is the primary use case for a class method?",
      "answer": "To operate on the class itself, most commonly to create alternative constructors like `Model.from_dict()`.",
      "tier": "tier2"
    },
    {
      "q": "How does a static method differ from an instance method?",
      "answer": "A static method does not take `self` (or `cls`) as its first argument and does not have access to instance or class state.",
      "tier": "tier2"
    },
    {
      "q": "Why is it dangerous to update a class attribute using `self.attribute += 1` inside an instance method?",
      "answer": "It reads the class attribute but creates a new instance attribute with the incremented value, which shadows the class attribute for that specific instance.",
      "tier": "tier2"
    }
  ],
  "understanding_checks": [
    {
      "type": "predict-output",
      "code": "class Counter:\n    count = 0\n    @classmethod\n    def increment(cls):\n        cls.count += 1\n\nCounter.increment()\nCounter.increment()\nprint(Counter.count)",
      "question": "What does this code output?",
      "answer": "2",
      "why": "The `@classmethod` takes `cls` (the class itself) as the first argument. Modifying `cls.count` updates the class-level attribute `count` directly. Calling it twice increments the class attribute to `2`."
    },
    {
      "type": "debug-misconception",
      "question": "A developer writes a utility function inside a class that doesn't use `self` or `cls`, but they forget to add the `@staticmethod` decorator. Why will this fail when called from an instance?",
      "answer": "Without `@staticmethod`, Python assumes it is an instance method and automatically passes the instance (`self`) as the first argument, causing a `TypeError` for receiving too many arguments.",
      "why": "Python's default behavior for methods inside a class is to pass the instance as the first argument. `@staticmethod` explicitly disables this behavior."
    }
  ],
  "practice_tasks": [
    {
      "title": "Alternative Constructor",
      "prompt": "Modify the `User` class to include a class method named `from_csv_string`. It should take a `csv_string` (e.g., 'alice,alice@example.com'), split it by the comma, and return a new instance of the class.",
      "starter_code": "class User:\n    def __init__(self, username, email):\n        self.username = username\n        self.email = email\n\n    # TODO: Add @classmethod from_csv_string here\n\n# Uncomment to test:\n# u = User.from_csv_string('alice,alice@example.com')\n# print(u.username, u.email)",
      "solution": "class User:\n    def __init__(self, username, email):\n        self.username = username\n        self.email = email\n\n    @classmethod\n    def from_csv_string(cls, csv_string):\n        username, email = csv_string.split(',')\n        return cls(username, email)"
    }
  ],
  "code_walkthrough": {
    "code": "class Config:\n    prefix = 'app_'\n\n    @classmethod\n    def get_key(cls, name):\n        return cls.prefix + name\n\nprint(Config.get_key('port'))",
    "focus": "return cls.prefix + name"
  },
  "aha_moment": {
    "code": "class Parent:\n    @classmethod\n    def create(cls):\n        return cls()\n\nclass Child(Parent):\n    pass\n\nprint(type(Child.create()).__name__)",
    "prediction": "What does this print? (`Parent` or `Child`?)",
    "common_guess": "`Parent`",
    "why": "Because it's a `@classmethod`, `cls` becomes the class it was called on (`Child`), not the class where it was defined (`Parent`). This makes class methods perfect for inheritable constructors."
  },
  "challenge": {
    "title": "Config Parser",
    "prompt": "Write a `ConfigParser` class. It should have a static method `clean_key(key)` that strips whitespace and lowercases a string. It should also have a class method `from_dict(data)` that takes a dictionary, cleans all its keys using `clean_key`, and returns a new `ConfigParser` instance where the cleaned data is stored in `self.config`. Its `__init__` should just take and store the config dictionary.",
    "solution": "class ConfigParser:\n    def __init__(self, config):\n        self.config = config\n\n    @staticmethod\n    def clean_key(key):\n        return key.strip().lower()\n\n    @classmethod\n    def from_dict(cls, data):\n        cleaned = {cls.clean_key(k): v for k, v in data.items()}\n        return cls(cleaned)"
  },
  "sources": [
    "https://docs.python.org/3/library/functions.html#classmethod",
    "https://docs.python.org/3/library/functions.html#staticmethod"
  ]
}
