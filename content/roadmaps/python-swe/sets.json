{
  "slug": "sets",
  "title": "Sets: operations and use cases",
  "roadmap": "python-swe",
  "kind": "concept",
  "tier": "tier1",
  "metadata": {
    "difficulty": "easy",
    "estimated_minutes": 35,
    "importance": 7,
    "interview_frequency": "medium",
    "prerequisites": [
      "primitive-types",
      "mutable-vs-immutable",
      "lists"
    ],
    "unlocks": [
      "collection-complexity",
      "dict-set-comprehensions"
    ],
    "project_usage": [
      "deduplicating API payloads",
      "permission/role checks",
      "tag filtering"
    ]
  },
  "glossary": [
    {
      "term": "unordered collection",
      "definition": "A group of items that do not have a specific sequence or position, meaning you can't rely on their order. Example: `{3, 1, 2}`."
    },
    {
      "term": "deduplicating",
      "definition": "The process of removing duplicate or repeated items from a dataset so that only unique items remain. Example: `set([1, 1, 2])` gives `{1, 2}`."
    }
  ],
  "overview": {
    "what": "A `set` is an unordered collection of unique, hashable (having a hash value which never changes during its lifetime) elements. It is defined using curly braces `{1, 2, 3}` or the `set()` constructor. Unlike a `list`, a `set` automatically discards duplicate values and cannot be accessed by index.\n\nThink of a `set` like a VIP guest list: someone is either on the list or they aren't, but their exact position in line doesn't matter (unordered).\n\nBecause sets are backed by a hash table, membership checks (`x in myset`) execute in `O(1)` time. This makes them significantly faster than scanning a `list` in `O(n)` time.\n\nSets support powerful mathematical operations for comparing collections:\n- `|` (Union): Returns a new `set` containing all unique elements from both sets.\n- `&` (Intersection): Returns a new `set` containing only elements present in both sets.\n- `-` (Difference): Returns a new `set` containing elements in the first set but not the second.\n\nWhen removing elements, you must choose how to handle missing values. Sets provide two distinct methods:\n- `remove(x)`: Removes `x` from the `set`. Returns `None`. Raises a `KeyError` if `x` is missing.\n- `discard(x)`: Removes `x` from the `set` if present. Returns `None`. Never raises an exception if `x` is missing.\n\nThis presents a common edge case where `remove()` crashes on missing elements:\n\n  active_users = {\"alice\", \"bob\"}\n  active_users.remove(\"charlie\")  # KeyError: 'charlie'\n  active_users.discard(\"charlie\") # Does nothing, succeeds\n\nSets only accept hashable objects, meaning you cannot store mutable types like a `list` or `dict`:\n\n  invalid_set = {[1, 2], [3, 4]}  # TypeError: unhashable type: 'list'\n\nThe relationship between mutability and hashing is covered fully in `collection-complexity`.\n\nAnother dangerous edge case is attempting to modify a `set` while iterating over it. This will immediately crash your program:\n\n  active_users = {\"alice\", \"bob\", \"charlie\"}\n  for user in active_users:\n    if user == \"bob\":\n      active_users.remove(user)  # RuntimeError: Set changed size during iteration\n\nTo safely remove items during iteration, you must iterate over a copy of the collection instead.",
    "why": "Sets are optimized for two things: answering whether an item is in the collection instantly, and guaranteeing uniqueness. When you need to check if a user has a specific permission out of thousands, a `set` makes that check extremely fast compared to scanning a `list`.",
    "where_used": [
      "API scope validation",
      "Deduplicating database rows",
      "Feature flags"
    ]
  },
  "why_learning_this": [
    "Deduplicating `list`s of IDs or tags automatically",
    "Fast permission and role checks (e.g., `if required_role in user_roles`)",
    "Finding commonalities or differences between two datasets using intersections and differences"
  ],
  "common_mistakes": [
    {
      "title": "Using {} for an empty set",
      "explanation": "`{}` creates an empty `dict`, not an empty `set`. To create an empty `set`, you must explicitly use `set()`."
    },
    {
      "title": "Trying to add mutable items",
      "explanation": "Set elements must be hashable. Trying to add a `list` or `dict` to a `set` raises a `TypeError`. Use `tuple` or `frozenset` instead."
    },
    {
      "title": "Assuming order is preserved",
      "explanation": "Sets do not preserve the order of elements you add to them. If you iterate over a `set`, the items will appear in an arbitrary order."
    }
  ],
  "recall_questions": [
    {
      "q": "What happens if you try to add a duplicate element to a `set`?",
      "answer": "The `set` silently ignores the duplicate. Sets only maintain unique elements.",
      "tier": "tier1"
    },
    {
      "q": "Why is it faster to check if an item exists in a `set` compared to a `list`?",
      "answer": "Because a `set` uses a hash table behind the scenes, allowing it to look up the item instantly (`O(1)`), whereas a `list` must scan elements one by one (`O(n)`).",
      "tier": "tier1"
    },
    {
      "q": "How do you create an empty `set`, and why can't you use `{}`?",
      "answer": "You create an empty `set` using `set()`. You cannot use `{}` because that syntax is reserved for creating an empty `dict`.",
      "tier": "tier1"
    },
    {
      "q": "What is the difference between `remove()` and `discard()` when removing an item from a `set`?",
      "answer": "`remove()` raises a `KeyError` if the element is missing, while `discard()` silently ignores the operation and does nothing.",
      "tier": "tier1"
    },
    {
      "q": "Why will your program crash if you remove items from a `set` during a standard `for` loop over it?",
      "answer": "Because modifying a `set`'s size while iterating over it raises a `RuntimeError`. You should iterate over a copy of the `set` instead.",
      "tier": "tier1"
    }
  ],
  "practice_tasks": [
    {
      "title": "Find missing scopes",
      "prompt": "The `get_missing_scopes` function takes two `list`s of `str`s. Modify it to return a `set` of scopes that are in `required_scopes` but NOT in `user_scopes`.",
      "starter_code": "def get_missing_scopes(user_scopes, required_scopes):\n    # Return required scopes that the user does not have\n    pass\n",
      "solution": "def get_missing_scopes(user_scopes, required_scopes):\n    return set(required_scopes) - set(user_scopes)\n"
    }
  ],
  "challenge": {
    "title": "Validate user permissions",
    "prompt": "You are building an API middleware that checks if a user has access to an endpoint. Write a function `has_access(user_roles, required_roles)` that takes two iterables. Return `True` if the user has ALL of the required roles, and `False` otherwise. Additionally, verify you don't use `list` operations.",
    "solution": "def has_access(user_roles, required_roles):\n    user_set = set(user_roles)\n    required_set = set(required_roles)\n    # issubset is True if all elements of required_set are in user_set\n    return required_set.issubset(user_set)\n\nprint(has_access(['admin', 'editor'], ['editor']))\nprint(has_access(['user'], ['admin']))"
  },
  "code_walkthrough": {
    "code": "user_roles = ['user', 'admin', 'user']\nunique_roles = set(user_roles)\nprint(unique_roles)",
    "focus": "Notice how passing the `list` into `set()` instantly strips out the duplicate `'user'` entry."
  },
  "sources": [
    "https://docs.python.org/3/tutorial/datastructures.html#sets",
    "https://docs.python.org/3/library/stdtypes.html#set-types-set-frozenset"
  ],
  "understanding_checks": [
    {
      "type": "predict-output",
      "question": "What is the output of this code?",
      "code": "user_roles = ['user', 'admin', 'user']\nrole_set = set(user_roles)\nprint(len(role_set))",
      "answer": "2",
      "why": "Sets automatically discard duplicate values. The resulting `set` is `{'user', 'admin'}`."
    },
    {
      "type": "debug-misconception",
      "question": "A developer writes `empty_set = {}` to initialize an empty `set`. Why is this incorrect?",
      "answer": "Because `{}` creates an empty `dict`, not an empty `set`.",
      "why": "Python uses `{}` for `dict`ionaries. To create an empty `set`, you must explicitly use the `set()` constructor."
    }
  ]
}
