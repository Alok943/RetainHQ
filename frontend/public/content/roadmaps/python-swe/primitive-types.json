{
  "slug": "primitive-types",
  "title": "Primitive types: int, float, bool, str",
  "roadmap": "python-swe",
  "kind": "concept",
  "tier": "tier1",
  "metadata": {
    "difficulty": "easy",
    "estimated_minutes": 30,
    "importance": 10,
    "interview_frequency": "low",
    "prerequisites": [],
    "unlocks": ["dynamic-typing", "mutable-vs-immutable"],
    "project_usage": ["FastAPI", "Pydantic", "Django"]
  },
  "overview": {
    "what": "Primitive types represent the foundational data structures in Python: integers for whole numbers, floats for decimals, booleans for truth values, and strings for text. Think of them as the elemental building blocks—like bricks, steel, and glass—used to construct complex architectural software systems.",
    "why": "Understanding these primitives is critical because every complex data structure or conditional logic ultimately evaluates down to these basic forms. Misunderstanding their boundaries, such as float precision or string immutability, introduces subtle, hard-to-trace bugs in production systems.",
    "where_used": ["FastAPI", "Pydantic", "SQLAlchemy"]
  },
  "why_learning_this": [
    "Defining strict type hints for Pydantic models in API request validation",
    "Writing safe state checks and numerical calculations in backend business logic"
  ],
  "common_mistakes": [
    {
      "title": "Floating Point Precision",
      "explanation": "Floats cannot exactly represent all decimal fractions natively, making `0.1 + 0.2 == 0.3` evaluate to False. Fix: Use the standard library `decimal` module for currency or high-precision math."
    },
    {
      "title": "Implicit String Concatenation Fails",
      "explanation": "Attempting to add a string and an integer (e.g. `'User_' + 12`) throws a TypeError instead of casting automatically. Fix: Explicitly cast using `str()` or leverage f-strings like `f'User_{12}'`."
    }
  ],
  "recall_questions": [
    {
      "q": "What are the four primary primitive data types in Python?",
      "answer": "Integers (int), floating-point numbers (float), booleans (bool), and strings (str).",
      "tier": "tier1"
    },
    {
      "q": "Why is it dangerous to use the float type for financial transaction calculations?",
      "answer": "Because floating-point numbers are imprecise when representing certain decimal fractions, which can lead to calculation errors.",
      "tier": "tier1"
    },
    {
      "q": "What happens if you try to combine a string and an integer using the plus operator without casting?",
      "answer": "Python will raise a TypeError because it does not implicitly convert integers to strings during concatenation.",
      "tier": "tier1"
    }
  ],
  "practice_tasks": [
    {
      "title": "API Response Formatter",
      "prompt": "Write a function `format_user_status` that accepts an age (int), a balance (float), an active status (bool), and a username (str). It should return an f-string for an API log. If the user is active, format as 'Active user {username} (Age: {age}) has ${balance:.2f}'. If inactive, replace 'Active' with 'Inactive'.",
      "starter_code": "def format_user_status(age: int, balance: float, is_active: bool, username: str) -> str:\n    pass",
      "solution": "def format_user_status(age: int, balance: float, is_active: bool, username: str) -> str:\n    status = 'Active' if is_active else 'Inactive'\n    return f'{status} user {username} (Age: {age}) has ${balance:.2f}'"
    }
  ],
  "challenge": {
    "title": "E-commerce Discount Calculator",
    "prompt": "Create a function `calculate_final_price` simulating an e-commerce checkout. It receives a base price (float) and a discount code valid status (bool). If the code is valid, apply a 15% discount. Ensure the final price is returned as a formatted string representing currency: 'Total: $'.",
    "solution": "def calculate_final_price(base_price: float, is_discount_valid: bool) -> str:\n    multiplier = 0.85 if is_discount_valid else 1.0\n    final_price = base_price * multiplier\n    return f'Total: ${final_price:.2f}'"
  },
  "code_walkthrough": {
    "code": "total = 0.1 + 0.2\nprint(total)\nprint(total == 0.3)\nname = \"User_\"\ncount = 12\nprint(name + str(count))",
    "focus": "Watch 0.1 + 0.2 land on 0.30000000000000004, and why the int needs str() to concatenate."
  },
  "sources": [
    "https://docs.python.org/3/library/stdtypes.html",
    "https://docs.python.org/3/tutorial/introduction.html"
  ]
}
