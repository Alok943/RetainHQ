{
  "slug": "mutable-vs-immutable",
  "title": "Mutable vs immutable objects",
  "roadmap": "python-swe",
  "kind": "concept",
  "tier": "tier2",
  "metadata": {
    "difficulty": "medium",
    "estimated_minutes": 30,
    "importance": 10,
    "interview_frequency": "high",
    "prerequisites": ["primitive-types"],
    "unlocks": ["shallow-vs-deep-copy", "mutable-default-argument"],
    "project_usage": ["Pydantic models", "config dictionaries", "FastAPI request bodies"]
  },
  "overview": {
    "what": "An object is mutable if its contents can change after it is created, and immutable if they cannot. Lists, dicts, and sets are mutable: list.append() edits the same object in place. int, float, bool, str, and tuple are immutable: 'changing' a string actually builds a brand-new string and leaves the original untouched. Think of a mutable object as a whiteboard you keep editing, and an immutable object as a printed page — to 'change' it you must print a new page. Because mutable objects are edited in place, a string or tuple can safely be a dict key while a list cannot.",
    "why": "Mutability decides whether an operation edits an object in place or returns a new one. Getting this wrong causes two classic bugs: assuming a string method changed the string (it didn't), and two names sharing one mutable object so an edit through one is unexpectedly seen through the other.",
    "where_used": ["Pydantic models", "config dictionaries", "API request bodies"]
  },
  "why_learning_this": [
    "Avoiding shared-state bugs when the same list or dict is referenced from two places in an API handler",
    "Knowing why a tuple or string can be a dict key but a list cannot"
  ],
  "common_mistakes": [
    {
      "title": "Expecting a string to change in place",
      "explanation": "s.upper() returns a NEW string and leaves s unchanged; calling it without reassigning does nothing. Fix: reassign, e.g. s = s.upper()."
    },
    {
      "title": "Sharing one mutable object by accident",
      "explanation": "After b = a where a is a list/dict, a and b are the SAME object, so a.append(...) is visible through b. Fix: copy it (a.copy()) when you need an independent object."
    }
  ],
  "recall_questions": [
    {
      "q": "What distinguishes a mutable object from an immutable one?",
      "answer": "A mutable object's contents can change after creation (list, dict, set); an immutable object's cannot (int, float, bool, str, tuple).",
      "tier": "tier1"
    },
    {
      "q": "When you 'change' a string, what actually happens?",
      "answer": "A new string object is created and the original string is left unchanged.",
      "tier": "tier1"
    },
    {
      "q": "Why can a string or tuple be used as a dict key but a list cannot?",
      "answer": "Strings and tuples are immutable so they can safely be keys; a list is mutable and cannot be a dict key.",
      "tier": "tier2"
    }
  ],
  "practice_tasks": [
    {
      "title": "Edit a dict's list in place",
      "prompt": "Write add_role(user, role) where user is a dict whose 'roles' value is a list. Append role to that list IN PLACE (do not rebuild the dict) and return user. Then print user['roles'] to show it changed.",
      "starter_code": "def add_role(user, role):\n    pass",
      "solution": "def add_role(user, role):\n    user['roles'].append(role)\n    return user\n\nu = {'name': 'sam', 'roles': ['viewer']}\nadd_role(u, 'editor')\nprint(u['roles'])"
    }
  ],
  "code_walkthrough": {
    "code": "nums = [1, 2]\nnums.append(3)\nprint(nums)\n\ntext = 'hi'\ntext.upper()\nprint(text)",
    "focus": "The list prints [1, 2, 3] (changed in place); the string still prints 'hi' because .upper() built a new string we ignored."
  },
  "aha_moment": {
    "code": "cart = ['laptop']\nbackup = cart\nbackup.append('mouse')\nprint(cart)",
    "prediction": "backup looks like a safe copy of cart. After appending 'mouse' to backup, what does print(cart) show?",
    "common_guess": "['laptop']",
    "why": "backup = cart binds a SECOND label to the same list object — it does not copy. So backup.append('mouse') mutates the one list both names point to, and cart sees it too. For an independent copy use cart.copy()."
  },
  "challenge": {
    "title": "Merge config without mutating",
    "prompt": "Write merge_config(base, override) that returns a NEW dict combining both (override wins) WITHOUT mutating base. Then print the result and base to prove base is unchanged.",
    "solution": "def merge_config(base, override):\n    return {**base, **override}\n\nbase = {'debug': False, 'region': 'us'}\nresult = merge_config(base, {'debug': True})\nprint(result)\nprint(base)"
  },
  "sources": [
    "https://docs.python.org/3/reference/datamodel.html#objects-values-and-types",
    "https://docs.python.org/3/library/stdtypes.html#mutable-sequence-types"
  ]
}
