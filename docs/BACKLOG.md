# Backlog — idea inbox

Raw, unprioritized capture. Mid-session "we should also…" ideas land here as **one line each**, and the session returns to its task. This is pre-triage: things graduate from here into `SYSTEM-OVERVIEW.md` §5 (plans) when actually prioritized — this file is never the roadmap.

Format: `- [ ] idea — one line of context (YYYY-MM-DD)`

## Product / features

- [ ] Node-mastery / per-node accuracy surfaced on the roadmap graph (color-coded), computed from Tests-section results — v1 deliberately computes this ad-hoc from `TestAttempt` scans instead of a dedicated table (SPEC-test-runtime.md non-goal) (2026-07-10)
- [ ] Tests-section score surfaced beyond the one-time completion screen (streaks/leaderboard/gamification) — `score`/`max_score` already computed and returned, just not displayed anywhere persistent (2026-07-10)
- [ ] School-audience nav: de-emphasize/hide the Log tab for `audience='school'` users — LogActivity's "source type: lecture/problem/course" framing doesn't fit a Class 9 student; their capture path is already "Add to reviews" on a lesson (2026-07-10)
- [ ] Make `key_memory` optional at log time — let the user log just the topic and have the LLM backfill the memory note, instead of requiring it upfront (explicit user ask, not built this session) (2026-07-05)
- [ ] Circuit and free-body 2D diagram renderers for physics lessons — `diagram.type: "circuit"/"free-body"` already has schema + membership-only validation, no renderer yet (2026-07-05)
- [ ] Retrofit `schematic`/`diagram3d` illustrations across the remaining ~120 physics lessons — only the 3-4 pitch-demo lessons got one; scoped as post-deal-only work (D-003) (2026-07-07)

## Tech debt / engineering

- [ ] (see also SYSTEM-OVERVIEW §5 for already-acknowledged debt: rate limiting, admin auth, feedback workflow)
- [ ] Tune the §7 `services/planner.py` scheduler constants against real dogfood evidence once ≥2 weeks have re-accumulated post-D-039 — shipped at spec defaults, never validated against a real plan (2026-07-26)
- [ ] `PLAN_HORIZON_DAYS=180` (planner urgency's timeline anchor) is a smallest-honest-substitute guess, not a derived value — `career_goals` has no stored start date, so "remaining fraction of the goal's timeline" isn't computable; flagged in IMPLEMENTATION-career-coach-phase3.md §3.5 (2026-07-26)
- [ ] The scheduler's `urgency` term (`score = priority × (1-m) × urgency × unlock_weight`) is a plan-wide multiplier — it scales every frontier node's score by the same factor, so a near `target_date` can never reorder or change which items get truncated relative to the open-ended plan (verified: G3 passes only because a uniform multiplier is structurally incapable of doing anything else). If deadline-crunch is meant to visibly change what's studied, urgency needs a per-node term, not a global one — a spec question, not a bug fix (D-041) (2026-07-26)
- [ ] `BALANCE_FLAG_THRESHOLD=-0.15` is subject-count-sensitive: measured against the 3 shipped v2 templates, only the single largest subject per template can ever trip it (`dsa` p_s=0.317 in backend.v2, `genai_engineering` p_s=0.170 in ai_engineer.v2, `dsa_core` p_s=0.263 in sde_generalist.v2) — every other subject (7-9 per template) is structurally unflaggable no matter how neglected, since a subject's zero-evidence balance floor is `-p_s` and most subjects sit at `p_s≤0.17`. Needs a template-size-aware threshold or a different formula once dogfood evidence exists (D-041) (2026-07-26)
- [ ] `Profile.jsx` calls raw `supabase.auth.getUser()` instead of `useAuth()`'s session — under `DEV_AUTH_BYPASS` (no real Supabase login happens) this never resolves, so the whole Profile page hangs on its loading skeleton forever in dev. Found verifying the audience-switcher redirect; not fixed, out of that session's scope (2026-07-10)
- [ ] Groq model upgrade path — currently `openai/gpt-oss-120b` (all 6 LLM endpoints); evaluate Kimi/GLM as alternatives. Raised by the founder, not evaluated (2026-07-05)
- [ ] Verify the Render build (dashboard → service → Settings) — Dockerfile vs native runtime; if Dockerfile, one rebuild is needed for the `.dockerignore` to purge old layers; also confirm no locally built image was ever pushed to a registry (closes/reopens the D-008 no-rotation call). Originally a Railway item; re-check after the 2026-07-11 move to Render (2026-07-10)
- [ ] Auth 401 responses echo JWT-library exception text (`detail=f"Could not validate credentials: {e}"`) — return a generic message, log the detail server-side (audit finding, deferred) (2026-07-10)
- [ ] `/health` doesn't touch the DB — the host (Render) reports healthy while the pool is dead; add a cheap `SELECT 1` (audit finding, deferred) (2026-07-10)
- [ ] Security headers (CSP, HSTS, etc.) on the Vercel frontend via a `headers` block in `vercel.json` (audit finding, deferred) (2026-07-10)
- [ ] Move `alembic upgrade head` out of the container CMD into a separate release/pre-deploy step — concurrent replicas/redeploys race migrations; a failing migration crash-loops the API (audit finding, deferred post-pitch) (2026-07-10)
- [ ] Pin backend dependencies — `pyproject.toml` is all `>=` with no lockfile; `pip-compile` a pinned `requirements.txt` for the Docker build (audit finding, deferred post-pitch) (2026-07-10)
- [ ] `datetime.utcnow()` deprecation sweep → `datetime.now(datetime.UTC)` — 40 warnings in the test run; naive-UTC storage convention itself stays (audit finding, deferred) (2026-07-10)
- [ ] ~32 roadmap seeds still use the destructive `DELETE FROM roadmap_nodes … + re-INSERT with uuid.uuid4()` pattern — re-running any of them against a roadmap with live `user_progress` cascade-wipes it (ON DELETE CASCADE). Only `seed_python_swe.py` was converted to the progress-safe upsert (D-009); convert the rest — or at minimum any roadmap already in use — before ever re-running to add nodes (2026-07-10)
- [x] ~~DSA `valid-parentheses` generator array-input bug~~ — VERIFIED FIXED 2026-07-11: `generators/valid-parentheses.js:3` handles `Array.isArray(input)`, it's golden-tested in `stack-queue-family.golden.mjs`, registered in `registry.js`, and the lesson JSON has its `viz` block. The 2026-07-10 "still shows the bug" claim was itself a stale read (2026-07-11)
- [ ] Decide the semantics of the lesson `tier` field (tier1/2/3, shown as a colored badge in `LessonView`; no validator rule ties it to `difficulty`). It currently does NOT track `metadata.difficulty` — **189 of ~582 lessons diverge across every roadmap, including easy topics marked tier2/tier3** (`else-finally`, `slots`, `join-keys`…), so it reads as an independent "advanced-ness / importance" axis applied inconsistently, NOT a difficulty typo. Decide: (a) tier ≡ difficulty → mass-align all 189 + add a `validate.py` check; or (b) distinct axis → document it and leave values as authored. NOTE: this session pre-emptively forced 7 DSA lessons (backtracking ×5, lower/upper-bound) to tier=difficulty on the (b)-violating assumption — revert or keep pending this decision (2026-07-11)
- [ ] Browser-verify the `lower-bound`/`upper-bound` viz blocks added this session (validated + synced but never rendered; low risk — they reuse the already-golden-tested lower/upper-bound generators + `ArrayViz`) (2026-07-10)

## Content / roadmaps

- [ ] (roadmap-seed candidates are tracked in CLAUDE-ARCHIVE-2026-07.md "Roadmap backlog" + SYSTEM-OVERVIEW plans)
- [ ] python-swe: 40 of 148 nodes still have no lesson (108 now link). Gaps are almost all **Phase 5 Testing** (pytest, unittest.mock, black, ruff, mypy, pre-commit, pdb, cProfile) and **Phase 6 Engineering Practices** (git, project structure, design patterns) — never authored — plus a few Phase 3/4 nodes whose lesson exists under divergent wording (duck typing, multiple inheritance, property decorator, dataclasses-vs-pydantic, with-statement). Needs authoring/title-mapping, not a mechanical fix — Antigravity content pass candidate (2026-07-10)
- [ ] Apply the fix lists for the ~26-29 python-swe lessons still flagged NEEDS-WORK by the lesson-critic pass (duplicate recall/understanding-check questions, a fabricated `__req__` dunder in `operator-overloading.json`, missing variance discussion in `generic-types.json`, etc.) — found and reported but never actually applied (D-013) (2026-07-10)
- [ ] `shallow-vs-deep-copy.json` has 2 recall_questions truncated mid-sentence (cut off right at an em-dash, no continuation or `?`) — a botched prior fix for a mojibake-encoding defect that swapped one bug for a worse one; needs the questions properly completed (2026-07-10)
- [ ] Run the format-upgrade pass (bullets/backticks/edge-case flags, D-011/D-012) on the other 8 roadmaps — only `python-swe` got the pilot pass; rollout order + per-roadmap edge-flag guidance already specified in `content/PROMPT-format-upgrade.md` (2026-07-10)
- [ ] Run the lesson-critic sweep on the remaining major roadmaps — core-cs (61), aptitude (40), dsa (**51** — folder grew past the quoted 37); python-swe/sql/ai-engineering were swept + fixes applied per D-013. NOTE (2026-07-11 audit): the only report artifact on disk is `content/CRITIC-REPORT-dsa.md` — none exist for the three claimed-swept roadmaps, so "swept" rests on D-013's session record, not artifacts; and the dsa report suggests a partial dsa run already happened. Reconcile before re-running. Via Gemini-in-Antigravity (`content/RUN-lesson-critic-antigravity.md`, D-016) (2026-07-10)
- [ ] Java (`java-swe`) roadmap: add `equals()/hashCode()` contract + autoboxing/Integer-cache seed nodes (placement-critical, currently only implicit) before authoring; then generate the Step-1 pilot per `content/PROMPT-cpp.md` (D-014). C++ Step-1 pilot also not yet generated (only the gold exemplar `rule-of-3-5-0-...json` exists) (2026-07-10)

## Marketing / growth

- [ ] Reverse-engineer the school-pilot sales timeline into a dated plan: launch pilot Jul/Aug → pull mid-term efficacy data Oct → pitch the School Management Committee Dec, landing inside the Nov–Feb CBSE procurement window (from the deep-research pilot playbook; not yet turned into calendar dates) (2026-07-05)
- Node metadata (difficulty prior, question_types) — generate ONLY when a consumer exists; FSRS already measures real difficulty (from syllabus-review discussion 2026-07-12)
- Dependency-graph VISUALIZATION for syllabus roadmaps (never locking — contradicts "track what you remember"); real value is cross-roadmap edges
- Syllabus strict-vs-enhanced extraction mode (strict = only what the syllabus names; one prompt fork + upload-page toggle)
- Content manifest has 9 case/wording-duplicate title keys (e.g. 'Duck typing' vs 'Duck typing in Python', 'None & truthiness' vs 'None & Truthiness') - duplicate JSON keys break strict parsers (PowerShell ConvertFrom-Json already chokes) and hide node-lesson mapping gaps; dedupe titles at the source lessons + make sync-content.mjs fail on duplicates. Found 2026-07-12 during the python-swe gap diff.
- DSA graph viz pipeline is wired but broken (found 2026-07-12 while speccing phase 16): compile.js emits graphNodeTags/graphCursor while GraphViz.jsx reads nodeTags/cursor (every node renders white); graphEdgeTags map never merged onto edge objects so no MST/path highlight; SET_EDGE is in BOTH tree+graph op families and treeReducer claims it unconditionally BEFORE graphReducer (flips graph frames to the tree renderer); ENQUEUE_NODE tags 'enqueued' which is absent from GraphViz FILL; RELAX targets args.u with an unresolved 'or v' comment. Full spec = Pass 0 in content/HANDOFF-dsa-phase16-graphs.md.
- SEO: execute docs/SEO-IMPLEMENTATION.md (P0 prerender full lesson content + seo title overrides; P1 keyword-gap sections + internal links) — plan written 2026-07-16 from GSC 3-month export; nothing implemented yet
- Agentic-AI lesson cluster for ai-engineering roadmap (agents, tool loops, planning, MCP): 'agentic ai' +1,350% rising under 'ai engineer roadmap' in Google Trends 2026-07-16; low-competition SEO window while the topic is young — candidate for next Antigravity batch
- combination-sum.golden.mjs FAILS at HEAD (pre-existing, found 2026-07-12 during pre-push gates): it calls compile({candidates,target}, events) but compile() spreads input as an array ([...input] throws 'input is not iterable'). Fix the golden to pass a compile-compatible input (or teach compile to accept non-array inputs for tree-view traces). Currently 1/14 suites red in npm run golden.
- MCP lesson phase inside the agentic-AI cluster: Trends 2026-07-17 — 'mcp server' (avg 49) now OUT-SIZES 'langchain' (40) worldwide, grew ~4x in 12mo, SERPs still young (only modelcontextprotocol.io is authoritative); framework lanes: LangGraph (23) >> CrewAI (8) / OpenAI Agents SDK (6). Write evergreen concept lessons (protocol, tools/resources/prompts, servers vs clients, security) not tool-of-the-week — agent-hype churn is real (openclaw/moltbook breakouts)
- Data Engineering roadmap (job-aligned, 3rd in build priority per strategy memory): 'data engineering' avg 29 and ~2x in 12mo (Trends 2026-07-17); builds directly on the existing SQL/PGlite runtime — window functions, data modeling, SCDs, partitioning, dbt/airflow/spark concepts; long-tail winnable with docs-as-truth + runnable SQL
- School SEO content-format finding (Trends IN 2026-07-17): Class 12 physics risers are ALL 'chapter-wise weightage 2026', 'sample paper with solutions', 'all chapters' — students search formats, not just concepts. Build static hub pages per class ('Class 12 Physics chapter-wise weightage', 'all chapters' index) + NEET/JEE 'high weightage chapters' angle when physics-12 ships; class 12 physics (avg 23, spikes 100 at Feb boards) > class 11 (14, steady) → physics-12 before physics-11 confirmed
- Calibrate career-template est_effort_min against real learning_events.duration_min before the phase-3 scheduler consumes them — current estimates are ~3-5x optimistic (backend tree sums to ~67h; realistic fresher prep is several hundred hours). Until calibrated, the scheduler must treat effort as RELATIVE weight, not absolute time (SPEC-career-templates-v2 §6, 2026-07-20).
- Career goal switch has no un-archive: creating any new goal permanently archives the current one (partial unique index, no restore endpoint) — fine for v1, but a user who tries a second role and wants their old tree back is stuck. Surface as explicit 'switch goal' UX with restore in phase 3+ (found 2026-07-20 while verifying SPEC-career-templates-v2).
- Companion/LLM-chat tracking design (owner calls, 2026-07-20, pre-decided ahead of the companion phase): (1) METADATA-ONLY — topic label + duration + timestamp extracted on-device, raw chat text never retained anywhere, not even locally. (2) NO per-item confirmation — one-time opt-in per surface (allowlist + study-mode toggle), then events flow automatically; review surface is UNDO (existing per-event delete+recompute) not approve; low-confidence mappings land in the existing triage bucket. Safe by construction: ambient = T3 (mastery cap 0.35) or w=0 TIME_BLOCK. (3) CROSS-SURFACE SESSION STITCHING in the extension: segments from Claude/ChatGPT/LeetCode etc. cluster by same-node/subject + time-gap <=~15min into ONE session event; duration = UNION of intervals (tab-switching must not double-count); stitched session = T3 time signal feeding Balance, while verified solves (LeetCode API, T1 + entity_id dedupe) separately move mastery — different event types, no double-count possible. Promote to DECISIONS when the companion phase is specced.
- Companion surface-expansion addendum (2026-07-20): YouTube/Coursera/Notion/PDFs join the LLM-chat surfaces with NO per-session prompts ever. Principle: THE TREE IS THE FILTER — classify content metadata (video title/channel, PDF filename, page title) against the user's own tree nodes via the same embedding seam as topic mapping; match → track silently (T3/w=0), no match → drop silently (understate over annoy, Philosophy #3), middle band → existing triage bucket. Surface classes: unambiguous (Coursera/LeetCode: domain opt-in then silent), mixed (YouTube/PDF: metadata classify), private-mixed (Notion/LLM chats: classify + triage). UX: recording-light extension badge (visible state, one-click stop) + one-time-per-DOMAIN reactive opt-in, never per chat/video/page.
- Companion filter scope addendum (2026-07-20): the classifier's candidate set is ALL the user's nodes (career tree + personal syllabus roadmaps + active catalog roadmaps), not just the active goal's tree — college subjects map via the existing syllabus-upload path; widen candidate_nodes_for_active_goal accordingly when the companion lands. New-learning discovery: one-off no-match drops silently, but a RECURRING unmatched topic cluster (~3 sessions/week) triggers a pull-based suggestion ('3h on compiler design this week — add it?') that one-tap creates a custom.* node or personal roadmap and retroactively maps the buffered sessions (append-only log + recompute makes this free). Also: SYLLABUS_LIFETIME_LIMIT=3 was sized for PDF-abuse, not for a student with 5-6 subjects/semester — revisit when coursework tracking becomes an on-ramp.
- Companion classification ladder addendum (2026-07-20): three rungs — (1) deterministic (allowlist/exact title), (2) cached embeddings vs the user's nodes, (3) small LLM ONLY for the ambiguous middle band and for intent that embeddings can't see — notably assistance_level detection ('learning DP' vs 'pasting homework'), which feeds the existing weight table (none=0.25 vs llm_assisted=0.06) and keeps ambient evidence honest. Placement rule: chat CONTENT classifies on-device (Chrome built-in Gemini Nano / Prompt API from the extension; no-support browsers degrade to rung 2 + triage — understate, never leak); METADATA-only jobs (cluster naming, add-node suggestions, triage hints) may use server-side gemini flash-lite, batched per session/cluster, cached. Never per-message calls.
- Companion privacy-tier user choice addendum (2026-07-20): three user-facing modes — Private (embeddings only, no assistance detection), On-Device AI (Gemini Nano/Prompt API; DEFAULT when available), Cloud AI (explicit opt-in only, never default; chat samples sent transiently to provider, never stored by RetainHQ). Default rule = safest-capable (Nano if present, else Private). Cloud model: gemini flash-lite — wins on effective cost (classification is input-heavy/output-tiny so input price dominates) AND on integration (provider already wired, same key/SDK as syllabus+tree-gen, same family as the on-device tier); a second provider (DeepSeek etc.) adds a key + SDK + failure mode to save ~fractions of a cent/user-day. Re-verify pricing at implementation.
- Companion design amendment from external LLM review (2026-07-20, full review in headroom hash a38879e7a676ab5aed4cb7b3, verdict 9.7/10): ADOPTED — (1) session-level classification: the stitcher builds one session, the LLM classifies it once (calls scale with ~5-10 sessions/day not hundreds of events; cross-surface sequence resolves ambiguity); (2) reframed prompt 'which node + confidence + study_type + assistance_level + reason' vs user's tree, reason feeds the evidence log; LLM = final automatic rung before triage, with deterministic short-circuit for exact-match single-surface sessions; (3) metadata-only memory in prompt (recent session topics, goal, weak node names — never content); 'AI Coach with chat understanding' as the name for the explicit cloud-content opt-in tier. REJECTED/DEFERRED — same-model coaching (phase-4 coach agent reads the server-side evidence spine, different trust surface; reviewer's own examples like 'watched 3h solved 0' are deterministic SQL over learning_events, no LLM needed); LLM self-reported confidence treated as ordinal band (high/med/low → auto/triage/drop) mapped to existing thresholds, never as a calibrated probability.
- Companion phase-5 idea, banked from second external review (2026-07-20): personal classifier via retrieval — inject the user's own past classified sessions (retrieved by embedding similarity from their ~hundreds of accumulated sessions) into the rung-3 prompt, so the model learns per-user patterns ('Fireship usually = backend for this user', 'Claude+LeetCode together = DSA'). Pure prompt-side change — no fine-tuning, no new infra; the payload versioning (classifier/prompt_version) shipped in C1 makes the before/after measurable. Prerequisite: months of accumulated session data; revisit after companion C1 dogfood.
- LeetCode retention surface specced but unscheduled: `docs/SPEC-leetcode-retention.md` (2026-07-21). Key finding — ~70% already exists at HEAD (`LearningEvent` + `"leetcode"` in SOURCES + `PROBLEM_SOLVED` weights keyed on (difficulty, assistance) + `Activity.difficulty/needed_hint/mistake` + `NodeMastery` + `Review.ai_verdict` as proposal-only). Genuinely new: `problems` catalog, `problem_concepts` mapping, `concept_cards` shared bank, backfill import. Blocking pre-work is step 0 — hand-author the ~20-40 missing dsa PATTERN nodes (109 lessons today); the closed concept vocabulary is the asset and LLMs may only classify onto it, never extend it. Two decisions worth promoting to DECISIONS when scheduled: (a) concept vocabulary IS the dsa roadmap_nodes, no parallel graph (else the companion classifier drifts into the wrong one); (b) FSRS schedules CARDS, concepts are a derived aggregate driving selection/framing only — concept-level FSRS silently degrades stability estimates.
- [ ] **Run the restore drill** in `docs/RUNBOOK-backup-restore.md` §4 against a scratch project and log the date in its table — until then the restore path is unverified and the nightly backup is a guess, not a backup (2026-07-26)
- [ ] Copy nightly backup artifacts offsite (Cloudflare R2 / Backblaze B2 free tier) — today they live in the same GitHub account as the code, so losing the account loses both (D-040) (2026-07-26)
- [ ] Decide on Supabase Pro (~$25/mo) for provider-side daily backups + PITR — the honest answer once the user base is more than people you know; nightly dumps still leave a 24h loss window (D-040) (2026-07-26)
- [ ] Canary in `backup-db.yml` only catches a table hitting *zero*; a partial deletion (say 60% of rows) passes silently. Consider comparing each night's counts against the previous run's and alerting on a large drop (2026-07-26)
- [ ] Decide whether to tell the 19 affected users their activity/review history was lost on 2026-07-26 — roadmap progress survived, captures and review history did not (2026-07-26)
- [ ] `SPEC-career-coach-phase3.md` §3.1 rule 4 (balance correction) has a day-zero bug: with no logged time `a_s=0`, so `balance_s = -p_s` and the "most neglected" subject is just the *largest* one — every new user's first plan would open by flagging their biggest subject as under-served. `IMPLEMENTATION-career-coach-phase3.md` §3.8a adds a `BALANCE_MIN_WINDOW_MINUTES=120` evidence floor; fold the fix back into the spec (2026-07-26)
- [ ] `BALANCE_FLAG_THRESHOLD=-0.15` is subject-count-sensitive: `p_s` shrinks as a template adds subjects, so on a 10-subject tree a zero-time subject only reaches -0.10 and can never trip the flag, while a 4-subject tree fires constantly. Measure the real `p_s` distribution per v2 template before trusting the constant (2026-07-26)
- [ ] Measure the `p_s` shift from attached roadmaps (D-042): attaching the 126-node DSA roadmap at priority 3 adds ~378 to the summed-priority denominator, shrinking every existing subject's share and moving which subjects can reach `BALANCE_FLAG_THRESHOLD=-0.15`. Report real distributions before retuning the constant (2026-07-26)
- [ ] Attached roadmaps ship with a flat `est_effort_min=60`; check whether one large attachment swamps `est_share` in a real plan and whether per-node effort is worth deriving (2026-07-26)
- [ ] **Prod outage 2026-07-26**: `alembic upgrade head` runs in the Dockerfile CMD, and `backend/.env` points `DATABASE_URL` at prod — so running a migration locally stamps production with a revision the deployed image may not contain, and the next container restart crash-loops (uvicorn never runs because of the `&&`). Options: move migrations to a Render pre-deploy command, or make the boot tolerant of an unknown head, or refuse to start when `alembic_version` isn't in the image (2026-07-26)
- [ ] Extend the `backup-db.yml` canary to assert prod's `alembic_version` matches a file in `backend/alembic/versions/` — the 2026-07-26 outage was latent for hours and only surfaced when Render's free tier spun the service down; a daily check would have caught it before it became downtime (2026-07-26)
- [ ] `ai_engineer.v2.json` has **no DSA subject** (all 10 subjects are ML/LLM/MLOps), and `career_tree.py` rejects an invented subject as a hard-rule violation — so an AI-Engineer user can regenerate forever and never get DSA, with no feedback saying why. Indian placement screens test DSA regardless of specialization. Needs an `ai_engineer.v3.json` with a DSA subject (D-035: v2 freezes, v3 ships alongside); the parent scope rule requires a removal per addition, so decide what gets cut (2026-07-26)
- [ ] `CAREER_TREE_DAILY_LIMIT = 3` makes the "Not quite right? Add context and regenerate" box unusable — the initial auto-generate burns one, leaving two retries per day. Sized for the Opus-era cost model; generation is Gemini Flash now (D-037). Raise it (2026-07-26)
- [ ] The regenerate box invites free-text context it structurally cannot honour (subjects are template-fixed). Surface that: when the user's text names a topic with no matching subject, say so instead of silently returning the same tree (2026-07-26)
- [ ] On-device Nano consent tier is plumbing only (permission request + storage) — the Prompt API feasibility spike (`SPEC-companion-phase1.md` §9 step 5, timeboxed ½ day) hasn't run. If it fails, the tier should degrade to Titles-only per spec; currently it silently just requests the LLM origins and does nothing on-device (2026-07-26)
- [ ] Companion consent/tiers (D-043) was never verified by an actual install-and-click-through — this environment's browser tooling can't reach `chrome://` pages. Do a manual pass before Web Store submission: fresh profile, confirm zero permission on any LLM origin pre-consent, choose each tier, confirm the popup settings row round-trips both directions (2026-07-26)
- [ ] `SPEC-companion-phase1.md` §2/§5-6 still describe the dynamic `chrome.scripting.registerContentScripts` approach for the LLM content script; the shipped mechanism is a static `content_scripts` entry gated by `optional_host_permissions` instead (D-043 — the dynamic approach silently drops the script from the `@crxjs/vite-plugin` build). Fold the correction back into the spec (2026-07-26)
- [ ] Companion Phase 1 (browser extension + `/api/companion/sessions`) shipped before this repo's `SYSTEM-OVERVIEW.md` existed in its current form and was never added — the 2026-07-26 companion-consent changelog entry is the first mention. Worth a proper backfill entry (routers count, `companion_browser` migration, extension architecture) next time this file gets a full pass (2026-07-26)
- [ ] Extension icons are a solo raster of `favicon.svg` at 16/32/48/128px (its `color(display-p3 …)` style overrides had to be stripped to render correctly under `resvg`) — fine for Web Store submission, but worth a real designer pass alongside the other store assets (promo tile, screenshots) called out in the parent doc's §10 (2026-07-26)
- [ ] Firefox build (D-044) was never verified in a real Firefox — this environment's browser tooling can't drive Firefox or read `about:debugging`. Before AMO submission: load `dist-firefox/` unpacked, sign in (note the logged OAuth redirect URL and add it to Supabase's allowed-redirect list — it's browser-specific and wasn't guessed), confirm a YouTube/LeetCode session actually syncs, confirm the consent screen grants an LLM origin the same way it does on Chrome (2026-07-26)
- [ ] `SPEC-companion-phase1.md` and `IMPLEMENTATION-companion-firefox.md` both assume `strict_min_version: 128.0` as the Firefox floor (the `optional_host_permissions` requirement) — shipped as `140.0` instead, because `web-ext lint` surfaced that `browser_specific_settings.gecko.data_collection_permissions` (mandatory for new extensions since 2025-11-03) itself needs Firefox ≥140. Fold this correction into the spec docs the same way D-043's mechanism correction was (2026-07-26)
- [ ] `web-ext lint` on the Firefox build carries 2 pre-existing warnings, both `UNSAFE_VAR_ASSIGNMENT`: a template-literal `innerHTML` assignment in `leetcode.ts`'s reflection panel, and a dynamic `import()` inside the bundled `@supabase/supabase-js` chunk. Neither is new to this port and both are 0-error warnings, but worth a look before an AMO reviewer flags them manually (2026-07-26)
- [ ] Chrome Web Store listing is deferred (Firefox-only for now, `docs/IMPLEMENTATION-amo-submission.md`) — pin the Chrome extension's id via a manifest `key` before that submission so the `chrome-extension://` id (and thus the OAuth redirect URL) stops changing between unpacked loads (2026-07-27)
- [ ] No `gecko.update_url` / update manifest set up yet — optional for the unlisted-v1 self-host path, but without it users who install the self-hosted `.xpi` never get automatic updates. Add before a second version ships (2026-07-27)
- [ ] AMO submission itself (dedicated reviewer account creation, `web-ext sign`/unlisted upload, downloading the signed `.xpi`, dropping it into `frontend/public/downloads/`) needs the user's own Mozilla Add-on API credentials — not something this environment can do. `extension/REVIEWER-BUILD.md` + `/privacy/companion` + the manifest's `data_collection_permissions` are ready; submission is the next manual step (2026-07-27)
- [ ] `problem_attempts` migration (`e2c4a8b1f960`, D-046) bridges user data to `problems`, a bulk-rebuildable catalog table already reachable by `TRUNCATE ... CASCADE` (see `a9d4f7c2e618`). Add it to that migration's `GUARDED_TABLES` list in a follow-up migration before the 2026-07-26 incident's exact failure mode can recur through this new table (2026-07-27)
- [ ] `problem_attempts` migration `e2c4a8b1f960` is written but **not applied to prod** — asked the owner, got no answer, left it unapplied per the doc's "do not upgrade prod." Apply it (and re-verify the mark/unmark write path live, not just via the SQLite test suite) before shipping the Problems section to real users (D-046) (2026-07-27)
- [ ] The DSA "Hash tables" node has 184 primary-role mapped problems, rendered as one flat unpaginated list (per spec: "median 13 does not need pagination, and the client sorts") — confirmed live that this specific node is a genuinely bad scan experience, unlike the median-13 case the doc reasoned from. Consider a per-node fold/show-more past some primary-role count, or a search/filter within a very large section (D-046) (2026-07-27)
- [ ] Product call, not made silently (D-046 doc §9): should `problem_attempts` marks feed the roadmap's node-level progress bar/percentage? Today that bar counts only `user_progress` rows, so solving problems for a node doesn't move its visible progress (2026-07-27)
