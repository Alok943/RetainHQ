# RetainHQ — System Overview

**Last full verification: 2026-07-10** (every claim checked against the actual code and the live production database via the read-only Supabase MCP).

> **This is a LIVING document — the single source of truth for system state.** Convention (also in CLAUDE.md): any commit that adds/removes an endpoint or router, changes `models/models.py` or adds a migration, adds a service/integration, changes deploy topology, or moves the security posture **must update the relevant section here and add a Changelog line below, in the same commit**. Claims about prod state (migration head, catalog counts) must be re-verified via the Supabase MCP, not assumed. Roughly monthly — or before any launch/pitch — re-run a full verification pass and bump the date above. It supersedes the stale parts of `ARCHITECTURE.md` (last touched 2026-06-07), `API.md`, and the status sections of `CLAUDE.md`. See the [Stale-docs appendix](#appendix--doc-freshness) at the bottom for what to trust elsewhere.

RetainHQ is a learning-retention platform: **Log Activity → Capture Key Memory → Schedule Reviews (FSRS) → Active Recall → Retain**, with a growing library of interactive lessons (Python/SQL/DSA/aptitude/Core-CS/Physics) that feed cards into that loop. Solo-founder build, live in production at `retainhq.app`.

---

## 1. Architecture

### Topology

```
React SPA (Vercel, root=frontend/)  ←  retainhq.app (apex + www)
  ├─ Supabase Auth (Google OAuth) ──► ES256 JWT
  └─ apiFetch + Bearer JWT ──► FastAPI (Render, root=backend/)
                                  └─ asyncpg ──► Supabase Postgres (Mumbai, transaction pooler :6543)
GitHub Actions (daily 01:30 UTC) ──X-Cron-Secret──► POST /api/internal/send-reminders ──► Resend (email) + Web Push (browser)
PostHog (product analytics): frontend `posthog-js` (no-op without VITE_POSTHOG_KEY) + backend `posthog` (server-truth events, no-op without POSTHOG_API_KEY); same project, keyed by Supabase user id
Sentry (error tracking): frontend `@sentry/react` (no-op without VITE_SENTRY_DSN, sourcemaps uploaded+deleted gated on SENTRY_AUTH_TOKEN) + backend `sentry-sdk` (no-op without SENTRY_DSN); pseudonymous (id only, no email/PII) both sides
Web Push (PWA): hand-rolled push-only `public/sw.js` (no fetch handler — SPA stays online-only) + `lib/push.js`; VAPID keys on Render only, public key served via GET /api/push/vapid-public-key
```

- The frontend talks **only** to FastAPI (`frontend/src/lib/api.js` → `apiFetch`, attaches the Bearer JWT; `optionalAuth` allows guest reads). No direct `supabase.from(...)` DB calls exist in React — Supabase on the client is auth-only.
- FastAPI is the single DB gateway. It connects as `postgres` (table owner), which bypasses RLS; RLS exists to block Supabase's auto-exposed PostgREST/anon-key path (see §4).
- Heavy interactive compute is **client-side**: Pyodide (Python step-tracer), PGlite (in-browser Postgres for SQL lessons), and a pure-JS DSA trace player. Zero server compute for lessons.

### Auth (verified in `backend/app/core/security.py`, `api/deps.py`)

- Supabase Google OAuth issues an **ES256** JWT; FastAPI verifies it via `PyJWKClient` against Supabase's JWKS (keys cached 1 h). **HS256 is removed** (`algorithms=["ES256"]` only — algorithm-confusion attack closed). JWKS outage → clean 503, not a 500.
- Claims checked: signature, `exp`, `aud="authenticated"`, `role == "authenticated"`. `sub` (string UUID) is cast to `uuid.UUID` before queries.
- `get_current_user` → `SupabaseUser` (attribute access). `get_optional_user` powers guest exploration. `get_admin_user` = case-insensitive email match against `ADMIN_EMAIL` (403 otherwise) — interim founder gate, not a role system.
- **`DEV_AUTH_BYPASS` is boot-guarded**: `config.py`'s model validator raises at startup unless `DEBUG=true` is also set, so a stray flag in the prod env (Render) crashes the deploy instead of silently handing admin to anonymous requests.

### Backend layout (`backend/app/`)

| Piece | Contents |
|---|---|
| `main.py` | App, env-driven CORS allow-list, `/health`, `/me`, mounts **15 routers**; `logging.basicConfig(INFO)` + gated `sentry_sdk.init` |
| `core/` | `config.py` (pydantic-settings + the bypass boot guard; **+ `MAX_CLASSROOMS_PER_TEACHER=20`**, **+ `CAREER_TREE_MODEL`=`gemini-3.5-flash`/`CAREER_TREE_DAILY_LIMIT`=3**), `database.py` (async engine: pooler-safe `statement_cache_size=0`, `pool_pre_ping=True`, `echo=DEBUG` only), `security.py` |
| `api/routes/` | `activities`, `reviews`, `dashboard`, `roadmaps`, `admin`, `feedback`, **`internal`** (cron-only), **`prefs`** (audience), **`tests`** (test banks), **`syllabus`** (PDF → personal roadmap), **`push`** (VAPID key + subscribe/unsubscribe), **`metrics`** (allowlisted client event capture), **`classrooms`** (2026-07-16 — teacher dashboard: roster/lifecycle + class analytics, see below), **`evidence`** (2026-07-19 — Career Coach Phase 1 evidence spine, see below; 2026-07-21 added `/leetcode/solve` and `/leetcode/backfill`), **`career`** (2026-07-19 — Career Coach Phase 2, goal/tree/diagnostic/mapping, see below) |
| `services/` | `scheduler.py` (FSRS-4.5: `apply_fsrs`, `FSRS_WEIGHTS`, `DESIRED_RETENTION=0.9`, `REVIEW_SESSION_CAP=10`), `grader.py` (Groq LLM: recall grading, question mode, capture assist, fill-up grading), **`mailer.py`** (Resend), **`reminders.py`** (claim-then-send daily batch; fans out to email + Web Push under the same `reminder_log` claim; emits `reminder_sent` per channel), **`analytics.py`** (server-side PostHog: `capture()`/`shutdown()`, no-op without `POSTHOG_API_KEY`; emits `review_scheduled`, `reminder_sent`), **`push.py`** (pywebpush wrapper: `is_configured()`, `send_push()` raising `PushGone`/`PushError`, no-op without VAPID keys), **`metrics.py`** (`record_metric_event()` — never commits, rides the caller's transaction), **`test_scoring.py`**, **`syllabus.py`** (syllabus-PDF → draft roadmap; **provider-routed by `SYLLABUS_MODEL`** — `gemini*` id → Google `google-genai` inline-PDF + `response_schema`, else Anthropic `claude-opus-4-8` document block + structured outputs/streaming; both send the same prompt+schema so the two can be A/B'd on quality; commit records `extraction_edit_delta` via `metrics.py`), **`mastery.py`** (2026-07-16 — pure functions: `compute_node_status`, `class_cell_status`, `find_root_cause`, `at_risk_flag`; no DB access, unit-tested via goldens), **`learner_stats.py`** (2026-07-16 — `get_review_metrics`/`get_heatmap`/`get_node_accuracy` extracted from `dashboard.py`, parameterized by `(db, user_id, roadmap_scope)`; `roadmap_scope=None` = today's self-serve behavior unchanged, a roadmap-id list = the teacher drill-down's scoped view), **`evidence_weights.py`** (2026-07-19 — `EVENT_TYPES`/`TRUST_TIERS`/`SOURCES` enums + `w()` weight-table lookup + `apply_event_weight()` single-step fold formula; pure, no DB), **`evidence.py`** (2026-07-19 — `fold_events()` pure two-stream replay (see `DECISIONS.md` for the spec-deviation rationale) + `record_event`/`recompute_node`/`recompute_user` DB layer + `node_state`/`confidence`/`retrievability_for` read-side derivations), **`career_tree.py`** (2026-07-19 — Career Coach Phase 2: template loader (`backend/app/data/career_templates/`, a synced copy — see below), `generate_career_tree()` provider-routed model call adapting a role template with §3.3 hard-rule validation + one retry + unmodified-template fallback, `_restore_diagnostic_fields()` always overwrites the model's diagnostic_probe/answer from the template — never trusted from generation output), **`topic_mapping.py`** (2026-07-19 — `suggest_node()` Jaccard word-overlap match against the user's committed tree with `AUTO_MAP_THRESHOLD=0.75`/`TRIAGE_THRESHOLD=0.55` (placeholder for a real embedding call — pgvector confirmed NOT installed, not worth adding for ≤90 vectors), `candidate_nodes_for_active_goal()`, `backfill_activity()` shared by the live `/unmapped/{id}/assign` route and the bulk backfill script) |
| `models/models.py` | Single source of truth — **22 tables** (see §2) |
| `alembic/versions/` | 32 migrations; head `37714df921df` (`add_paid_only` — **applied to prod 2026-07-21**) |
| `tests/` | `test_ownership.py` (cross-tenant isolation), `test_question_sets.py` (question-set reuse/scoping/reference-answer injection), `test_syllabus_quota.py` (lifetime cap, no delete-refund), `test_push.py`, `test_review_duration.py`, `test_metrics.py`, `test_dashboard_analytics.py`, **`test_classrooms.py`** (2026-07-16 — IDOR/ownership + the classroom privacy-boundary rule, explicitly tested per spec), **`test_mastery.py`** (pure-function goldens), **`test_evidence.py`** (2026-07-19 — the 18-test suite from SPEC-career-coach-phase1 §8), **`test_career.py`** (2026-07-19 — the 19-test suite from SPEC-career-coach-phase2 §9 plus extras: templates/validator, generation validation+fallback, atomic commit, diagnostic replay, mapping tiers, evidence-survival on node delete/goal switch, IDOR); SQLite via JSONB→JSON variant (Postgres-only paths — `auth.users` joins, `jsonb_array_elements`, `extract(hour from…)` — get an auth-gate/dialect-error smoke test instead of a full round-trip, documented in `conftest.py`); `pytest.ini`, `.dockerignore`, `Dockerfile`. **171/171 passing** as of 2026-07-19 |
| `seed_*.py` | **34 seed scripts** (idempotent, fixed UUIDs): 31 roadmap seeds + 2 prereq-edge seeds (`python_swe_prereqs`, `physics_school_prereqs`) + **`seed_classroom_demo.py`** (2026-07-16 — the pitch-demo classroom: "Class 9-A Physics", 12 members/3 personas against `physics-9-10`; written and validated but **not yet run against prod** — run it yourself when ready) |
| `sync_career_templates.py` / `backfill_career_mapping.py` | (2026-07-19, one-off/manual scripts, not wired into any build hook) `sync_career_templates.py` copies `content/career-templates/*.json` → `backend/app/data/career_templates/` — **run by hand whenever a template changes, before deploying** (Render's backend root is `backend/`, so the sibling `content/` dir is outside the Docker build context; see its docstring). `backfill_career_mapping.py <user_id>` re-runs topic mapping over every activity phase 1 flagged `evidence_unmapped`, auto-writing ≥`AUTO_MAP_THRESHOLD` matches and leaving the rest for in-app triage, then calls `recompute_user` |

### API surface (all verified against route decorators)

| Endpoint | Notes |
|---|---|
| `GET/POST /api/activities/` | Vault list; create card (FSRS init, first review +1d, first-ever = demo due now; `source_type='lesson'` idempotent per node) |
| `POST /api/activities/suggest-key-points` | Capture assist (gated `GRADER_ENABLED`, else 404) |
| `GET /api/reviews/due` | Capped at 10, oldest first |
| `POST /api/reviews/{id}/complete` | Advances FSRS, schedules next; then (2026-07-19) emits a `RECALL_GRADED` evidence event if `activity.node_id` is set, else counts an `evidence_unmapped` metric — see `/api/evidence/*` below |
| `POST /api/reviews/{id}/grade`, `/questions`, `/grade-questions` | LLM grader + question mode (all gated on `GRADER_ENABLED`). `/questions` serves a **persisted `question_sets` row** (reused `QUESTION_SET_REUSE`=2 sessions, shuffled each serve, then regenerated; optional body `{depth: 'main'\|'deep'}`); node-linked cards get **topic-grounded** generation (node title+description is the contract, key_memory only biases), free-form cards stay key_memory-grounded. `/grade-questions` injects the set's stored `reference_answer`s server-side (never sent to the client) |
| `GET /api/dashboard/` + **`/review-metrics`** + **`/heatmap`** | The last two are newer than CLAUDE.md |
| **`GET /api/dashboard/source-retention`** · **`/calibration`** · **`/memory-strength`** · **`/node-accuracy`** · **`/time-of-day`** | 5 aggregation endpoints (2026-07-13), each with its own `enough_data` floor (sparse history → honest "not enough yet", never a misleading number). `node-accuracy` (`jsonb_array_elements`) and `time-of-day` (`extract(hour from…)`) are Postgres-only |
| **`GET /api/dashboard/focus-areas`** | Home's "Key areas to focus": topics with `recalled=False` completions in the last 14 days, grouped per card, most-missed first, capped at 5; carries the latest `ai_feedback` line per topic. Empty list → the Home section doesn't render |
| **`GET /api/push/vapid-public-key`** · **`POST`/`DELETE /api/push/subscribe`** | Web Push subscription lifecycle. Vapid key 404s when unconfigured; subscribe upserts by `endpoint` (unique — a device re-subscribing under a different account intentionally reassigns ownership) |
| **`POST /api/metrics/events`** | Allowlisted client-side metric capture (`CLIENT_EVENT_TYPES`, currently `{review_depth_chosen}`, 2KB payload cap) — everything else goes through `record_metric_event()` server-side |
| `GET /api/roadmaps/` (+`{id-or-slug}`, `{id}/blockers`, `PUT nodes/{id}/progress`) | List is **filtered by the caller's `user_prefs.audience`** (career vs school) **plus the caller's own personal roadmaps** (`user_id`); personal roadmaps resolve only for their owner (404 otherwise) |
| **`POST /api/syllabus/extract` · `/extract-text` · `/commit` · `GET /quota` · `DELETE /api/syllabus/{id}`** | Syllabus upload → personal roadmap. Extract = PDF (≤10 MB) → LLM → draft JSON, **nothing saved**; extract-text = pasted syllabus text (≤40K chars, the token-cheap default in the UI — no per-page document tokens; also how DOCX is handled: user copies the text) → same draft; both share the ≤5/user/day in-memory limit. Commit = user-edited draft → `roadmaps(user_id)` + nodes, **capped at `SYLLABUS_LIFETIME_LIMIT`=3 per user LIFETIME** (atomic claim on `user_prefs.custom_roadmaps_created`; delete ≠ refund; 403 when spent); quota = {used, limit, remaining} for the UI; delete = own roadmaps only (activities keep history, links nulled). Extraction gated on the selected provider's key — `GEMINI_API_KEY` or `ANTHROPIC_API_KEY` (404 when unset) |
| `POST /api/feedback/` · `GET /api/admin/funnel` · `GET /api/admin/feedback` | Feedback + founder admin |
| **`POST /api/internal/send-reminders`** | No user JWT — `X-Cron-Secret` header, `hmac.compare_digest`, closed entirely if `CRON_SECRET` unset. Driven by `.github/workflows/reminders.yml` (01:30 UTC daily + manual dispatch) |
| **`GET/PUT /api/prefs/`** | Server-side audience preference ('career' \| 'school') |
| **`GET /api/tests/weights` · `POST /api/tests/grade-fillup` · `POST /api/tests/attempts`** | Tests section: per-node weights from last 10 attempts, LLM fill-up grading (gated), attempt submission — **bridges into FSRS** (`apply_fsrs`, misses → Rating.Again) |
| **`/api/classrooms/*`** (2026-07-16, teacher dashboard, 12 routes) | `POST /` create (caller = teacher, `MAX_CLASSROOMS_PER_TEACHER=20`) · `GET /mine` (teaching + enrolled) · `POST /join` (join-code → membership upsert, 404 on unknown/archived alike) · `PATCH /{id}` · `DELETE /{id}/members/{member_id}` · `DELETE /{id}/membership` (student leaves) · `PATCH /{id}/members/{member_id}` · `PUT /{id}/roadmaps`. Analytics (teacher-owner only, via a `get_owned_classroom` 404-not-403 IDOR wall): `GET /{id}/overview`, `/gap-map?roadmap_id=` (per-node mastery via `services/mastery.py`, root-cause via `roadmap_node_prerequisites`), `/students` (roster + at-risk flag+reasons), `/students/{member_id}` (drill-down via `services/learner_stats.py` scoped to the classroom's assigned roadmaps). Every analytics query scopes to `student_user_id IN classroom_members` **AND** the classroom's assigned-roadmap set — a teacher can never see a student's non-classroom activity (career roadmaps, personal syllabus roadmaps, unassigned cards); see §4 |
| **`/api/evidence/*`** (2026-07-19, Career Coach Phase 1 — Evidence Spine, `docs/SPEC-career-coach-phase1.md`, 8 routes) | `POST /manual` (Producer B: `TIME_BLOCK`/`T4_claimed`, always w=0, node-visibility checked) · `GET /nodes` (per-node `m_learned`/`r`/`m`/`state`/`confidence`/`evidence_count`, optional `?roadmap_id=`) · `GET /nodes/{id}/events` (per-event weight + running `m_learned` — the "why is this number what it is" trace) · `GET /summary` (`total_events`/`by_tier`/`by_type`/`unmapped_events`/`nodes_with_evidence`/`weights_version`) · `POST /recompute` (full replay for the caller, idempotent) · `DELETE /events/{id}` (soft-delete + recompute) · **`POST /leetcode/solve`** and **`POST /leetcode/backfill`** (2026-07-21 — records LeetCode extension signals as `PROBLEM_SOLVED` and unmapped slugs as `evidence_unmapped` metrics). Producer A lives in `POST /api/reviews/{id}/complete` (below), not here — it emits `RECALL_GRADED` after the FSRS commit, wrapped in a SAVEPOINT (not a bare `db.rollback()`, which would expire the already-eager-loaded `review.activity` and crash the next line) so an evidence bug can never break review completion |
| **`/api/career/*`** (2026-07-19, Career Coach Phase 2 — Goal, Tree & Topic Mapping, `docs/SPEC-career-coach-phase2.md`, 12 routes) | `GET /templates` (role_key/title/version/node_count, latest version per role) · `POST /goals/` (create, archives any existing active goal — partial unique index `(user_id) WHERE status='active'` is the DB-level backstop) · `GET /goals/active` (404 if none) · `GET /diagnostic` (5-8 probes drawn from the active goal's template — never generated fresh; selection is **subject-aware round-robin** since 2026-07-20, see `_select_diagnostic_probes` — a flat priority sort was subject-blind and served the AI Engineer 8 math/classical questions and zero LLM ones) · `POST /diagnostic/submit` (grades via the existing `grade_recall`, 404 if `GRADER_ENABLED` is off; results held client-side, not written yet) · `POST /tree/generate` (draft only, **writes nothing**, rate-limited `CAREER_TREE_DAILY_LIMIT`=3/user/day in-memory) · `POST /tree/commit` (draft → `Roadmap`+`RoadmapNode`+`NodeMeta`+prerequisite rows in one transaction via **staged flushes** — see `DECISIONS.md` for why — pins `template_version`, replays held diagnostic results into ordinary `RECALL_GRADED` learning_events keyed by `uuid5(goal.id, stable_key)` so a resubmitted commit can't double-count) · `PATCH /nodes/{id}` / `DELETE /nodes/{id}` (edit sets `user_edited=true`; delete is a **soft detach** — nulls `learning_events.node_id`/`activities.node_id` with a `payload.detached_from_stable_key` marker, never deletes the event, then deletes the `RoadmapNode` row — required because neither FK carries `ondelete=CASCADE`, confirmed at migration time) · `GET /unmapped` / `POST /unmapped/{activity_id}/assign` (Tier-3 triage bucket with a Tier-2 suggestion when the active tree has one; assign retroactively writes every skipped review as a `RECALL_GRADED` event and recomputes) · `POST /sprint` (parent A3: stores a temporary priority-override node + end date on the active goal; stored only, phase 3 is the first consumer) |

### Frontend layout (`frontend/src/`)

- **Shell/routing** (`App.jsx`): `/dashboard`, `/reviews`, `/log`, `/roadmaps(/:id)(/learn/:slug)`, **`/roadmaps/new`** (`SyllabusUpload.jsx` — upload → editable draft → commit; "Bring Your Own Path" card on `/roadmaps` links here; personal roadmaps render in a "Your Roadmaps" group with delete), **`/roadmaps/:slug/numericals/:phase`** (Physics), **`/roadmaps/:slug/test/:phase`** (Tests), `/paths` (`CareerPaths.jsx` — static role-transition content, unrelated to Career Coach despite the name), **`/coach`** (2026-07-19 — `CareerCoach.jsx` router: 404 on `GET /goals/active` → `CareerOnboarding.jsx` (4-step wizard: role cards → diagnostic, prominently skippable → tree review, subjects collapsed by default with inline priority/effort/title edit + delete, regenerate-with-context → confirm/commit); goal with `roadmap_id` set → `CareerTree.jsx` (nodes grouped by subject via `GET /api/roadmaps/{id}` + `GET /api/evidence/nodes?roadmap_id=`, merged client-side; tap a node → the phase-1 evidence-log drill-down, same endpoint as `/evidence`; unmapped-activity triage inline, "dismiss" is session-only client state — no backend endpoint for it)), `/vault`, `/analytics`, `/profile`, `/admin` (founder-only), **`/evidence`** (founder-only, 2026-07-19 — `Evidence.jsx`, dev-only instrument for the Career Coach evidence spine: `GET /nodes` table + per-node event drill-down + `POST /recompute` button; plain/unstyled by design, not a product surface), `/dsa-dev` (dev player), **`/teach`, `/teach/:id`, `/teach/:id/students/:memberId`, `/join/:code`** (2026-07-16 Phase 0 + 2026-07-17 Phase 1 partial, `docs/SPEC-teacher-dashboard.md`: `Teach.jsx` class list + create, `TeachClassroom.jsx` Overview/Gap-Map/Students tabs — Students now wires the 3 endpoints that existed but were unused (member rename/remove inline in the roster, join-code regeneration next to the copy chip) — `TeachStudentDetail.jsx` (new, 2026-07-17) the per-student drill-down (retention stats, 8-week recall trend, weakest topics, per-node mastery grid), `JoinClassroom.jsx` student consent + join; a "Teach" nav entry appears only when `GET /api/classrooms/mine.teaching` is non-empty, plus a "Set up your class"/"My Class" entry point on `Profile.jsx`). **Still open from spec Phase 1:** join-attempt throttle (explicitly deferred in code), DPDP one-pager, weekly teacher digest email.
- **Newer than CLAUDE.md**: `Tests.jsx`, `AudiencePicker.jsx` (one-time career/school picker), **`SchoolRoadmaps.jsx`** (Class → Subject → Chapter browser, fully replaces the career catalog when `audience==='school'`; parses `RoadmapNode.phase` "Class 9 · Motion" — no backend change; + roadmap search; commit `b94d233`, D-006), `PhysicsNumericals.jsx` + `physics/` module, `ReviewHeatmap.jsx`, `SqlFlow/SqlJoinViz/SqlResult.jsx`, `GlossaryTerm.jsx`, `WelcomeModal.jsx`, `ToastContext.jsx`, `lib/analytics.js` (**PostHog** wrapper — autocapture OFF, curated events, silent no-op without key), `lib/useSeo.js` + prebuild sitemap generation (SEO pass), `lib/testGrading.js`.
- **Sentry + Push + PWA (2026-07-13)**: `lib/errors.js` (Sentry wrapper mirroring `analytics.js`'s no-op-without-key pattern) + `ErrorBoundary.jsx` (custom class component wrapping `<App/>`, works with Sentry off) + `vite.config.js`'s `sentryVitePlugin` (sourcemaps generated+uploaded+deleted only when `SENTRY_AUTH_TOKEN` is set — never shipped publicly). `lib/push.js` (subscribe lifecycle, stashes the API origin into Cache Storage so the cross-origin `public/sw.js` — a static file, no `import.meta.env` — can reach the Render API) + `public/manifest.webmanifest` + `public/icon-*.png`. Profile.jsx notification toggle + Review.jsx done-screen one-time prompt card.
- **DSA viz module (`src/dsa/`)** — canonical artifact is the event trace: pure generator (`generators/*.js`, golden-tested `*.golden.mjs`) → `compile.js` folds events into frames → renderers. **Far ahead of CLAUDE.md's claim**: ~29 generators live (through backtracking: `n-queens`, `permutations`, `subsets`, `combination-sum`, plus stacks/queues, sliding windows, `kadane`, `fast-slow-pointers`, `next-greater-element`, bounds…) and **9 renderers** (`ArrayViz`, `StateMachine`, `StackQueueViz`, `ListViz`, `TreeViz`, `GraphViz`, `GridViz`, `IntervalViz`, `BitsViz`) — the renderer gaps CLAUDE.md lists as "still needed" are built. Of the 9: 7 are view-dispatched, 2 (`StateMachine`, `StackQueueViz`) are always-on side panels; **4 still have no generator feeding them** (`ListViz`, `GraphViz`, `IntervalViz`, `BitsViz` — no `LIST/GRAPH/INTERVAL/BITS_INIT` emitter yet). Backtracking generators DO feed `TreeViz`/`GridViz` (n-queens → grid; template/permutations/subsets/combination-sum → tree).
- Dark mode = centralized `html.dark` override layer in `index.css`; Login page intentionally always-dark.

### Content system (`content/`)

- One JSON per lesson at `content/roadmaps/<key>/<slug>.json`; **9 content folders**: `python-swe`, `sql`, `aptitude`, `core-cs`, `dsa`, `ai-engineering`, `python-backend`, `cpp-swe`, `physics-9-10`.
- `content/validate.py` is **the gate**. Allowed `kind`s (its `KIND` set): `concept` (base shape, no explicit branch) + explicit branches for `aptitude` / `theory` / `engineering` / `dsa` / `reasoning` / `physics` / `numericals` / `test` (+ `milestone` allowed but unused in content); `content/scripts/` (glossary injection, prose extraction) is real pipeline code. `sync-content.mjs` copies to `frontend/public/content/` on `predev`/`prebuild` (git-ignored, Vercel regenerates).
- **`content/career-templates/`** (2026-07-19, Career Coach Phase 2; **v2 quality pass 2026-07-20**, `docs/SPEC-career-templates-v2.md`): versioned role templates `<role_key>.v<N>.json`, immutable once shipped — content changes mean a new version file, never an edit. Current latest: `backend.v2.json` (66 nodes — +heaps/greedy/intervals, +system-design probe), `ai_engineer.v2.json` (70 nodes — new `llm_apis` subject (chat-completions/streaming/rate-limits/cost-budgeting/Hugging Face), +reranking-hybrid-retrieval and fine-tune-vs-RAG-vs-prompt in genai; kmeans/PCA/eigenvalues/feature-stores/RNN removed as payment), `sde_generalist.v2.json` (62 nodes — +two-pointers/sliding-window/binary-search/heaps/greedy). The three `*.v1.json` files stay frozen (committed v1 trees pin them as metadata). `content/validate_career_templates.py` (stable-key uniqueness, dependency-cycle detection, depends_on resolution, priority/effort bounds, probe-pair check, **probe-spread warning** — a `default_priority ≥4` subject with zero probes gets zero diagnostic coverage) is a **second gate**, run alongside `validate.py` by `.github/workflows/validate-content.yml` (workflow created in phase 2 — both spec docs assumed it already existed; it didn't). Runtime copy lives at `backend/app/data/career_templates/` (see `sync_career_templates.py` above) because Render's backend deploy root excludes the sibling `content/` directory.
- Test banks live at `content/roadmaps/<key>/_test/*.json` (static content; only attempt outcomes hit the DB).
- Bulk generation is delegated to Antigravity; Claude owns contracts (`schema.json`, `validate.py`, `PROMPT-*.md`), runtime/renderers, and critique. DSA content handoffs exist through **phase 13** (recursion/viz/prose) — CLAUDE.md's "phases 1–7" is stale.

---

## 2. Data model (25 tables — 22 prod-verified + `problems`/`problem_concepts`/`concept_cards` applied 2026-07-21)

`tracks`, `activities` (the FSRS card: `stability`/`difficulty_fsrs` NULL until first graded review; legacy SM-2 columns still written; optional `roadmap_id`/`node_id` links), `reviews` (due/completed + `rating`/`recalled`/`quality` + `ai_*` grader columns + **`duration_ms`** nullable int, card-shown-to-outcome-submitted via client `performance.now()`, server-clamped to (0, 1_800_000]), `feedbacks`, `roadmaps` (**+ `slug`, + `audience` 'career'|'school', + `user_id` NULL=catalog / set=personal syllabus-upload or career-tree roadmap**), `roadmap_nodes` (self-ref `parent_id` subtopics), `roadmap_node_prerequisites` (directed edges, powers "Why am I stuck?"), `user_progress`, **`user_prefs`** (audience, server-side; + `custom_roadmaps_created` lifetime syllabus-commit counter — never decremented), **`test_attempts`** (JSONB per-question results; `node_title` is the join key), **`reminder_log`** (unique `(user_id, sent_on)` = at-most-once-daily email+push idempotency, one claim gates both channels), **`question_sets`** (persisted LLM question set per card: JSONB `items` [{question, reference_answer}], `depth` 'main'|'deep', `times_used`; reused ≥2 sessions then regenerated; reference answers never leave the server), **`push_subscriptions`** (one row per browser/device Web Push subscription; `endpoint` unique = the upsert key; RLS), **`metric_events`** (generic JSONB event store for heterogeneous/exploratory learning signals — `event_type`+`payload`+optional `entity_id`; RLS; producers: syllabus commit's `extraction_edit_delta`, evidence spine's `evidence_unmapped`), **`classrooms`** (2026-07-16 — `teacher_user_id`, `name`, `school_name`, unique `join_code`, `archived_at`; RLS), **`classroom_members`** (unique `(classroom_id, student_user_id)`; `display_name` is student-typed at join, never the Google identity — see §4; RLS), **`classroom_roadmaps`** (which catalog roadmaps a classroom tracks — the scope boundary for every teacher-facing analytics query; RLS), **`learning_events`** (2026-07-19 — Career Coach Phase 1, `docs/SPEC-career-coach-phase1.md` §2.1: immutable append-only evidence log, one row per producer signal; partial unique `(user_id, source, entity_id) WHERE entity_id IS NOT NULL` = idempotency for polling producers; `deleted_at` soft-delete only; CHECK-constrained `event_type`/`trust_tier`/`grade`/`duration_min`; RLS; `node_id` FK carries **no `ondelete=CASCADE`**, confirmed at phase 2 migration time — a raw node delete is rejected by Postgres unless the app nulls it first, see phase 2's soft-detach), **`node_mastery`** (2026-07-19 — derived cache, one row per `(user_id, node_id)`, fully rebuildable from `learning_events` alone via `services/evidence.recompute_node`; unique `(user_id, node_id)`; RLS), **`career_goals`** (2026-07-19 — Career Coach Phase 2, `docs/SPEC-career-coach-phase2.md` §2.1: one active goal per user via partial unique `(user_id) WHERE status='active'`; `roadmap_id`/`template_version` NULL until tree commit; `sprint_node_id`/`sprint_until` stored for phase 3, unconsumed today; RLS), **`node_meta`** (2026-07-19 — career-tree sidecar to `roadmap_nodes` so the shared catalog table stays unwidened: `stable_key` (template-authored, survives node UUID regeneration), `priority`, `est_effort_min`, `subject`, `user_edited`; unique `node_id`; RLS), **`problems`** (2026-07-21 — LeetCode problems catalog; `slug`, `difficulty`, `paid_only`), **`problem_concepts`** (2026-07-21 — maps Problems to RoadmapNodes; `role`, `confidence`), **`concept_cards`** (2026-07-21 — flashcards generated for problem-node pairs), `alembic_version`.

**Migration chain (32):** `c71d8f31ee19` initial → … → `f1b3d5e7a9c0` metric_events (RLS) → `431279836d74` classrooms/classroom_members/classroom_roadmaps (RLS) → `a5e9c3d7f102` learning_events/node_mastery (RLS) → `b6f2d8a1c934` career_goals/node_meta (RLS) → `37714df921de` leetcode_retention_tables (RLS) → **`37714df921df` add_paid_only (head; applied to prod 2026-07-21)**.

**Prod state (checked 2026-07-21 via Supabase MCP):** `alembic_version = 37714df921df` — **prod is at head**. All 24 RLS-eligible public tables (excludes `alembic_version`; 25 total) have `relrowsecurity = true`, confirmed for the 3 new LeetCode tables. Live catalog: **30 roadmaps (29 career + 1 school)** plus one real career-tree roadmap committed during phase 2 verification (owner's account, "Backend Engineer", 63 nodes — kept, not test junk). Evidence spine backfilled against the owner's real review history (31 completed reviews): 1 node-linked → 1 `learning_event` written (a miss, so `m_learned=0` — the honest answer); 30 unmapped (97% — most existing `activities` rows predate node-linking) tracked via `evidence_unmapped` metric events. Phase 2's `backfill_career_mapping.py` re-ran topic mapping over those 30 (well, the 6 still without a mapping suggestion attempt recorded — see §3): **0 auto-mapped, 6 below the 0.55 triage floor** — the owner's real activity topics (e.g. "Mutable and Immutable Objects") don't lexically overlap at all with the Backend Engineer tree's node titles, a genuine signal that the Jaccard-overlap placeholder (not a real embedding call) is too weak once there's real semantic-but-not-lexical drift; noted as the concrete next-upgrade trigger in `topic_mapping.py`'s docstring.

Hardening migration `73c79267ec74` adds CHECK constraints (`reviews.status/rating`, `user_progress.status`, `feedbacks.status`, `ease_factor ≥ 1.3`) and the hot-path indexes (`reviews(user_id,status,scheduled_for)`, `activities(user_id,created_at DESC)`, unique `user_progress(user_id,node_id)`).

---

## 3. Where the code is ahead of the docs (verified deltas)

| Claimed (CLAUDE.md / ARCHITECTURE.md) | Actual (verified 2026-07-10) |
|---|---|
| RLS + review-invariant migrations "pending" in prod | Applied; prod at head `f2b7d3a9c8e4` |
| 6 routers | 9 — `internal`, `prefs`, `tests` added |
| No email/reminder system | Full loop live: `reminder_log` + `mailer/reminders.py` + GH Actions cron + `CRON_SECRET` |
| 10 seeded roadmaps | 33 seed scripts, 30 live in prod |
| School platform = "vision / pitch-gated MVP" | Shipped in-product: `audience` split, `user_prefs`, `AudiencePicker`, Physics 9-10 roadmap + numericals route + content folder |
| Tests section absent | Live: `/api/tests/*`, `test_attempts`, `Tests.jsx`, `_test/` banks, FSRS bridge |
| DSA viz: renderers for stack/list/tree/graph/grid "still needed"; phases 1–7 | 9 renderers exist BUT 6 are unused (List/Tree/Graph/Grid/Interval/Bits — no generator feeds them); **24 registered generators** (array/string/stack/search/sort only) — **NO backtracking or linked-list generators yet** (audit 2026-07-10); prose handoffs through phase 13 |
| Dashboard has one endpoint | `/review-metrics` + `/heatmap` added (`ReviewHeatmap.jsx`) |
| No analytics | PostHog wrapper (`lib/analytics.js`), curated events, key-gated |
| No backend test suite / CI | `tests/test_ownership.py` (tenant isolation) + `reminders.yml` workflow; `Dockerfile` + `.dockerignore` |
| hardening-plan.md Tier-0 open | Done: ES256-only, `echo=DEBUG`, env CORS, required `ADMIN_EMAIL`, boot-guarded bypass, 15 `max_length` caps across request schemas |

---

## 4. Security

### Current posture (what's verifiably in place)

- **ES256-only JWT verification** via JWKS; algorithm-confusion closed; audience + role checked; expired/invalid → 401.
- **Tenant isolation**: every query scoped by `current_user.id`; mutating endpoints verify ownership (`WHERE id=:id AND user_id=:uid`); enforced by an automated test suite (`backend/tests/test_ownership.py`).
- **RLS enabled on all 12 public tables with zero policies** — deliberate: it blanket-denies Supabase's PostgREST/anon-key surface while the backend (table owner) bypasses it. Supabase's advisor flags this pattern as INFO ("RLS enabled, no policy"); that is the intended design, not a gap.
- **DB integrity as defense**: CHECK constraints on status/rating enums, partial unique indexes enforcing review invariants (`e7f2a4c9b1d5`), FK `ondelete` rules.
- **Secrets & config**: `.env` git-ignored; CORS is an explicit env-driven allow-list (no `*`); `ADMIN_EMAIL` has no hardcoded default; SQL echo off in prod; `DEV_AUTH_BYPASS` crashes the boot without `DEBUG`.
- **New env vars (2026-07-13, all follow the empty-default no-op convention)**: `SENTRY_DSN`/`SENTRY_ENVIRONMENT`/`SENTRY_TRACES_SAMPLE_RATE` (Render), `VITE_SENTRY_DSN`/`VITE_SENTRY_RELEASE`/`SENTRY_AUTH_TOKEN`/`SENTRY_ORG`/`SENTRY_PROJECT` (Vercel — `SENTRY_AUTH_TOKEN` is a **build-time** secret, never a client-exposed `VITE_*` var); `VAPID_PRIVATE_KEY`/`VAPID_PUBLIC_KEY`/`VAPID_SUBJECT` (Render only — single source of truth, public key served via API to avoid a Vercel/Render key-sync hazard). Sentry stays pseudonymous both sides (`send_default_pii=False`, user context = Supabase id only, no email) — same convention as PostHog's `identifyUser`.
- **Machine endpoints**: `/api/internal/send-reminders` uses constant-time secret comparison and is fully closed when `CRON_SECRET` is unset; reminder sends are idempotent per user/day via an atomic `INSERT … ON CONFLICT DO NOTHING` claim.
- **Input bounding**: 15 `max_length` caps on request schemas (topic 300, key_memory 500, notes 5000, mistake 2000, …) — bounds both abuse and LLM token spend.
- **LLM gating**: all 5 LLM endpoints 404 unless `GRADER_ENABLED`; LLM output is advisory-only (never auto-applied, never auto-grades the user down).
- **Teacher/classroom authorization (2026-07-16)**: a second, distinct authorization path alongside tenant isolation. `get_owned_classroom` is the IDOR wall for every teacher-scoped route (`WHERE id=:id AND teacher_user_id=:uid`, **404 not 403** — same don't-confirm-existence convention as the rest of the API); membership in a class does not imply ownership of it (tested explicitly in `test_classrooms.py`). Separately, every analytics query's inner scope is `student_user_id IN classroom_members` **AND** content restricted to that classroom's `classroom_roadmaps` — a teacher can never see a student's non-classroom activity (career roadmaps, personal syllabus roadmaps, unassigned free-form cards). This scope rule is enforced **server-side by field selection and the join itself**, never by frontend omission, and is covered by a dedicated privacy-boundary test (seed one assigned-roadmap activity + one unassigned one, assert only the former is visible to the teacher). No `user_prefs.role` column exists — "is a teacher" is defined purely as "owns ≥1 classroom" (see DECISIONS.md).

### Open vulnerabilities / risks — CURRENT

1. **No rate limiting anywhere** (verified: no slowapi/limiter in the codebase). Highest-value targets: the 5 LLM endpoints (`/grade`, `/questions`, `/grade-questions`, `/suggest-key-points`, `/grade-fillup`) — any authenticated user can hammer them into a **Groq cost-DoS**; also `POST /api/feedback/` (spam) and `POST /api/activities/` (row flooding). Input caps bound per-request cost, not request count. *This is the #1 item to fix before opening signups wider.*
2. **Admin gate is an email string compare**, not a role system. Fine for a 3-user cohort; brittle the day a second admin or email change happens. Backend check is authoritative (frontend check is UX-only) — keep it that way.
3. **Prompt-injection surface in the graders**: user-controlled text (`key_memory`, free-recall answers, drafts) is interpolated into Groq prompts, and the returned `feedback`/`revision_note`/`related_subtopics` strings are rendered in the UI. Blast radius is low (advisory-only, JSON-parsed, no tool use), but a crafted card could make the grader emit misleading/hostile text to the user. Mitigation when it matters: treat LLM strings as untrusted display data (they already render as text, not HTML — keep it that way), and consider a moderation/format check.
4. **JWT lives in localStorage** (Supabase JS default) → any XSS = token theft. React's escaping + no `dangerouslySetInnerHTML` on user content is the real defense; re-audit if lesson content ever renders raw HTML.
5. **User content flows to a third party (Groq)** whenever the grader is on — `key_memory` and answers leave your infra. Not a bug, but a disclosure/ToS item before a paid cohort, and a DPDP consideration for the school pilot.
6. **Naive-UTC timestamps** rendered as local time — cosmetic date-label off-by-one near midnight for IST users (known debt; scheduling math unaffected).
7. **Supabase advisor WARN**: leaked-password (HIBP) protection disabled. Moot while auth is Google-only; becomes real if email+password is ever enabled.
8. **Guest surface** (`optionalAuth` reads) — currently read-only roadmap browsing; keep writes behind `requireAuth` as new endpoints are added.

### Watch-list — FUTURE (not bugs today, will bite at the next stage)

- **Opening signups / any marketing push** → rate limiting (slowapi or a Render edge/proxy) becomes blocking, and the Groq/Gemini/Anthropic spend needs a per-user daily budget + alerting (the syllabus extractor's 5/user/day cap is in-memory and resets on redeploy — not a hard budget guard).
- **School pilot with minors** → India DPDP Act: parental consent, data-minimization, and a data-processing story for Groq/PostHog/Resend before any real Class 9-10 cohort.
- **GitHub Actions cron** pauses after 60 days of repo inactivity and can lag ~15 min — reminder emails silently stop if the repo goes quiet; move to a host cron if cadence ever matters.
- **Transaction pooler limits**: `pool_size=5 + max_overflow=10` per instance is fine now; multiple Render instances or heavier traffic can exhaust Supabase pooler slots — revisit before scaling out.
- **Email deliverability/abuse**: Resend sender reputation once volume grows; unsubscribe/compliance (CAN-SPAM-style) before non-trivial sending.
- **Content supply chain**: lessons are Antigravity-generated JSON rendered by the app — the validator checks structure, not pedagogy or malice; keep the human/Claude critique step for anything that ships, and never render lesson fields as raw HTML.
- **PostHog**: once a key is set in prod, it's a third-party script + user-event stream — add it to the privacy story.
- **Single founder key risk**: Supabase/Render/Vercel/Groq/Google/Anthropic accounts + `CRON_SECRET`/`GROQ_API_KEY`/`GEMINI_API_KEY`/`ANTHROPIC_API_KEY` all hang off one identity; password-manager + 2FA hygiene is the actual security perimeter of this product.

---

## 5. Future plans (cross-checked: still genuinely open)

**Near-term product (in priority order, from CLAUDE.md §next — still valid):**
1. **Review-loop friction pass** (`docs/PROMPT-review-ux-friction.md`): lesson-grounded recall questions (use `recall_questions` instead of generic LLM questions — early verdict was "too generic"), visible grading state, outcome-chip descriptors, mobile nav to 5 items, Home Continue-card.
2. **Node-complete → pre-filled Log modal** — closes the roadmap→retention loop (design doc §9b).
3. **Logged-reviews vault** (review history).
4. Persist Track/Activity-Type properly in `ActivityCreate` (currently partially UI-only).
5. Feedback status workflow (new→reviewed→resolved) + real admin auth after the first cohort.
6. **Rate limiting** (see §4 — promote this above the fold).

**Content build order** (per JD research runs 1–3): finish **AI Engineering**, **DSA phases 8+** (stack/queue → linked lists → trees → graphs → DP; viz renderers already exist, content + generators are the remaining work), then **Data Engineering**, then ML Fundamentals. Verbal/vocab aptitude pending. Core-CS OS batch in progress.

**School platform** (Classes 6–12 on the same engine): the product split is **already live** (audience prefs + Physics 9-10 + numericals). Remaining: finish Physics 9-10 content to pitch-quality, then the Gorakhpur/private-school pitch (`docs/PROMPT-school-b2b-research.md`, `school-b2b-research-findings.md`).

**Career Paths v2**: live "% to role X" tracker on top of the static `lib/careerPaths.js` v1.

**Phase 2 engine work**: real Momentum/Retention-Strength metrics (Analytics currently shows honest placeholders), Re-entry Mode, FSRS parameter optimization once there's enough review data.

**SEO**: static lesson pages + sitemap work has started (`useSeo.js`, prebuild sitemap, `docs/PROMPT-seo-static-lessons.md`) — continue toward indexable lesson content as the organic-acquisition channel.

---

## 6. Operational notes

- **Deploy**: Vercel (frontend, SPA rewrite in `vercel.json` is load-bearing for deep links/OAuth) + **Render** (backend; `Dockerfile` present). Moved off Railway 2026-07-11 (free tier ended) — **region unverified; keep it close to Supabase Mumbai (Singapore) for latency**. Deploys from `main`. Never auto-deploy — the founder pushes. Prod env vars now include `GEMINI_API_KEY` / `SYLLABUS_MODEL` / `CAREER_TREE_MODEL` (set on Render). **Gemini is the default LLM provider for both syllabus extraction and career-tree generation (D-037) — an explicit `SYLLABUS_MODEL`/`CAREER_TREE_MODEL` env var on Render OVERRIDES the code default, so both must read `gemini-*` there for the switch to take effect in prod.** `ANTHROPIC_API_KEY` is no longer required.
- **DB**: always the transaction pooler URL (`aws-1-ap-south-1…:6543`); direct host is IPv6-only. Engine already sets `statement_cache_size=0` + `pool_pre_ping`. Alphanumeric DB password only; repeated auth failures trip Supabase's circuit breaker (~minutes).
- **Schema changes**: Alembic only, and **every new table's migration must include `ENABLE ROW LEVEL SECURITY`** (the PostgREST-blocking pattern, migration `f8a3b5c2d9e1`).
- **Repo hygiene** (post-cleanup 2026-07-10): the repo root and `content/` hold **no** Python except `content/validate.py` and `content/scripts/`; `.gitignore` now enforces this (`/*.py`, `content/*.py`, `content/roadmaps/**/*.py`). One-off patch scripts should be written under the session scratchpad or deleted after use — their output (the JSON) is the artifact, not the script.
- **Verification tools**: read-only Supabase MCP is configured (`.mcp.json`) — use it to verify prod schema/migration state instead of trusting docs (this document was built that way).
- **Sentry/Push activation (founder action, not code — 2026-07-13)**: all Sentry/push code ships gated-off (no-op without keys), same as PostHog. To activate: (1) create a Sentry org + 2 projects (backend python/fastapi, frontend react), set `SENTRY_DSN`/`SENTRY_ENVIRONMENT=production` on Render + `VITE_SENTRY_DSN`/`VITE_SENTRY_RELEASE=$VERCEL_GIT_COMMIT_SHA`/`SENTRY_AUTH_TOKEN`/`SENTRY_ORG`/`SENTRY_PROJECT` on Vercel; (2) generate a VAPID keypair once locally (`vapid --gen`, ships with `pywebpush`), both keys → Render only; (3) redeploy both; (4) smoke-test: force an error each side → confirm in Sentry; subscribe on retainhq.app + `workflow_dispatch` the reminders Action → confirm a push notification arrives; check Supabase advisors show no new RLS warnings post-migration.

---

## Appendix — doc freshness

| Doc | Status (2026-07-10) |
|---|---|
| `docs/SYSTEM-OVERVIEW.md` (this file) | **Current — verified against code + prod DB** |
| `CLAUDE.md` | **Rewritten 2026-07-10** as a lean rules + update-routing file (no status). The old ~450-line version is frozen at `docs/CLAUDE-ARCHIVE-2026-07.md` — its status/roadmap-count/DSA-viz sections were already stale (see §3) |
| `docs/claude-code-workflow.md`, `docs/DECISIONS.md`, `docs/BACKLOG.md` | Added 2026-07-10 — session playbook, go-forward decision log, idea inbox |
| `docs/ARCHITECTURE.md` | Topology/auth still broadly right; predates reminders, prefs, tests, audience split, 20+ roadmaps (2026-06-07) |
| `docs/API.md`, `docs/FLOWS.md` | Predate the 3 new routers + dashboard endpoints — **stale** |
| `docs/hardening-plan.md` | Tier-0/1 items largely **done in code**; still useful as the rationale + for unfinished items (rate limiting) |
| `docs/SPEC-test-runtime.md`, `dsa-*`, `content-engine.md`, `design-bible.md` | Recent, written alongside the shipped features — trustworthy |
| `docs/SESSION_HANDOFF.md`, `HANDOFF-*`, `PROMPT-*` | Point-in-time working docs — read with their dates in mind |

---

## Changelog

- 2026-07-20 — **LLM provider consolidated on Gemini (D-037).** `SYLLABUS_MODEL` and `CAREER_TREE_MODEL` now default to `gemini-3.5-flash` (was `claude-opus-4-8`), so `GEMINI_API_KEY` is the only LLM key syllabus extraction and career-tree generation need; `ANTHROPIC_API_KEY` becomes optional. The Anthropic code path is left wired — provider is routed by model-id prefix, so a single env override routes back with no code change. Verified end-to-end against the live API: `gemini-3.5-flash` returns a schema-valid 70-node/10-subject tree through the nested Pydantic `response_schema`. **Render env vars must be updated by hand** — an explicit `SYLLABUS_MODEL=claude-opus-4-8` there wins over the new default.

One line per system-state change, newest first: `YYYY-MM-DD — what changed (sections touched)`.

- 2026-07-21 — LeetCode Retention phase (Prod Actions): 3 new tables added (`problems`, `problem_concepts`, `concept_cards`), updated migration chain and applied `add_paid_only` to prod via manual `alembic upgrade`. Two new Evidence endpoints (`POST /api/evidence/leetcode/solve` and `POST /api/evidence/leetcode/backfill`) implemented to record `PROBLEM_SOLVED` and `evidence_unmapped` metrics, tested successfully up to database routing layer but failing locally due to connection refused. Seeded 16 advanced nodes to the DSA roadmap (`seed_dsa.py`) and ran against prod database via a background task. Updated system documentation (§1, §2).

- 2026-07-20 — Career templates v2 quality pass (`docs/SPEC-career-templates-v2.md`, all 7 steps): fixed the subject-blind diagnostic probe selection (flat priority sort + Python's stable tie-break by file order meant the AI Engineer diagnostic asked 8 math/classical-ML questions and **zero LLM/GenAI questions**; now round-robin across subjects in `_select_diagnostic_probes`, deterministic, +3 tests). Validator gained a probe-spread warning (`default_priority ≥4` subject with no probes → zero diagnostic coverage) which immediately caught backend v1's probe-less `system_design` subject. Three new immutable template files: `ai_engineer.v2.json` 70 nodes (new 5-node `llm_apis` subject — the "working with LLM APIs" layer every 2026 JD calls baseline and the in-repo ai-engineering roadmap already teaches; +reranking/hybrid-retrieval +fine-tune-vs-RAG-vs-prompt; paid for by removing kmeans/PCA/eigenvalues/feature-stores/RNN), `sde_generalist.v2.json` 62 nodes (+the four highest-frequency interview patterns: two-pointers, sliding-window, binary-search, heaps; +greedy), `backend.v2.json` 66 nodes (+heaps/greedy/intervals, +caching probe). v1 files frozen; committed v1 trees unaffected (nothing re-reads a template post-commit). Deliberate deviation from the spec's own step 7: skipped the live throwaway-goal check because creating one would **archive the owner's real Backend Engineer goal** (one-active-goal index, no un-archive endpoint) — verified against the synced runtime artifact via direct function call instead. Suite: 176/176. Market evidence + future-role research prompt in the spec's §8.
- 2026-07-19 — Career Coach Phase 2 — Goal, Tree & Topic Mapping (`docs/SPEC-career-coach-phase2.md`, build order §10, steps 1-10 of 11; docs are step 11): 2 new tables (`career_goals`, `node_meta` sidecar; migration `b6f2d8a1c934`, RLS, **applied to prod**), new `career` router (12 routes: templates, goal CRUD, diagnostic get/submit, tree generate/commit, node patch/delete, unmapped list/assign, sprint), new `services/career_tree.py` (provider-routed generation adapting a role template, §3.3 hard-rule validation + one retry + unmodified-template fallback — never an error screen) and `services/topic_mapping.py` (Jaccard word-overlap Tier-2 match, `AUTO_MAP_THRESHOLD`/`TRIAGE_THRESHOLD`, shared `backfill_activity`), 3 role templates (`content/career-templates/*.v1.json`: backend 63 nodes, ai_engineer 68, sde_generalist 57) each validated by a new `content/validate_career_templates.py`. New `.github/workflows/validate-content.yml`: **the "existing content gate" both phase specs assumed was already wired into CI didn't exist before this** — now gates both validators. Frontend: `/coach` (`CareerCoach.jsx` router → `CareerOnboarding.jsx` 4-step wizard, `CareerTree.jsx` tree view + unmapped triage, reusing phase 1's per-node evidence-log drill-down as the trust feature it was always meant to become). New `backfill_career_mapping.py` (one-shot, mirrors `backfill_evidence.py`'s convention) and `sync_career_templates.py` (manual — see deploy-topology note below). New `test_career.py`: 23 tests (the spec's 19 plus extras, incl. the sprint endpoint). Backend suite: 171/171 passing.
  - **Live end-to-end verification** (not just pytest against SQLite) walked the real onboarding flow in-browser and caught two real bugs SQLite's non-enforced FKs couldn't: `commit_tree` needs staged `flush()` calls between `Roadmap` → `RoadmapNode` → `{NodeMeta, RoadmapNodePrerequisite, CareerGoal update}`, because none of those mapped classes have an ORM `relationship()` to each other, so the unit-of-work has no dependency edge ordering their writes — a single `add_all()`-then-`flush()` could (and, against real Postgres, did) send a child INSERT or the goal's UPDATE before its parent row existed. Recorded in `DECISIONS.md`. Also fixed a frontend crash (`CareerTree.jsx`'s `UnmappedTriage`: `useEffect(load, [])` passed a function that implicitly returned a Promise, which React mistook for a cleanup function).
  - **Deploy-topology gap found and worked around, not fixed at the root**: the spec puts role templates under `content/career-templates/` (matching the existing content pipeline), but Render's backend service deploys with root=`backend/` (§1), so that directory is outside the Docker build context and unreadable at runtime. Worked around with a backend-owned synced copy (`backend/app/data/career_templates/`, `sync_career_templates.py`, manual invocation) rather than changing deploy topology. Two copies now need to be kept in sync by hand when a template changes — flagged, not silently absorbed.
  - **Real-history backfill** (`backfill_career_mapping.py` against the owner's real account): of the 6 activities phase 1 flagged unmapped, **0 auto-mapped, 6 landed below the 0.55 triage floor** — real activity topics (e.g. "Mutable and Immutable Objects") share no vocabulary at all with the Backend Engineer tree's node titles. Honest result, not a bug: the Jaccard-overlap placeholder needs a real embedding call once mapping volume justifies it (already noted in `topic_mapping.py`'s docstring as the upgrade trigger).
  - **A real career tree now exists in the owner's own account** (not test junk — kept on request): "Backend Engineer" goal, 63 nodes across 8 subjects, committed during verification.
  - Missing from the spec's own API table until this pass: `POST /api/career/sprint` was speced (§6) but never implemented in earlier steps — added here with its own test.
- 2026-07-19 — Career Coach Phase 1 — Evidence Spine (`docs/SPEC-career-coach-phase1.md`, build order §9, steps 1-9): 2 new tables (`learning_events` append-only log, `node_mastery` derived cache; migration `a5e9c3d7f102`, RLS, **applied to prod**), new `evidence` router (6 routes: manual log, per-node mastery, per-node event trace, summary, recompute, soft-delete), new pure `services/evidence_weights.py` (weight table + `w()`) and `services/evidence.py` (`fold_events` + DB layer + read-side derivations), Producer A wired into `reviews.py`'s `complete_review` (emits `RECALL_GRADED` post-commit, failure-isolated via a SAVEPOINT — a bare `db.rollback()` was tried first and would have expired the already-eager-loaded `review.activity`, crashing the next line with `MissingGreenlet`; caught by the test suite, not by inspection), dev-only `/evidence` page (founder-gated). Real-history backfill (one-off `backfill_evidence.py`, entity_id-deduped so it can't double-count against future organic completions): of the owner's 31 completed reviews, only 1 was node-linked (30 = 97% unmapped, tracked via `evidence_unmapped` metric events) — that one event was a recall miss, so `m_learned=0` (honest, not inflated). New `test_evidence.py`: 52 tests (the spec's 18 plus extra threshold/property coverage). Backend suite: 148/148 passing. One spec deviation, recorded in `DECISIONS.md`: the fold tracks two independent running totals (capped vs. uncapped evidence, combined by `max()`) rather than the single shared running total the spec's §4.2 pseudocode shows — the single-total version isn't actually order-independent when a capped event lands before the uncapped push that dominates it (§1, §2).
- 2026-07-17 — Teacher dashboard Phase 1, partial (frontend-only — the 3 backend endpoints already existed from Phase 0 but had zero UI): new `TeachStudentDetail.jsx` (`/teach/:id/students/:memberId` — retention stats, 8-week recall trend bar chart, weakest topics, per-node mastery grid, calling the already-built `GET .../students/{member_id}`); `TeachClassroom.jsx` Students tab gained inline rename (pencil → edit → `PATCH .../members/{id}`) and remove (trash → confirm → `DELETE .../members/{id}`) row actions, roster names now link to the drill-down; classroom header gained a join-code regeneration button (`PATCH .../{id}` with `regenerate_join_code`). Verified via client-side `fetch` mock (no prod writes — the one real classroom has 0 students) + one live check against real data (regen button renders/wired, not exercised — it's a real destructive action on a real, possibly-shared join code). Still open from spec Phase 1: join-attempt throttle (explicit `NOTE:` deferral in `classrooms.py`), DPDP one-pager, weekly teacher digest email (§1).
- 2026-07-17 — Agentic-AI batch specced end-to-end (research → critique → contract): `content/research/agentic-ai-curriculum.json` (26 lessons, 5 phases, every docs URL fetch-verified; round-2 patch record in `agentic-ai-round2.json`), `backend/seed_ai_engineering.py` +26 nodes (62 total — **prod reseed pending**, founder-run), PROMPT-engineering.md gained the AGENTIC BATCH rules, `content/HANDOFF-agentic-ai.md` = Antigravity mission (MCP phase first for SEO). No schema/endpoint change (§1 catalog counts change on reseed).
- 2026-07-17 — School science-viz spec (`docs/SPEC-school-science-viz.md`) + M0 executed: `physics-9-10` roadmap (128 lessons) split into `physics-9` (77) + `physics-10` (51), each its own `content/roadmaps/` directory and DB `Roadmap` row — the class-first IA prep for Chemistry/11-12 (D-029/D-030). `SchoolRoadmaps.jsx` (§ frontend layout) needed no changes — it already groups any roadmap's nodes into a Class→Subject→Chapter browser by parsing `"Class N · Chapter"` phase strings, so the split alone fixes Physics previously double-bucketing under both classes. `backend/seed_physics_school.py`/`_prereqs.py` rewritten to seed two roadmaps (132 same-class prereq edges kept; 8 cross-year edges dropped — the roadmaps API has no cross-roadmap prerequisite resolution); **not yet run against prod** (manual hand-off, wipes `user_progress`). `frontend/vercel.json` gained 129 permanent redirects for the old lesson/roadmap URLs. Build verified: 640 static pages (unchanged), 12 hub pages (was 11), title-parity re-confirmed at the new URLs (§1, §2).
- 2026-07-16 — Review-feedback fixes + "Key areas to focus": new `GET /api/dashboard/focus-areas` (recalled=False completions, 14-day window, per-card grouping, cap 5, latest `ai_feedback` per topic) + `FocusAreasCard` on Home (amber accent, renders only with data); Review.jsx "Missed" fixed in both places (per-question chip grey→house alert red `#ba1a1a`; outcome buttons' inline styles bypass the html.dark remap layer, so OUTCOMES gained theme-aware `darkColor`/`darkBorder` resolved via `useTheme` — 'Missed it' `#334155` was invisible on dark); sidebar now open by default on Home only (spacer widens to 240px so the panel is structural there, hover-rail everywhere else). No schema change (§1, §2).
- 2026-07-16 — Teacher dashboard Phase 0 (`docs/SPEC-teacher-dashboard.md`, build-first scope): 3 new tables (`classrooms`/`classroom_members`/`classroom_roadmaps`, migration `431279836d74`, RLS, **not yet applied to prod**), new `classrooms` router (12 routes: roster/lifecycle + overview/gap-map/students/drill-down analytics) behind a fresh `get_owned_classroom` IDOR wall, new pure `services/mastery.py` (node-mastery + at-risk + root-cause logic, unit-tested), `services/learner_stats.py` extracted from `dashboard.py` (review-metrics/heatmap/node-accuracy now reusable with an optional roadmap scope — self-serve behavior unchanged, verified against the existing analytics test suite), new `test_classrooms.py` (IDOR + the privacy-boundary rule tested explicitly) and `test_mastery.py`. Frontend: `/teach`, `/teach/:id`, `/join/:code` (`Teach.jsx`/`TeachClassroom.jsx`/`JoinClassroom.jsx`), Profile.jsx entry points, conditional "Teach" nav item — builds clean. `seed_classroom_demo.py` written and validated (pitch-demo classroom) but **not run against prod**. Backend suite: 96/96 passing. Remaining before this is pitch-ready: apply the migration to prod, run the seed script, and a live browser smoke-test of the new UI (§1, §2, §4).
- 2026-07-13 — Sentry + Web Push/PWA + deeper analytics (`docs/HANDOFF-sentry-push-analytics.md`, 13 commits, all gated-off/no-op until env vars are set): **Sentry** — backend `sentry_sdk.init` + `logging.basicConfig`, frontend `lib/errors.js` + custom `ErrorBoundary.jsx`, sourcemap upload+delete via `sentryVitePlugin` (verified: zero `.map` files ship without `SENTRY_AUTH_TOKEN`). **Web Push/PWA** — `manifest.webmanifest` + icons, push-only `sw.js` (no fetch handler; caught+fixed a cross-origin bug — a static SW file can't read `import.meta.env`, so `push.js` stashes the API origin via Cache Storage), new `push_subscriptions` table (migration `d4f6a8c1e3b7`), `services/push.py` (pywebpush), `/api/push/*` routes, Profile toggle + Review done-screen prompt, reminder fan-out to push under the same `reminder_log` claim. **Analytics** — `reviews.duration_ms` (migration `e7a9c2f4b6d8`, clamped (0,1_800_000]), generic `metric_events` table (migration `f1b3d5e7a9c0`, first producer: syllabus's `extraction_edit_delta`), 5 new `/api/dashboard/*` aggregation endpoints (each with its own `enough_data` floor) + Analytics.jsx UI, PostHog event audit (`pwa_installed`/`syllabus_committed`/`review_skipped`/`feedback_sent` — the last one found a second, previously-untracked call site in `KnowledgeVault.jsx`). Live-Postgres verification (not just the SQLite test harness) caught and fixed a real bug: `source-retention`'s `GROUP BY func.coalesce(...)` used two separately-compiled expressions that SQLite tolerated but Postgres correctly rejected. New env vars in §4; alembic head now `f1b3d5e7a9c0` (3 migrations pending in prod, auto-apply on next deploy). Backend test suite: 45/45 passing (§1, §2, §4, §6).
- 2026-07-13 — Analytics build-out (AARRR + North Star; plan in `docs/analytics-plan.md`). Frontend `lib/analytics.js` event vocab expanded (activation funnel: `signup_started`/`signed_up`/`onboarding_started`/`onboarding_completed`/`first_capture_shown`/`activated`; retention: `reviews_due_shown`/`review_queue_empty`/`reminder_clicked`; engagement: `lesson_completed`/`card_created_from_lesson`; friction: `api_error`) + `trackOnce` helper + `capture_pageleave:true` (Web Analytics bounce/duration). New backend `services/analytics.py` (server-side PostHog) emits `review_scheduled` (activities + reviews) and `reminder_sent` (reminders, link tagged `?src=reminder`). New env `POSTHOG_API_KEY`/`POSTHOG_HOST`; `posthog>=3.7.0` dep; flush on app shutdown (§1 topology/services/frontend). **No schema change.**
- 2026-07-12 — Syllabus commits capped at **3 per user lifetime** (`SYLLABUS_LIFETIME_LIMIT`; new `user_prefs.custom_roadmaps_created` counter, migration `c8e2a7f5d1b9`, atomic claim, delete ≠ refund) + `GET /api/syllabus/quota`; Home gains a "Customize your own" tile → `/roadmaps/new`; upload page shows remaining quota and blocks at 0. Content plan doc added: `docs/PLAN-lesson-generation.md` (Sonnet-drafted, founder to review) (§1–2).
- 2026-07-12 — Question mode goes persistent + topic-grounded: new `question_sets` table (migration `b3d9f1a4c6e2`, 13th table; RLS) — LLM sets generated with per-question `reference_answer`s, reused ≥2 review sessions shuffled, then regenerated; node-linked cards get topic-grounded generation with a user-facing `depth` choice ('main'|'deep', Review.jsx toggle); `/grade-questions` injects stored references server-side. Superseded key_memory-only `generate_questions` removed from `grader.py`. New tests `test_question_sets.py` (§1–2).
- 2026-07-12 — Syllabus: added `POST /api/syllabus/extract-text` (pasted text ≤40K chars, token-cheap default; shares the daily limit) + paste-text tab as the primary input in `SyllabusUpload.jsx`; `services/syllabus.py` grew `extract_roadmap_from_text` with shared `_finalize` guardrails (§1).
- 2026-07-11 — Backend deploy host moved **Railway → Render** (Railway free tier ended); env vars (incl. `ANTHROPIC_API_KEY`/`GEMINI_API_KEY`/`SYLLABUS_MODEL`) re-set on Render. Docs updated across SYSTEM-OVERVIEW §1/§4/§6, CLAUDE.md, README, BACKLOG. **Render region unverified** — keep near Supabase Mumbai (Singapore) for latency (§6, watch-list).
- 2026-07-11 — Syllabus → personal roadmap feature: `syllabus` router (extract/commit/delete, review-before-commit), `services/syllabus.py` (**provider-routed by `SYLLABUS_MODEL`**: Anthropic `claude-opus-4-8` or Google Gemini via `google-genai`; new `ANTHROPIC_API_KEY`/`GEMINI_API_KEY`/`SYLLABUS_*` env), migration `a1c5e8f2d7b3` `roadmaps.user_id` (**applied to prod 2026-07-11; file committed same day after it crash-looped the deploy while uncommitted**), roadmap visibility = catalog-by-audience + own, `/roadmaps/new` UI (§1, §2).
- 2026-07-11 — Agent audit vs code + prod DB: prod re-verified (head `f2b7d3a9c8e4`, RLS ×12, 30 roadmaps/1689 nodes); fixed validate.py kind list (was 5, actually 9 branched + base); DSA renderer-feed status precised (4 unfed, backtracking feeds Tree/Grid). Backend §1–2 verified clean.
- 2026-07-11 — Frontend layout: documented `SchoolRoadmaps.jsx` Class→Subject→Chapter browser + roadmap search (shipped in commit `b94d233` on 07-10 without a doc update; rationale in `DECISIONS.md` D-006).
- 2026-07-10 — Docs-layer restructure: CLAUDE.md slimmed to rules + update-routing (old version → `docs/CLAUDE-ARCHIVE-2026-07.md`); added `docs/claude-code-workflow.md`, `docs/DECISIONS.md` (D-001), `docs/BACKLOG.md` (Appendix updated).
- 2026-07-10 — Document created from a full code + prod-DB verification pass; repo cleanup (one-off scripts purged, `.gitignore` hardened); living-doc convention added to CLAUDE.md.
