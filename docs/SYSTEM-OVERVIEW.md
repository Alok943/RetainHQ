# RetainHQ — System Overview

**Last full verification: 2026-07-10** (every claim checked against the actual code and the live production database via the read-only Supabase MCP).

> **This is a LIVING document — the single source of truth for system state.** Convention (also in CLAUDE.md): any commit that adds/removes an endpoint or router, changes `models/models.py` or adds a migration, adds a service/integration, changes deploy topology, or moves the security posture **must update the relevant section here and add a Changelog line below, in the same commit**. Claims about prod state (migration head, catalog counts) must be re-verified via the Supabase MCP, not assumed. Roughly monthly — or before any launch/pitch — re-run a full verification pass and bump the date above. It supersedes the stale parts of `ARCHITECTURE.md` (last touched 2026-06-07), `API.md`, and the status sections of `CLAUDE.md`. See the [Stale-docs appendix](#appendix--doc-freshness) at the bottom for what to trust elsewhere.

RetainHQ is a learning-retention platform: **Log Activity → Capture Key Memory → Schedule Reviews (FSRS) → Active Recall → Retain**, with a growing library of interactive lessons (Python/SQL/DSA/aptitude/Core-CS/Physics) that feed cards into that loop. Solo-founder build, live in production at `retainhq.app`.

---

## 1. Architecture

### Topology

```
React SPA (Vercel, root=frontend/)  ←  retainhq.app (apex + www)
  ├─ Supabase Auth (Google OAuth) ──► ES256 JWT
  └─ apiFetch + Bearer JWT ──► FastAPI (Render, root=backend/)
                                  └─ asyncpg ──► Supabase Postgres (Mumbai, transaction pooler :6543)
GitHub Actions (daily 01:30 UTC) ──X-Cron-Secret──► POST /api/internal/send-reminders ──► Resend (email) + Web Push (browser)
PostHog (product analytics): frontend `posthog-js` (no-op without VITE_POSTHOG_KEY) + backend `posthog` (server-truth events, no-op without POSTHOG_API_KEY); same project, keyed by Supabase user id
Sentry (error tracking): frontend `@sentry/react` (no-op without VITE_SENTRY_DSN, sourcemaps uploaded+deleted gated on SENTRY_AUTH_TOKEN) + backend `sentry-sdk` (no-op without SENTRY_DSN); pseudonymous (id only, no email/PII) both sides
Web Push (PWA): hand-rolled push-only `public/sw.js` (no fetch handler — SPA stays online-only) + `lib/push.js`; VAPID keys on Render only, public key served via GET /api/push/vapid-public-key
```

- The frontend talks **only** to FastAPI (`frontend/src/lib/api.js` → `apiFetch`, attaches the Bearer JWT; `optionalAuth` allows guest reads). No direct `supabase.from(...)` DB calls exist in React — Supabase on the client is auth-only.
- FastAPI is the single DB gateway. It connects as `postgres` (table owner), which bypasses RLS; RLS exists to block Supabase's auto-exposed PostgREST/anon-key path (see §4).
- Heavy interactive compute is **client-side**: Pyodide (Python step-tracer), PGlite (in-browser Postgres for SQL lessons), and a pure-JS DSA trace player. Zero server compute for lessons.

### Auth (verified in `backend/app/core/security.py`, `api/deps.py`)

- Supabase Google OAuth issues an **ES256** JWT; FastAPI verifies it via `PyJWKClient` against Supabase's JWKS (keys cached 1 h). **HS256 is removed** (`algorithms=["ES256"]` only — algorithm-confusion attack closed). JWKS outage → clean 503, not a 500.
- Claims checked: signature, `exp`, `aud="authenticated"`, `role == "authenticated"`. `sub` (string UUID) is cast to `uuid.UUID` before queries.
- `get_current_user` → `SupabaseUser` (attribute access). `get_optional_user` powers guest exploration. `get_admin_user` = case-insensitive email match against `ADMIN_EMAIL` (403 otherwise) — interim founder gate, not a role system.
- **`DEV_AUTH_BYPASS` is boot-guarded**: `config.py`'s model validator raises at startup unless `DEBUG=true` is also set, so a stray flag in the prod env (Render) crashes the deploy instead of silently handing admin to anonymous requests.

### Backend layout (`backend/app/`)

| Piece | Contents |
|---|---|
| `main.py` | App, env-driven CORS allow-list, `/health`, `/me`, mounts **15 routers**; `logging.basicConfig(INFO)` + gated `sentry_sdk.init` |
| `core/` | `config.py` (pydantic-settings + the bypass boot guard; **the single place any model id is written — enforced by `tests/test_llm_routing.py`**; **+ `MAX_CLASSROOMS_PER_TEACHER=20`**, **+ `CAREER_TREE_MODEL`=`gemini-3.6-flash`/`CAREER_TREE_DAILY_LIMIT`=3**, **+ `COMPANION_LITE_MODEL`=`gemini-3.5-flash-lite`** — the companion's high-volume classify/segment calls; `gemini-3.6-flash-lite` does not exist, see D-047; **+ `GRADER_MODEL`=`gemini-3.5-flash-lite`** (replaces `GROQ_MODEL`; grader + question gen, D-058), **+ `APPROACH_MODEL`=`gemini-3.5-flash-lite`/`APPROACH_CODE_MAX_CHARS`=8000**, **+ `EMBEDDING_MODEL`=`gemini-embedding-001`**), `database.py` (async engine: pooler-safe `statement_cache_size=0`, `pool_pre_ping=True`, `echo=DEBUG` only), `security.py` |
| `api/routes/` | `activities`, `reviews`, `dashboard`, `roadmaps`, `admin`, `feedback`, **`internal`** (cron-only), **`prefs`** (audience), **`tests`** (test banks), **`syllabus`** (PDF → personal roadmap), **`push`** (VAPID key + subscribe/unsubscribe), **`metrics`** (allowlisted client event capture), **`classrooms`** (2026-07-16 — teacher dashboard: roster/lifecycle + class analytics, see below), **`evidence`** (2026-07-19 — Career Coach Phase 1 evidence spine, see below; 2026-07-21 added `/leetcode/solve` and `/leetcode/backfill`), **`career`** (2026-07-19 — Career Coach Phase 2, goal/tree/diagnostic/mapping, see below), **`companion`** (Companion Phase 1 — browser extension ambient evidence, `docs/SPEC-companion-phase1.md`; `POST /api/companion/sessions` batch-syncs stitched study sessions as `TIME_BLOCK`/w=0 events, school-audience 403, `COMPANION_MAX_SESSIONS_PER_DAY=50`, classification ladder rungs 2-3 via `topic_mapping`/`llm_classifier`; **undocumented here until 2026-07-26** — shipped earlier but this file was never updated for it) |
| `services/` | `scheduler.py` (FSRS-4.5: `apply_fsrs`, `FSRS_WEIGHTS`, `DESIRED_RETENTION=0.9`, `REVIEW_SESSION_CAP=10`), **`llm.py`** (2026-08-02 — the provider seam: `call_json(model=...)` routes `gemini*` → Google `response_schema`, anything else → Groq `json_object`; `is_configured(model)` so features gate off cleanly. `syllabus.py`/`career_tree.py` still carry their own copies, see BACKLOG), `grader.py` (recall grading, question mode, capture assist, fill-up grading — provider follows `GRADER_MODEL` via `llm.py`, no longer Groq-hardwired), **`approach_inference.py`** (2026-08-02 — one lite-tier Gemini call resolving a user's pasted LeetCode solution onto the closed set of nodes already mapped to that problem, + concrete facts about the code; every failure returns `None` and capture proceeds unchanged), **`mailer.py`** (Resend), **`reminders.py`** (claim-then-send daily batch; fans out to email + Web Push under the same `reminder_log` claim; emits `reminder_sent` per channel), **`analytics.py`** (server-side PostHog: `capture()`/`shutdown()`, no-op without `POSTHOG_API_KEY`; emits `review_scheduled`, `reminder_sent`), **`push.py`** (pywebpush wrapper: `is_configured()`, `send_push()` raising `PushGone`/`PushError`, no-op without VAPID keys), **`metrics.py`** (`record_metric_event()` — never commits, rides the caller's transaction), **`test_scoring.py`**, **`syllabus.py`** (syllabus-PDF → draft roadmap; **provider-routed by `SYLLABUS_MODEL`** — `gemini*` id → Google `google-genai` inline-PDF + `response_schema`, else Anthropic `claude-opus-4-8` document block + structured outputs/streaming; both send the same prompt+schema so the two can be A/B'd on quality; commit records `extraction_edit_delta` via `metrics.py`), **`mastery.py`** (2026-07-16 — pure functions: `compute_node_status`, `class_cell_status`, `find_root_cause`, `at_risk_flag`; no DB access, unit-tested via goldens), **`learner_stats.py`** (2026-07-16 — `get_review_metrics`/`get_heatmap`/`get_node_accuracy` extracted from `dashboard.py`, parameterized by `(db, user_id, roadmap_scope)`; `roadmap_scope=None` = today's self-serve behavior unchanged, a roadmap-id list = the teacher drill-down's scoped view), **`evidence_weights.py`** (2026-07-19 — `EVENT_TYPES`/`TRUST_TIERS`/`SOURCES` enums + `w()` weight-table lookup + `apply_event_weight()` single-step fold formula; pure, no DB), **`evidence.py`** (2026-07-19 — `fold_events()` pure two-stream replay (see `DECISIONS.md` for the spec-deviation rationale) + `record_event`/`recompute_node`/`recompute_user` DB layer + `node_state`/`confidence`/`retrievability_for` read-side derivations), **`career_tree.py`** (2026-07-19 — Career Coach Phase 2: template loader (`backend/app/data/career_templates/`, a synced copy — see below), `generate_career_tree()` provider-routed model call adapting a role template with §3.3 hard-rule validation + one retry + unmodified-template fallback, `_restore_diagnostic_fields()` always overwrites the model's diagnostic_probe/answer from the template — never trusted from generation output), **`embeddings.py`** (`embed()`/`embed_batch()`/`similarity()` — Gemini embedding calls with their own timeout; the Jaccard placeholder this replaced is gone), **`topic_mapping.py`** (2026-07-19, **rewritten onto real embeddings** — `suggest_node()` cosine-matches against `node_meta.embedding` (JSONB column, migration `fd9b3eaf3c37`, populated by `embed_batch` during `commit_tree`) with `AUTO_MAP_THRESHOLD=0.75`/**`TRIAGE_THRESHOLD=0.65`** — the old 0.55 band no longer exists. **Nothing auto-maps despite the constant's name**: tier 2 only returns a *suggestion* (`tier: 'auto' | 'triage'`) and assignment is always a manual action, so no event carries `mapping_confidence` — suggest-and-confirm is the intended final design, not a way-station (Q2 in `docs/BUGFIX-career-coach-docs-review.md`). `candidate_nodes_for_user()` scopes to personal roadmaps + `goal.roadmap_id` and therefore **excludes attached catalog roadmaps** (BACKLOG); `backfill_activity()` shared by the live `/unmapped/{id}/assign` route and the bulk backfill script), **`topic_segmentation.py`** (2026-07-27, D-047 — `segment_session_topics()`: one lite-model call splitting a companion chat session into ≤5 model-written topic labels; returns `[]` on any failure so session sync degrades to the single-event path), **`planner.py`** (2026-07-26 — Career Coach Phase 3, `docs/SPEC-career-coach-phase3.md` §3: pure `build_plan()` scheduler core — no DB, no `datetime.now()`, no randomness. Named constants incl. `PREREQ_UNLOCK_THRESHOLD`, `URGENCY_MAX`, `BALANCE_FLAG_THRESHOLD`, `BALANCE_MIN_WINDOW_MINUTES` (an evidence floor added during implementation, not in the parent spec — suppresses the balance nudge entirely below 120 logged minutes in the trailing 21-day window, since at zero evidence every subject's balance reads as `-p_s` and the largest subject in the tree always trips `-0.15` on day one), `PLAN_HORIZON_DAYS=180` (urgency's timeline anchor, since goals have no stored start date). 20-scenario golden suite (`tests/test_planner.py`, G1-G10 incl. 2 property tests × 50 seeds) pins every behavioral promise; 3 committed exact-output snapshots), **`llm_classifier.py`** (Companion rung 3: `classify_session()` — Gemini Flash-Lite session classifier, never invents a node (hallucination check against the rung-2 shortlist), **2026-07-26 added a `consent_tier`/`content` gate**: refuses to process a content-bearing field unless `consent_tier == "cloud"` — no producer sets `content` yet, so this is scaffolding for the phase-6 feature that will, written now while the invariant is cheap to keep true) |
| `models/models.py` | Single source of truth — **26 SQLModel tables** (27 physical, incl. `alembic_version` — see §2) |
| `alembic/versions/` | **40 migrations; local head `a3f8c1d92b47`** (`drop_attached_roadmap_node_meta`, D-049 — **written, not yet applied to prod**). **Prod verified at `d5f1a7c3e284`** via Supabase MCP 2026-07-28 — one migration behind local |
| `tests/` | `test_ownership.py` (cross-tenant isolation), `test_question_sets.py` (question-set reuse/scoping/reference-answer injection), `test_syllabus_quota.py` (lifetime cap, no delete-refund), `test_push.py`, `test_review_duration.py`, `test_metrics.py`, `test_dashboard_analytics.py`, **`test_classrooms.py`** (2026-07-16 — IDOR/ownership + the classroom privacy-boundary rule, explicitly tested per spec), **`test_mastery.py`** (pure-function goldens), **`test_evidence.py`** (2026-07-19 — the 18-test suite from SPEC-career-coach-phase1 §8), **`test_career.py`** (2026-07-19 — the 19-test suite from SPEC-career-coach-phase2 §9 plus extras: templates/validator, generation validation+fallback, atomic commit, diagnostic replay, mapping tiers, evidence-survival on node delete/goal switch, IDOR); **`test_planner.py`** (2026-07-26 — the 20-scenario Phase-3 golden suite, G1-G10 + 2 property tests × 50 seeds + `goldens/plan_g*.json` exact-output snapshots), **`test_career_today.py`** / **`test_career_roadmaps.py`** (2026-07-26 — `/today` and the attach/detach routes, incl. attach-during-onboarding), **`test_problems.py`** (2026-07-27 — incl. `test_marking_writes_no_evidence`, the load-bearing D-046 guarantee), **`test_leetcode_evidence.py`**, **`test_llm_classifier.py`**, **`api/routes/test_companion.py`** (2026-07-27 — 24 tests: session sync, chat-topic and YouTube-chapter fan-out, the weight-0 invariant under fan-out, transport-field 422s, idempotency); SQLite via JSONB→JSON variant (Postgres-only paths — `auth.users` joins, `jsonb_array_elements`, `extract(hour from…)` — get an auth-gate/dialect-error smoke test instead of a full round-trip, documented in `conftest.py`); `pytest.ini`, `.dockerignore`, `Dockerfile`. **378/378 passing** as of 2026-07-28 (plus 68 extension vitest tests under `extension/src/`) |
| `seed_*.py` | **34 seed scripts** (idempotent, fixed UUIDs): 31 roadmap seeds + 2 prereq-edge seeds (`python_swe_prereqs`, `physics_school_prereqs`) + **`seed_classroom_demo.py`** (2026-07-16 — the pitch-demo classroom: "Class 9-A Physics", 12 members/3 personas against `physics-9-10`; written and validated but **not yet run against prod** — run it yourself when ready) |
| `sync_career_templates.py` / `backfill_career_mapping.py` | (2026-07-19, one-off/manual scripts, not wired into any build hook) `sync_career_templates.py` copies `content/career-templates/*.json` → `backend/app/data/career_templates/` — **run by hand whenever a template changes, before deploying** (Render's backend root is `backend/`, so the sibling `content/` dir is outside the Docker build context; see its docstring). `backfill_career_mapping.py <user_id>` re-runs topic mapping over every activity phase 1 flagged `evidence_unmapped`, auto-writing ≥`AUTO_MAP_THRESHOLD` matches and leaving the rest for in-app triage, then calls `recompute_user` |

### API surface (all verified against route decorators)

| Endpoint | Notes |
|---|---|
| `GET/POST /api/activities/` | Vault list; create card (FSRS init, first review +1d, first-ever = demo due now; `source_type='lesson'` idempotent per node) |
| `POST /api/activities/suggest-key-points` | Capture assist (gated `GRADER_ENABLED`, else 404) |
| `GET /api/reviews/due` | Capped at 10, oldest first |
| `POST /api/reviews/{id}/complete` | Advances FSRS, schedules next; then (2026-07-19) emits a `RECALL_GRADED` evidence event if `activity.node_id` is set, else counts an `evidence_unmapped` metric — see `/api/evidence/*` below |
| `POST /api/reviews/{id}/grade`, `/questions`, `/grade-questions` | LLM grader + question mode (all gated on `GRADER_ENABLED`). `/questions` serves a **persisted `question_sets` row** (reused `QUESTION_SET_REUSE`=2 sessions, shuffled each serve, then regenerated; optional body `{depth: 'main'\|'deep'}`); node-linked cards get **topic-grounded** generation (node title+description is the contract, key_memory only biases), free-form cards stay key_memory-grounded. `/grade-questions` injects the set's stored `reference_answer`s server-side (never sent to the client) |
| `GET /api/dashboard/` + **`/review-metrics`** + **`/heatmap`** | The last two are newer than CLAUDE.md |
| **`GET /api/dashboard/source-retention`** · **`/calibration`** · **`/memory-strength`** · **`/node-accuracy`** · **`/time-of-day`** | 5 aggregation endpoints (2026-07-13), each with its own `enough_data` floor (sparse history → honest "not enough yet", never a misleading number). `node-accuracy` (`jsonb_array_elements`) and `time-of-day` (`extract(hour from…)`) are Postgres-only |
| **`GET /api/dashboard/focus-areas`** | Home's "Key areas to focus": topics with `recalled=False` completions in the last 14 days, grouped per card, most-missed first, capped at 5; carries the latest `ai_feedback` line per topic. Empty list → the Home section doesn't render |
| **`GET /api/push/vapid-public-key`** · **`POST`/`DELETE /api/push/subscribe`** | Web Push subscription lifecycle. Vapid key 404s when unconfigured; subscribe upserts by `endpoint` (unique — a device re-subscribing under a different account intentionally reassigns ownership) |
| **`POST /api/metrics/events`** | Allowlisted client-side metric capture (`CLIENT_EVENT_TYPES`, currently `{review_depth_chosen}`, 2KB payload cap) — everything else goes through `record_metric_event()` server-side |
| `GET /api/roadmaps/` (+`{id-or-slug}`, `{id}/blockers`, `PUT nodes/{id}/progress`) | List is **filtered by the caller's `user_prefs.audience`** (career vs school) **plus the caller's own personal roadmaps** (`user_id`); personal roadmaps resolve only for their owner (404 otherwise) |
| **`POST /api/syllabus/extract` · `/extract-text` · `/commit` · `GET /quota` · `DELETE /api/syllabus/{id}`** | Syllabus upload → personal roadmap. Extract = PDF (≤10 MB) → LLM → draft JSON, **nothing saved**; extract-text = pasted syllabus text (≤40K chars, the token-cheap default in the UI — no per-page document tokens; also how DOCX is handled: user copies the text) → same draft; both share the ≤5/user/day in-memory limit. Commit = user-edited draft → `roadmaps(user_id)` + nodes, **capped at `SYLLABUS_LIFETIME_LIMIT`=3 per user LIFETIME** (atomic claim on `user_prefs.custom_roadmaps_created`; delete ≠ refund; 403 when spent); quota = {used, limit, remaining} for the UI; delete = own roadmaps only (activities keep history, links nulled). Extraction gated on the selected provider's key — `GEMINI_API_KEY` or `ANTHROPIC_API_KEY` (404 when unset) |
| `POST /api/feedback/` · `GET /api/admin/funnel` · `GET /api/admin/feedback` | Feedback + founder admin |
| **`POST /api/internal/send-reminders`** | No user JWT — `X-Cron-Secret` header, `hmac.compare_digest`, closed entirely if `CRON_SECRET` unset. Driven by `.github/workflows/reminders.yml` (01:30 UTC daily + manual dispatch) |
| **`GET/PUT /api/prefs/`** | Server-side audience preference ('career' \| 'school') |
| **`GET /api/tests/weights` · `POST /api/tests/grade-fillup` · `POST /api/tests/attempts`** | Tests section: per-node weights from last 10 attempts, LLM fill-up grading (gated), attempt submission — **bridges into FSRS** (`apply_fsrs`, misses → Rating.Again) |
| **`/api/classrooms/*`** (2026-07-16, teacher dashboard, 12 routes) | `POST /` create (caller = teacher, `MAX_CLASSROOMS_PER_TEACHER=20`) · `GET /mine` (teaching + enrolled) · `POST /join` (join-code → membership upsert, 404 on unknown/archived alike) · `PATCH /{id}` · `DELETE /{id}/members/{member_id}` · `DELETE /{id}/membership` (student leaves) · `PATCH /{id}/members/{member_id}` · `PUT /{id}/roadmaps`. Analytics (teacher-owner only, via a `get_owned_classroom` 404-not-403 IDOR wall): `GET /{id}/overview`, `/gap-map?roadmap_id=` (per-node mastery via `services/mastery.py`, root-cause via `roadmap_node_prerequisites`), `/students` (roster + at-risk flag+reasons), `/students/{member_id}` (drill-down via `services/learner_stats.py` scoped to the classroom's assigned roadmaps). Every analytics query scopes to `student_user_id IN classroom_members` **AND** the classroom's assigned-roadmap set — a teacher can never see a student's non-classroom activity (career roadmaps, personal syllabus roadmaps, unassigned cards); see §4 |
| **`/api/evidence/*`** (2026-07-19, Career Coach Phase 1 — Evidence Spine, `docs/SPEC-career-coach-phase1.md`, 8 routes) | `POST /manual` (Producer B: `TIME_BLOCK`/`T4_claimed`, always w=0, node-visibility checked) · `GET /nodes` (per-node `m_learned`/`r`/`m`/`state`/`confidence`/`evidence_count`, optional `?roadmap_id=`) · `GET /nodes/{id}/events` (per-event weight + running `m_learned` — the "why is this number what it is" trace) · `GET /summary` (`total_events`/`by_tier`/`by_type`/`unmapped_events`/`nodes_with_evidence`/`weights_version`) · `POST /recompute` (full replay for the caller, idempotent) · `DELETE /events/{id}` (soft-delete + recompute) · **`POST /leetcode/solve`** and **`POST /leetcode/backfill`** (2026-07-21 — records LeetCode extension signals as `PROBLEM_SOLVED` and unmapped slugs as `evidence_unmapped` metrics; 2026-08-02 — backfill gained optional `solved_at: {slug: datetime}` so a windowed import keeps each problem's real solve date instead of stamping the whole batch with import time. Normalised to naive-UTC and clamped to `now` for future dates. Absent slugs still fall back to import time, so an older extension build is unaffected) 2026-08-02 — **`POST /neetcode/solve`** added, a second verified-solve producer (source='neetcode') sharing `_record_verified_solve` with the LeetCode route; both routes' `Problem` lookups are now scoped by `source` as well as `slug` (previously an unscoped, latent cross-source bug). No `/neetcode/backfill` exists — there is no way to fetch a signed-in user's historical NeetCode solves without an account.. Producer A lives in `POST /api/reviews/{id}/complete` (below), not here — it emits `RECALL_GRADED` after the FSRS commit, wrapped in a SAVEPOINT (not a bare `db.rollback()`, which would expire the already-eager-loaded `review.activity` and crash the next line) so an evidence bug can never break review completion |
| **`/api/career/*`** (2026-07-19, Career Coach Phase 2 — Goal, Tree & Topic Mapping, `docs/SPEC-career-coach-phase2.md`, **19 routes** after phase 3 + attached roadmaps) | `GET /templates` (role_key/title/version/node_count, latest version per role) · `POST /goals/` (create, archives any existing active goal — partial unique index `(user_id) WHERE status='active'` is the DB-level backstop) · `GET /goals/active` (404 if none) · `GET /diagnostic` (5-8 probes drawn from the active goal's template — never generated fresh; selection is **subject-aware round-robin** since 2026-07-20, see `_select_diagnostic_probes` — a flat priority sort was subject-blind and served the AI Engineer 8 math/classical questions and zero LLM ones) · `POST /diagnostic/submit` (grades via the existing `grade_recall`, 404 if `GRADER_ENABLED` is off; results held client-side, not written yet) · `POST /tree/generate` (draft only, **writes nothing**, rate-limited `CAREER_TREE_DAILY_LIMIT`=3/user/day in-memory) · `POST /tree/commit` (draft → `Roadmap`+`RoadmapNode`+`NodeMeta`+prerequisite rows in one transaction via **staged flushes** — see `DECISIONS.md` for why — pins `template_version`, replays held diagnostic results into ordinary `RECALL_GRADED` learning_events keyed by `uuid5(goal.id, stable_key)` so a resubmitted commit can't double-count) · `PATCH /nodes/{id}` / `DELETE /nodes/{id}` (edit sets `user_edited=true`; delete is a **soft detach** — nulls `learning_events.node_id`/`activities.node_id` with a `payload.detached_from_stable_key` marker, never deletes the event, then deletes the `RoadmapNode` row — required because neither FK carries `ondelete=CASCADE`, confirmed at migration time) · `GET /unmapped` / `POST /unmapped/{activity_id}/assign` (Tier-3 triage bucket with a Tier-2 suggestion when the active tree has one; assign retroactively writes every skipped review as a `RECALL_GRADED` event and recomputes) · `POST /sprint` (parent A3: stores a temporary priority-override node + end date on the active goal; **consumed by phase 3 below**) · **`GET /today`** (2026-07-26 — Phase 3: assembles `PlanInputs` in one query pass — nodes LEFT JOINed to `node_meta`/`node_mastery`, prerequisite edges, trailing-21-day per-subject `learning_events.duration_min` shares + raw total, due reviews capped at `REVIEW_SESSION_CAP`; lazily clears an expired sprint before building inputs — and hands off to `services/planner.build_plan()`; 404 without a goal or committed tree) · **`PATCH /goals/active`** (2026-07-26 — sets `daily_minutes`, clamped 30-240 server-side rather than 422ing on overshoot) · **`POST /today/feedback`** (2026-07-26 — `{item_ref, action}` → a `plan_item_feedback` metric event; mutates no plan state, the plan is derived and always recomputed fresh) · **`GET /roadmaps/` · `POST /roadmaps/` · `GET /roadmaps/available` · `DELETE /roadmaps/{roadmap_id}`** (2026-07-26, D-042; mechanism fixed 2026-07-28, D-049 — attach an inbuilt-catalog or personal roadmap to the active goal so the planner can draw from it; cap `MAX_ATTACHED_ROADMAPS`=5; guarded by `_get_active_goal` (tree **not** required, so the picker works during onboarding) unlike `/today`'s stricter `_get_active_goal_with_tree`. **Writes no `node_meta` row** — the `career_goal_roadmaps` link (`subject`/`default_priority`) is the only per-attachment state, applied to every node in that roadmap at `/today` read time; `/today`'s node query and its balance-window aggregation both synthesize from that link for attached nodes rather than joining `node_meta`, which attached nodes no longer have. This closes A1 (`docs/BUGFIX-career-coach-docs-review.md`) — two users attaching the same catalog roadmap no longer share any row to collide over. Migration `a3f8c1d92b47` deletes the pre-fix `attached.%` sidecars (148 in prod, **written, not yet applied**)) |
| **`/api/problems/*`** (2026-07-27, `docs/IMPLEMENTATION-problem-capture.md`, D-046) | `GET /by-node/{node_id}` (optional auth — `problems`/`problem_concepts` are an unscoped shared catalog; ordered primary → supporting → alternative, then difficulty, then `external_id`; includes the caller's `problem_attempts` marks when authenticated; a node with zero mapped problems returns `[]`, not 404) · `POST /{problem_id}/mark` (`{status}`, upsert, 204) · `DELETE /{problem_id}/mark` (204). Deliberately writes only `problem_attempts` — never `learning_events`, never `node_mastery` — a manual click is a completion checkbox, not evidence (see D-046) |

### Frontend layout (`frontend/src/`)

- **Shell/routing** (`App.jsx`): `/dashboard`, `/reviews`, `/log`, `/roadmaps(/:id)(/learn/:slug)`, **`/roadmaps/new`** (`SyllabusUpload.jsx` — upload → editable draft → commit; "Bring Your Own Path" card on `/roadmaps` links here; personal roadmaps render in a "Your Roadmaps" group with delete), **`/roadmaps/:slug/numericals/:phase`** (Physics), **`/roadmaps/:slug/test/:phase`** (Tests), `/paths` (`CareerPaths.jsx` — static role-transition content, unrelated to Career Coach despite the name), **`/coach`** (2026-07-19 — `CareerCoach.jsx` router: 404 on `GET /goals/active` → `CareerOnboarding.jsx` (4-step wizard: role cards → diagnostic, prominently skippable → tree review, subjects collapsed by default with inline priority/effort/title edit + delete, regenerate-with-context → confirm/commit); goal with `roadmap_id` set → `CareerTree.jsx` (nodes grouped by subject via `GET /api/roadmaps/{id}` + `GET /api/evidence/nodes?roadmap_id=`, merged client-side; tap a node → the phase-1 evidence-log drill-down, same endpoint as `/evidence`; unmapped-activity triage inline, "dismiss" is session-only client state — no backend endpoint for it)), `/vault`, `/analytics`, `/profile`, `/admin` (founder-only), **`/evidence`** (founder-only, 2026-07-19 — `Evidence.jsx`, dev-only instrument for the Career Coach evidence spine: `GET /nodes` table + per-node event drill-down + `POST /recompute` button; plain/unstyled by design, not a product surface), `/dsa-dev` (dev player), **`/teach`, `/teach/:id`, `/teach/:id/students/:memberId`, `/join/:code`** (2026-07-16 Phase 0 + 2026-07-17 Phase 1 partial, `docs/SPEC-teacher-dashboard.md`: `Teach.jsx` class list + create, `TeachClassroom.jsx` Overview/Gap-Map/Students tabs — Students now wires the 3 endpoints that existed but were unused (member rename/remove inline in the roster, join-code regeneration next to the copy chip) — `TeachStudentDetail.jsx` (new, 2026-07-17) the per-student drill-down (retention stats, 8-week recall trend, weakest topics, per-node mastery grid), `JoinClassroom.jsx` student consent + join; a "Teach" nav entry appears only when `GET /api/classrooms/mine.teaching` is non-empty, plus a "Set up your class"/"My Class" entry point on `Profile.jsx`). **Still open from spec Phase 1:** join-attempt throttle (explicitly deferred in code), DPDP one-pager, weekly teacher digest email. **`/privacy/companion`** (2026-07-27, `PrivacyCompanion.jsx`) — Companion extension privacy policy, mounted at top level next to `/join/:code` (no session check, no `AppLayout` shell), required for the Firefox AMO listing/install-consent screen (`docs/IMPLEMENTATION-amo-submission.md` §1).
- **Newer than CLAUDE.md**: `Tests.jsx`, `AudiencePicker.jsx` (one-time career/school picker), **`SchoolRoadmaps.jsx`** (Class → Subject → Chapter browser, fully replaces the career catalog when `audience==='school'`; parses `RoadmapNode.phase` "Class 9 · Motion" — no backend change; + roadmap search; commit `b94d233`, D-006), `PhysicsNumericals.jsx` + `physics/` module, `ReviewHeatmap.jsx`, `SqlFlow/SqlJoinViz/SqlResult.jsx`, `GlossaryTerm.jsx`, `WelcomeModal.jsx`, `ToastContext.jsx`, `lib/analytics.js` (**PostHog** wrapper — autocapture OFF, curated events, silent no-op without key), `lib/useSeo.js` + prebuild sitemap generation (SEO pass), `lib/testGrading.js`.
- **Sentry + Push + PWA (2026-07-13)**: `lib/errors.js` (Sentry wrapper mirroring `analytics.js`'s no-op-without-key pattern) + `ErrorBoundary.jsx` (custom class component wrapping `<App/>`, works with Sentry off) + `vite.config.js`'s `sentryVitePlugin` (sourcemaps generated+uploaded+deleted only when `SENTRY_AUTH_TOKEN` is set — never shipped publicly). `lib/push.js` (subscribe lifecycle, stashes the API origin into Cache Storage so the cross-origin `public/sw.js` — a static file, no `import.meta.env` — can reach the Render API) + `public/manifest.webmanifest` + `public/icon-*.png`. Profile.jsx notification toggle + Review.jsx done-screen one-time prompt card.
- **DSA viz module (`src/dsa/`)** — canonical artifact is the event trace: pure generator (`generators/*.js`, golden-tested `*.golden.mjs`) → `compile.js` folds events into frames → renderers. **Far ahead of CLAUDE.md's claim**: ~29 generators live (through backtracking: `n-queens`, `permutations`, `subsets`, `combination-sum`, plus stacks/queues, sliding windows, `kadane`, `fast-slow-pointers`, `next-greater-element`, bounds…) and **9 renderers** (`ArrayViz`, `StateMachine`, `StackQueueViz`, `ListViz`, `TreeViz`, `GraphViz`, `GridViz`, `IntervalViz`, `BitsViz`) — the renderer gaps CLAUDE.md lists as "still needed" are built. Of the 9: 7 are view-dispatched, 2 (`StateMachine`, `StackQueueViz`) are always-on side panels; **4 still have no generator feeding them** (`ListViz`, `GraphViz`, `IntervalViz`, `BitsViz` — no `LIST/GRAPH/INTERVAL/BITS_INIT` emitter yet). Backtracking generators DO feed `TreeViz`/`GridViz` (n-queens → grid; template/permutations/subsets/combination-sum → tree).
- Dark mode = centralized `html.dark` override layer in `index.css`; Login page intentionally always-dark.

### Content system (`content/`)

- One JSON per lesson at `content/roadmaps/<key>/<slug>.json`; **9 content folders**: `python-swe`, `sql`, `aptitude`, `core-cs`, `dsa`, `ai-engineering`, `python-backend`, `cpp-swe`, `physics-9-10`.
- `content/validate.py` is **the gate**. Allowed `kind`s (its `KIND` set): `concept` (base shape, no explicit branch) + explicit branches for `aptitude` / `theory` / `engineering` / `dsa` / `reasoning` / `physics` / `numericals` / `test` (+ `milestone` allowed but unused in content); `content/scripts/` (glossary injection, prose extraction) is real pipeline code. `sync-content.mjs` copies to `frontend/public/content/` on `predev`/`prebuild` (git-ignored, Vercel regenerates).
- **`content/career-templates/`** (2026-07-19, Career Coach Phase 2; **v2 quality pass 2026-07-20**, `docs/SPEC-career-templates-v2.md`; **Data Analyst added 2026-07-30**, `docs/SPEC-career-templates-data-family.md`): versioned role templates `<role_key>.v<N>.json`, immutable once shipped — content changes mean a new version file, never an edit. Current latest: `backend.v2.json` (66 nodes — +heaps/greedy/intervals, +system-design probe), `ai_engineer.v2.json` (70 nodes — new `llm_apis` subject (chat-completions/streaming/rate-limits/cost-budgeting/Hugging Face), +reranking-hybrid-retrieval and fine-tune-vs-RAG-vs-prompt in genai; kmeans/PCA/eigenvalues/feature-stores/RNN removed as payment), `sde_generalist.v2.json` (62 nodes — +two-pointers/sliding-window/binary-search/heaps/greedy), `data_analyst.v1.json` (64 nodes — 8 subjects: sql_core (12), sql_advanced (10), statistics (9), bi_dashboards (8, Power BI deep), spreadsheets (7), python_analysis (8, notebook-level pandas/numpy/matplotlib), data_modeling (6), analyst_craft (4); 9 diagnostic probes). The three original `*.v1.json` files stay frozen (committed v1 trees pin them as metadata). `content/validate_career_templates.py` (stable-key uniqueness, dependency-cycle detection, depends_on resolution, priority/effort bounds, probe-pair check, **probe-spread warning** — a `default_priority ≥4` subject with zero probes gets zero diagnostic coverage) is a **second gate**, run alongside `validate.py` by `.github/workflows/validate-content.yml` (workflow created in phase 2 — both spec docs assumed it already existed; it didn't). Runtime copy lives at `backend/app/data/career_templates/` (see `sync_career_templates.py` above) because Render's backend deploy root excludes the sibling `content/` directory.
- Test banks live at `content/roadmaps/<key>/_test/*.json` (static content; only attempt outcomes hit the DB).
- Bulk generation is delegated to Antigravity; Claude owns contracts (`schema.json`, `validate.py`, `PROMPT-*.md`), runtime/renderers, and critique. DSA content handoffs exist through **phase 13** (recursion/viz/prose) — CLAUDE.md's "phases 1–7" is stale.

---

## 2. Data model (26 SQLModel tables; 27 physical incl. `alembic_version` — all prod-verified 2026-07-28)

`tracks`, `activities` (the FSRS card: `stability`/`difficulty_fsrs` NULL until first graded review; legacy SM-2 columns still written; optional `roadmap_id`/`node_id`/`problem_id` links; **+ `solution_code`/`approach_node_id`/`approach_confidence`/`approach_summary`** — the user's pasted solution and the approach inferred from it, question-generation grounding only, never mastery routing), `reviews` (due/completed + `rating`/`recalled`/`quality` + `ai_*` grader columns + **`duration_ms`** nullable int, card-shown-to-outcome-submitted via client `performance.now()`, server-clamped to (0, 1_800_000]), `feedbacks`, `roadmaps` (**+ `slug`, + `audience` 'career'|'school', + `user_id` NULL=catalog / set=personal syllabus-upload or career-tree roadmap**), `roadmap_nodes` (self-ref `parent_id` subtopics), `roadmap_node_prerequisites` (directed edges, powers "Why am I stuck?"), `user_progress`, **`user_prefs`** (audience, server-side; + `custom_roadmaps_created` lifetime syllabus-commit counter — never decremented), **`test_attempts`** (JSONB per-question results; `node_title` is the join key), **`reminder_log`** (unique `(user_id, sent_on)` = at-most-once-daily email+push idempotency, one claim gates both channels), **`question_sets`** (persisted LLM question set per card: JSONB `items` [{question, reference_answer}], `depth` 'main'|'deep', `times_used`; reused ≥2 sessions then regenerated; reference answers never leave the server), **`push_subscriptions`** (one row per browser/device Web Push subscription; `endpoint` unique = the upsert key; RLS), **`metric_events`** (generic JSONB event store for heterogeneous/exploratory learning signals — `event_type`+`payload`+optional `entity_id`; RLS; producers: syllabus commit's `extraction_edit_delta`, evidence spine's `evidence_unmapped`), **`classrooms`** (2026-07-16 — `teacher_user_id`, `name`, `school_name`, unique `join_code`, `archived_at`; RLS), **`classroom_members`** (unique `(classroom_id, student_user_id)`; `display_name` is student-typed at join, never the Google identity — see §4; RLS), **`classroom_roadmaps`** (which catalog roadmaps a classroom tracks — the scope boundary for every teacher-facing analytics query; RLS), **`learning_events`** (2026-07-19 — Career Coach Phase 1, `docs/SPEC-career-coach-phase1.md` §2.1: immutable append-only evidence log, one row per producer signal; partial unique `(user_id, source, entity_id) WHERE entity_id IS NOT NULL` = idempotency for polling producers; `deleted_at` soft-delete only; CHECK-constrained `event_type`/`trust_tier`/`grade`/`duration_min`; RLS; `node_id` FK carries **no `ondelete=CASCADE`**, confirmed at phase 2 migration time — a raw node delete is rejected by Postgres unless the app nulls it first, see phase 2's soft-detach), **`node_mastery`** (2026-07-19 — derived cache, one row per `(user_id, node_id)`, fully rebuildable from `learning_events` alone via `services/evidence.recompute_node`; unique `(user_id, node_id)`; RLS), **`career_goals`** (2026-07-19 — Career Coach Phase 2, `docs/SPEC-career-coach-phase2.md` §2.1: one active goal per user via partial unique `(user_id) WHERE status='active'`; `roadmap_id`/`template_version` NULL until tree commit; `sprint_node_id`/`sprint_until` consumed by phase 3's scheduler (see below); **+ `daily_minutes` INT NOT NULL DEFAULT 60, CHECK BETWEEN 30 AND 240 (2026-07-26, migration `b7b1755c4d22` — Phase 3's per-goal daily study budget)**; RLS), **`node_meta`** (2026-07-19 — sidecar to `roadmap_nodes` so the shared catalog table stays unwidened: `stable_key` (template-authored, survives node UUID regeneration), `priority`, `est_effort_min`, `subject`, `user_edited`, **+ `embedding` JSONB** (migration `fd9b3eaf3c37`, filled by `embed_batch` at tree commit); unique `node_id`. **Rows exist only for career-tree (template) nodes again as of D-049 (2026-07-28)** — attached-roadmap rows were the A1 defect (unscoped-uniqueness collision across users) and are no longer written; migration `a3f8c1d92b47` deletes the 148 pre-fix `attached.%` rows that still exist in prod (**written, not yet applied**). RLS), **`career_goal_roadmaps`** (2026-07-26, D-042, migration `c3e8a1f47b92` — which catalog/personal roadmaps an active goal draws from: `goal_id`, `roadmap_id`, `subject`, `default_priority`; unique `(goal_id, roadmap_id)`; cap 5/goal; RLS. **As of D-049 this row IS the attachment's metadata** — `/today` synthesizes plan nodes for attached roadmaps from it instead of a `node_meta` sidecar), **`problems`** (2026-07-21 — LeetCode problems catalog; `slug`, `difficulty`, `paid_only`), **`problem_concepts`** (2026-07-21 — maps Problems to RoadmapNodes; `role`, `confidence`), **`concept_cards`** (2026-07-21 — flashcards generated for problem-node pairs), **`problem_attempts`** (2026-07-27 — manual "I solved this" marks from the roadmap UI, migration `e2c4a8b1f960`; **applied to prod, verified 2026-07-28** — correcting the earlier "written but not applied" note — currently 0 rows; unique `(user_id, problem_id)`; RLS; deliberately never joined into `learning_events`/`node_mastery` — see D-046), `alembic_version`.

**Migration chain (40):** `c71d8f31ee19` initial → … → `f1b3d5e7a9c0` metric_events (RLS) → `431279836d74` classrooms/classroom_members/classroom_roadmaps (RLS) → `a5e9c3d7f102` learning_events/node_mastery (RLS) → `b6f2d8a1c934` career_goals/node_meta (RLS) → `37714df921de` leetcode_retention_tables (RLS) → `37714df921df` add_paid_only → `a9d4f7c2e618` TRUNCATE guard on 10 user-data tables → `b7b1755c4d22` add career_goals.daily_minutes → `fd9b3eaf3c37` add_embedding_to_node_meta → `d47fe16300ee` add_companion_browser_source → `c3e8a1f47b92` career_goal_roadmaps (RLS) → `e2c4a8b1f960` problem_attempts (RLS) → `d5f1a7c3e284` guard_problem_attempts_truncate **(prod head)** → **`a3f8c1d92b47` drop_attached_roadmap_node_meta, data-only, D-049 (local head — not yet applied)**.

**Prod state (checked 2026-07-28 via Supabase MCP):** `alembic_version = d5f1a7c3e284` — **prod is at head** (matches the local chain head; 39 migrations). 27 public tables, **all 27 with `relrowsecurity = true`**. `problem_attempts` and `career_goal_roadmaps` both **exist in prod** — correcting the standing "migration written but not applied" note carried since 2026-07-27; `problem_attempts` is applied but still empty (0 rows). Live catalog: **33 roadmaps (31 catalog + 2 personal/career-tree)**. `node_meta`: 211 rows carry an embedding; **148 rows are `attached.%` sidecars** from the one existing attachment — these are the orphan rows the A1/Q1(b) fix must clean up (`docs/BUGFIX-career-coach-docs-review.md`). Attachments: **1 total, 1 distinct user, 0 roadmaps attached by more than one user** — which is why A1 is a real code defect but has *not* fired and cannot until a second user attaches the same catalog roadmap. `learning_events` from `companion_browser`: 0 (the extension is self-hosted/unlisted and not yet syncing).

**Historical note (superseded):** the 2026-07-21 reading of this paragraph recorded `37714df921df`, 30 roadmaps, and the Jaccard-era mapping backfill result. Those numbers were correct then and are kept only in the changelog; do not cite them as current. All 24 RLS-eligible public tables (excludes `alembic_version`; 25 total) have `relrowsecurity = true`, confirmed for the 3 new LeetCode tables. Live catalog: **30 roadmaps (29 career + 1 school)** plus one real career-tree roadmap committed during phase 2 verification (owner's account, "Backend Engineer", 63 nodes — kept, not test junk). Evidence spine backfilled against the owner's real review history (31 completed reviews): 1 node-linked → 1 `learning_event` written (a miss, so `m_learned=0` — the honest answer); 30 unmapped (97% — most existing `activities` rows predate node-linking) tracked via `evidence_unmapped` metric events. Phase 2's `backfill_career_mapping.py` re-ran topic mapping over those 30 (well, the 6 still without a mapping suggestion attempt recorded — see §3): **0 auto-mapped, 6 below the 0.55 triage floor** — the owner's real activity topics (e.g. "Mutable and Immutable Objects") don't lexically overlap at all with the Backend Engineer tree's node titles, a genuine signal that the Jaccard-overlap placeholder (not a real embedding call) is too weak once there's real semantic-but-not-lexical drift; noted as the concrete next-upgrade trigger in `topic_mapping.py`'s docstring.

Hardening migration `73c79267ec74` adds CHECK constraints (`reviews.status/rating`, `user_progress.status`, `feedbacks.status`, `ease_factor ≥ 1.3`) and the hot-path indexes (`reviews(user_id,status,scheduled_for)`, `activities(user_id,created_at DESC)`, unique `user_progress(user_id,node_id)`).

---

## 3. Where the code is ahead of the docs (verified deltas)

| Claimed (CLAUDE.md / ARCHITECTURE.md) | Actual (verified 2026-07-10) |
|---|---|
| RLS + review-invariant migrations "pending" in prod | Applied; prod at head `f2b7d3a9c8e4` |
| 6 routers | 9 — `internal`, `prefs`, `tests` added |
| No email/reminder system | Full loop live: `reminder_log` + `mailer/reminders.py` + GH Actions cron + `CRON_SECRET` |
| 10 seeded roadmaps | 33 seed scripts, 30 live in prod |
| School platform = "vision / pitch-gated MVP" | Shipped in-product: `audience` split, `user_prefs`, `AudiencePicker`, Physics 9-10 roadmap + numericals route + content folder |
| Tests section absent | Live: `/api/tests/*`, `test_attempts`, `Tests.jsx`, `_test/` banks, FSRS bridge |
| DSA viz: renderers for stack/list/tree/graph/grid "still needed"; phases 1–7 | 9 renderers exist BUT 6 are unused (List/Tree/Graph/Grid/Interval/Bits — no generator feeds them); **24 registered generators** (array/string/stack/search/sort only) — **NO backtracking or linked-list generators yet** (audit 2026-07-10); prose handoffs through phase 13 |
| Dashboard has one endpoint | `/review-metrics` + `/heatmap` added (`ReviewHeatmap.jsx`) |
| No analytics | PostHog wrapper (`lib/analytics.js`), curated events, key-gated |
| No backend test suite / CI | `tests/test_ownership.py` (tenant isolation) + `reminders.yml` workflow; `Dockerfile` + `.dockerignore` |
| hardening-plan.md Tier-0 open | Done: ES256-only, `echo=DEBUG`, env CORS, required `ADMIN_EMAIL`, boot-guarded bypass, 15 `max_length` caps across request schemas |

---

## 4. Security

### Current posture (what's verifiably in place)

- **ES256-only JWT verification** via JWKS; algorithm-confusion closed; audience + role checked; expired/invalid → 401.
- **Tenant isolation**: every query scoped by `current_user.id`; mutating endpoints verify ownership (`WHERE id=:id AND user_id=:uid`); enforced by an automated test suite (`backend/tests/test_ownership.py`).
- **RLS enabled on all 12 public tables with zero policies** — deliberate: it blanket-denies Supabase's PostgREST/anon-key surface while the backend (table owner) bypasses it. Supabase's advisor flags this pattern as INFO ("RLS enabled, no policy"); that is the intended design, not a gap.
- **DB integrity as defense**: CHECK constraints on status/rating enums, partial unique indexes enforcing review invariants (`e7f2a4c9b1d5`), FK `ondelete` rules.
- **Secrets & config**: `.env` git-ignored; CORS is an explicit env-driven allow-list (no `*`); `ADMIN_EMAIL` has no hardcoded default; SQL echo off in prod; `DEV_AUTH_BYPASS` crashes the boot without `DEBUG`.
- **New env vars (2026-07-13, all follow the empty-default no-op convention)**: `SENTRY_DSN`/`SENTRY_ENVIRONMENT`/`SENTRY_TRACES_SAMPLE_RATE` (Render), `VITE_SENTRY_DSN`/`VITE_SENTRY_RELEASE`/`SENTRY_AUTH_TOKEN`/`SENTRY_ORG`/`SENTRY_PROJECT` (Vercel — `SENTRY_AUTH_TOKEN` is a **build-time** secret, never a client-exposed `VITE_*` var); `VAPID_PRIVATE_KEY`/`VAPID_PUBLIC_KEY`/`VAPID_SUBJECT` (Render only — single source of truth, public key served via API to avoid a Vercel/Render key-sync hazard). Sentry stays pseudonymous both sides (`send_default_pii=False`, user context = Supabase id only, no email) — same convention as PostHog's `identifyUser`.
- **Machine endpoints**: `/api/internal/send-reminders` uses constant-time secret comparison and is fully closed when `CRON_SECRET` is unset; reminder sends are idempotent per user/day via an atomic `INSERT … ON CONFLICT DO NOTHING` claim.
- **Input bounding**: 15 `max_length` caps on request schemas (topic 300, key_memory 500, notes 5000, mistake 2000, …) — bounds both abuse and LLM token spend.
- **LLM gating**: all 5 LLM endpoints 404 unless `GRADER_ENABLED`; LLM output is advisory-only (never auto-applied, never auto-grades the user down).
- **Teacher/classroom authorization (2026-07-16)**: a second, distinct authorization path alongside tenant isolation. `get_owned_classroom` is the IDOR wall for every teacher-scoped route (`WHERE id=:id AND teacher_user_id=:uid`, **404 not 403** — same don't-confirm-existence convention as the rest of the API); membership in a class does not imply ownership of it (tested explicitly in `test_classrooms.py`). Separately, every analytics query's inner scope is `student_user_id IN classroom_members` **AND** content restricted to that classroom's `classroom_roadmaps` — a teacher can never see a student's non-classroom activity (career roadmaps, personal syllabus roadmaps, unassigned free-form cards). This scope rule is enforced **server-side by field selection and the join itself**, never by frontend omission, and is covered by a dedicated privacy-boundary test (seed one assigned-roadmap activity + one unassigned one, assert only the former is visible to the teacher). No `user_prefs.role` column exists — "is a teacher" is defined purely as "owns ≥1 classroom" (see DECISIONS.md).

### Open vulnerabilities / risks — CURRENT

1. **No rate limiting anywhere** (verified: no slowapi/limiter in the codebase). Highest-value targets: the 5 LLM endpoints (`/grade`, `/questions`, `/grade-questions`, `/suggest-key-points`, `/grade-fillup`) — any authenticated user can hammer them into a **Groq cost-DoS**; also `POST /api/feedback/` (spam) and `POST /api/activities/` (row flooding). Input caps bound per-request cost, not request count. *This is the #1 item to fix before opening signups wider.*
2. **Admin gate is an email string compare**, not a role system. Fine for a 3-user cohort; brittle the day a second admin or email change happens. Backend check is authoritative (frontend check is UX-only) — keep it that way.
3. **Prompt-injection surface in the graders**: user-controlled text (`key_memory`, free-recall answers, drafts) is interpolated into Groq prompts, and the returned `feedback`/`revision_note`/`related_subtopics` strings are rendered in the UI. Blast radius is low (advisory-only, JSON-parsed, no tool use), but a crafted card could make the grader emit misleading/hostile text to the user. Mitigation when it matters: treat LLM strings as untrusted display data (they already render as text, not HTML — keep it that way), and consider a moderation/format check.
4. **JWT lives in localStorage** (Supabase JS default) → any XSS = token theft. React's escaping + no `dangerouslySetInnerHTML` on user content is the real defense; re-audit if lesson content ever renders raw HTML.
5. **User content flows to a third party (Groq)** whenever the grader is on — `key_memory` and answers leave your infra. Not a bug, but a disclosure/ToS item before a paid cohort, and a DPDP consideration for the school pilot.
6. **Naive-UTC timestamps** rendered as local time — cosmetic date-label off-by-one near midnight for IST users (known debt; scheduling math unaffected).
7. **Supabase advisor WARN**: leaked-password (HIBP) protection disabled. Moot while auth is Google-only; becomes real if email+password is ever enabled.
8. **Guest surface** (`optionalAuth` reads) — currently read-only roadmap browsing; keep writes behind `requireAuth` as new endpoints are added.

### Watch-list — FUTURE (not bugs today, will bite at the next stage)

- **Opening signups / any marketing push** → rate limiting (slowapi or a Render edge/proxy) becomes blocking, and the Groq/Gemini/Anthropic spend needs a per-user daily budget + alerting (the syllabus extractor's 5/user/day cap is in-memory and resets on redeploy — not a hard budget guard).
- **School pilot with minors** → India DPDP Act: parental consent, data-minimization, and a data-processing story for Groq/PostHog/Resend before any real Class 9-10 cohort.
- **GitHub Actions cron** pauses after 60 days of repo inactivity and can lag ~15 min — reminder emails silently stop if the repo goes quiet; move to a host cron if cadence ever matters.
- **Transaction pooler limits**: `pool_size=5 + max_overflow=10` per instance is fine now; multiple Render instances or heavier traffic can exhaust Supabase pooler slots — revisit before scaling out.
- **Email deliverability/abuse**: Resend sender reputation once volume grows; unsubscribe/compliance (CAN-SPAM-style) before non-trivial sending.
- **Content supply chain**: lessons are Antigravity-generated JSON rendered by the app — the validator checks structure, not pedagogy or malice; keep the human/Claude critique step for anything that ships, and never render lesson fields as raw HTML.
- **PostHog**: once a key is set in prod, it's a third-party script + user-event stream — add it to the privacy story.
- **Single founder key risk**: Supabase/Render/Vercel/Groq/Google/Anthropic accounts + `CRON_SECRET`/`GROQ_API_KEY`/`GEMINI_API_KEY`/`ANTHROPIC_API_KEY` all hang off one identity; password-manager + 2FA hygiene is the actual security perimeter of this product.

---

## 5. Future plans (cross-checked: still genuinely open)

**Near-term product (in priority order, from CLAUDE.md §next — still valid):**
1. **Review-loop friction pass** (`docs/PROMPT-review-ux-friction.md`): lesson-grounded recall questions (use `recall_questions` instead of generic LLM questions — early verdict was "too generic"), visible grading state, outcome-chip descriptors, mobile nav to 5 items, Home Continue-card.
2. **Node-complete → pre-filled Log modal** — closes the roadmap→retention loop (design doc §9b).
3. **Logged-reviews vault** (review history).
4. Persist Track/Activity-Type properly in `ActivityCreate` (currently partially UI-only).
5. Feedback status workflow (new→reviewed→resolved) + real admin auth after the first cohort.
6. **Rate limiting** (see §4 — promote this above the fold).

**Content build order** (per JD research runs 1–3): finish **AI Engineering**, **DSA phases 8+** (stack/queue → linked lists → trees → graphs → DP; viz renderers already exist, content + generators are the remaining work), then **Data Engineering**, then ML Fundamentals. Verbal/vocab aptitude pending. Core-CS OS batch in progress.

**School platform** (Classes 6–12 on the same engine): the product split is **already live** (audience prefs + Physics 9-10 + numericals). Remaining: finish Physics 9-10 content to pitch-quality, then the Gorakhpur/private-school pitch (`docs/PROMPT-school-b2b-research.md`, `school-b2b-research-findings.md`).

**Career Paths v2**: live "% to role X" tracker on top of the static `lib/careerPaths.js` v1.

**Phase 2 engine work**: real Momentum/Retention-Strength metrics (Analytics currently shows honest placeholders), Re-entry Mode, FSRS parameter optimization once there's enough review data.

**SEO**: static lesson pages + sitemap work has started (`useSeo.js`, prebuild sitemap, `docs/PROMPT-seo-static-lessons.md`) — continue toward indexable lesson content as the organic-acquisition channel.

---

## 6. Operational notes

- **Deploy**: Vercel (frontend, SPA rewrite in `vercel.json` is load-bearing for deep links/OAuth) + **Render** (backend; `Dockerfile` present). Moved off Railway 2026-07-11 (free tier ended) — **region unverified; keep it close to Supabase Mumbai (Singapore) for latency**. Deploys from `main`. Never auto-deploy — the founder pushes. Prod env vars now include `GEMINI_API_KEY` / `SYLLABUS_MODEL` / `CAREER_TREE_MODEL` (set on Render). **Gemini is the default LLM provider for both syllabus extraction and career-tree generation (D-037) — an explicit `SYLLABUS_MODEL`/`CAREER_TREE_MODEL` env var on Render OVERRIDES the code default, so both must read `gemini-*` there for the switch to take effect in prod.** `ANTHROPIC_API_KEY` is no longer required.
- **DB**: always the transaction pooler URL (`aws-1-ap-south-1…:6543`); direct host is IPv6-only. Engine already sets `statement_cache_size=0` + `pool_pre_ping`. Alphanumeric DB password only; repeated auth failures trip Supabase's circuit breaker (~minutes).
- **Schema changes**: Alembic only, and **every new table's migration must include `ENABLE ROW LEVEL SECURITY`** (the PostgREST-blocking pattern, migration `f8a3b5c2d9e1`).
- **Repo hygiene** (post-cleanup 2026-07-10): the repo root and `content/` hold **no** Python except `content/validate.py` and `content/scripts/`; `.gitignore` now enforces this (`/*.py`, `content/*.py`, `content/roadmaps/**/*.py`). One-off patch scripts should be written under the session scratchpad or deleted after use — their output (the JSON) is the artifact, not the script.
- **Verification tools**: read-only Supabase MCP is configured (`.mcp.json`) — use it to verify prod schema/migration state instead of trusting docs (this document was built that way).
- **Sentry/Push activation (founder action, not code — 2026-07-13)**: all Sentry/push code ships gated-off (no-op without keys), same as PostHog. To activate: (1) create a Sentry org + 2 projects (backend python/fastapi, frontend react), set `SENTRY_DSN`/`SENTRY_ENVIRONMENT=production` on Render + `VITE_SENTRY_DSN`/`VITE_SENTRY_RELEASE=$VERCEL_GIT_COMMIT_SHA`/`SENTRY_AUTH_TOKEN`/`SENTRY_ORG`/`SENTRY_PROJECT` on Vercel; (2) generate a VAPID keypair once locally (`vapid --gen`, ships with `pywebpush`), both keys → Render only; (3) redeploy both; (4) smoke-test: force an error each side → confirm in Sentry; subscribe on retainhq.app + `workflow_dispatch` the reminders Action → confirm a push notification arrives; check Supabase advisors show no new RLS warnings post-migration.

---

## Appendix — doc freshness

| Doc | Status (2026-07-10) |
|---|---|
| `docs/SYSTEM-OVERVIEW.md` (this file) | **Current — verified against code + prod DB** |
| `CLAUDE.md` | **Rewritten 2026-07-10** as a lean rules + update-routing file (no status). The old ~450-line version is frozen at `docs/CLAUDE-ARCHIVE-2026-07.md` — its status/roadmap-count/DSA-viz sections were already stale (see §3) |
| `docs/claude-code-workflow.md`, `docs/DECISIONS.md`, `docs/BACKLOG.md` | Added 2026-07-10 — session playbook, go-forward decision log, idea inbox |
| `docs/ARCHITECTURE.md` | Topology/auth still broadly right; predates reminders, prefs, tests, audience split, 20+ roadmaps (2026-06-07) |
| `docs/API.md`, `docs/FLOWS.md` | Predate the 3 new routers + dashboard endpoints — **stale** |
| `docs/hardening-plan.md` | Tier-0/1 items largely **done in code**; still useful as the rationale + for unfinished items (rate limiting) |
| `docs/SPEC-test-runtime.md`, `dsa-*`, `content-engine.md`, `design-bible.md` | Recent, written alongside the shipped features — trustworthy |
| `docs/SESSION_HANDOFF.md`, `HANDOFF-*`, `PROMPT-*` | Point-in-time working docs — read with their dates in mind |

---

## Changelog

- 2026-08-03 - **`/evidence` rewritten for humans (D-073).** The page D-063 un-gated for every user was still a dev instrument — `m_learned`/`r`/`m`/`w` column headers, a summary JSON dump, a `POST /recompute` button, and 120+ untouched nodes padding the list. Now a per-topic evidence trail: plain-language state/confidence labels driven by the same `services/evidence.py` thresholds, each event rendered as a sentence, raw numbers + recompute + per-event delete behind one "Show raw numbers" toggle (translated, not removed). Says what it does NOT know, which the old page didn't: a `REMOVED — NOT COUNTED` badge (soft-deleted rows were marked by `line-through` alone — invisible at a glance and stripped on copy, so a purged 115-row import read as live data scoring 0.000), a `DIDN'T CHANGE THE NUMBER` badge for legitimate zero-weight events, and a "Not yet matched" stat for `evidence_unmapped` captures that move nothing and appear nowhere else. Frontend-only; no API change. Verified in-browser at desktop + mobile widths in both themes.

- 2026-08-02 - **NeetCode completions can now be imported — at T3_observed, not T1 (D-072).** New `POST /api/evidence/neetcode/backfill` + a second popup button. Endpoint found by recording API traffic in a signed-in tab: `POST /api/callableFunctionHttp {"data":{"functionId":"getCompletedProblems"}}` returns **LeetCode URLs** (NeetCode links out to LeetCode to solve), so the import resolves against the existing LeetCode catalog and never touches the alias table. **Tiered T3, unlike every other backfill here**: that endpoint cannot prove a problem was judged — a "completed" entry may be a real run, or a checkbox ticked after solving elsewhere, and nothing distinguishes them. `is_capped()` therefore bounds the whole import at `T3_EXPOSURE_CAP` (0.35) and it can never drag down a node real T1 evidence already raised. NeetCode exposes no per-problem dates, so `NeetCodeBackfillIn` takes a single `occurred_at` (the account's earliest activity date) rather than fabricating a per-slug map — backdating understates recency on purpose. `leetcode_tab_fetch.ts` generalised to `tab_fetch.ts` (site as an argument, POST support) and shared by both imports. Extension v1.2.5. 459 backend tests (up from 450), 133 extension tests (up from 116), `addons-linter` clean.

- 2026-08-02 - **NeetCode aliases onto the LeetCode catalog instead of duplicating it (D-071, reverses D-064).** New table `problem_aliases` (13 tables now; migration `c2d4f6a8b013`, RLS enabled per convention) maps `(source, alias_slug) -> problems.id`; new `evidence.resolve_problem()` tries a native row for the source first, then the alias. NeetCode's problems ARE LeetCode's re-slugged — measured against the live catalog: 588/588 NeetCode150 problems resolve to an existing LeetCode row (514 by identical slug, 55 by title via `getProblemMetadataFunctionHttp`, 19 by hand-listed rename override), zero collisions, and **533 (90.6%) land on a node already mapped by the curated LeetCode pass**. The other 55 are gaps in that pass itself, which NeetCode now inherits the fix for. `import_neetcode_catalog.py` is **deleted** — never run against prod, and two bugs found in it while measuring (`name` absent on all 588 NeetCode150 entries, so it would have titled them with their raw slug; `topics` discarded entirely). New `backend/scripts/sync_neetcode_aliases.py` populates the table, dry-run by default. **Still open:** no known way to read NeetCode solve *history* — its equivalent of `/api/submissions/` is behind an auth boundary this codebase can't cross, so NeetCode is live-capture-forward only, no backfill. 450 backend tests (up from 444), new `tests/test_problem_aliases.py`.

- 2026-08-02 - **The LeetCode import never worked: `/api/submissions/` 403s any extension-context request. Now runs inside a leetcode.com tab (D-070).** Measured, not inferred: the identical URL with the identical cookies returns 200 from the page console and 403 from the service worker. That endpoint dumps full submission SOURCE CODE, so LeetCode guards it far harder than the `/api/problems/all/` the pre-D-060 import used, and no extension can satisfy it — `Referer` is a forbidden header name and an extension-initiated request is cross-site for SameSite purposes. New `extension/src/background/leetcode_tab_fetch.ts` borrows a real leetcode.com tab (reusing an open one, else opening a background one it closes again) and runs each page fetch there via `scripting.executeScript({world:'MAIN'})`; the injected function is three lines that fetch a URL and return raw text, with every product decision — cutoff, language filter, stop conditions — still in the pure, unit-tested `leetcode_backfill.ts`. `scanRecentSolves`'s `fetchImpl` lost its `= fetch` default so the broken context can't return by omission. New `scripting` permission (manifest test guards it); new `tabs*`/`scripting` wrappers in `browser_api.ts`, which matter more than the existing direct `chrome.tabs` calls because these are awaited **for their return value** — Firefox's callback alias would resolve them to `undefined`. Extension v1.2.4. 116 extension tests (up from 102), `addons-linter` clean.

- 2026-08-02 - **`POST /api/companion/sessions` 500'd on every request; fixed (D-065). Plus two D-064 audit fixes (D-066, D-067).** The sync endpoint built its rate-limit window as a timezone-AWARE datetime and compared it against `metric_events.created_at`, which is `timestamp without time zone` - asyncpg rejects that binding outright (`DataError: can't subtract offset-naive and offset-aware datetimes`), so every batch 500'd and the extension's 8-attempt retry cap then dropped each session permanently. Pre-existing, not a regression: it hid behind SQLite (which accepts the binding) in tests, and behind D-054's ungranted Firefox host permission in production - the endpoint's first-ever real request was also its first failure. Now uses a naive-UTC `utc_start_of_day()` helper; `tests/test_companion_sessions.py` adds the endpoint's first test coverage (7 tests). Audit fixes: the NeetCode importer's CRC32 `external_id` overflowed int4 for 453/938 slugs (48.3%) and its dry-run never flushed, so no INSERT was ever validated - both fixed; and the NeetCode probe disarmed on inconclusive responses, letting a non-judge endpoint swallow a real verdict. 438 backend tests (up from 431), 100 extension tests.

- 2026-08-02 - **NeetCode.io support added (D-064) as a second, separate evidence producer.** Not a LeetCode variant — its own catalog (`problems.source='neetcode'`, synced from NeetCode's public `getProblemListFunctionHttp` API by new `backend/scripts/import_neetcode_catalog.py`, confirmed live: ~938 problems, not yet run with `--commit`), its own judge (`POST /api/runCodeFunctionHttp` on neetcode.io, reverse-engineered to its auth boundary — this codebase cannot create accounts to verify past "Please sign in first."), new `POST /api/evidence/neetcode/solve`, new `extension/src/content/neetcode.ts`+`neetcode_probe.ts`. The verdict probe is explicit best-effort pending live signed-in testing. **No `problem_concepts` roadmap-node mapping was written** — per CLAUDE.md's content-pipeline convention that's Antigravity's job; every imported problem is `evidence_unmapped` until that pass happens. Found and fixed in the same pass: `/leetcode/solve` and `/leetcode/backfill` looked up `Problem` by `slug` alone, unscoped by `source` — harmless with one source, a real cross-attribution risk with two. Also: `*://neetcode.io/*` added to host_permissions and folded into the consent-flow's guaranteed onboarding gesture (Firefox doesn't grant host_permissions at install, same as D-053/054) — and that same audit found youtube.com/coursera.org/notion.so have NEVER had their required host_permissions requested on Firefox either, logged to BACKLOG as a separate, significant follow-up. 431 backend tests (up from 426), 100 extension tests (unchanged, see D-064 for why).

- 2026-08-02 - **Chrome/Edge distribution build (D-062) + `/evidence` un-gated for every user (D-063).** New `npm run build:chrome` mirrors `build:firefox`: `.env.chrome` pins the prod API URL, `build-chrome.mjs` strips the localhost host permission and `browser_specific_settings`, writes `dist-chrome/` - submittable to both the Chrome Web Store and Edge Add-ons (same Chromium MV3 package). Separately, `/evidence` - the only surface showing companion sessions/imported solves - had its route and both nav entries removed from the `isAdmin` gate; backend routes underneath were already scoped to `current_user`, so this was a frontend-only restriction with no auth boundary behind it. Layout unchanged (BACKLOG has a redesign). 100 extension tests (up from 93).

- 2026-08-02 - **LeetCode import language/window are now user-configurable (D-061), correcting D-060.** D-060 shipped the import hardcoded to Python + 60 days, built against one account's own migration off C++ - wrong for any other install (24 registered users at the time). New popup panel: 11 language checkboxes + a window select, auto-expanded on a never-configured install so a fresh user is asked rather than silently defaulted; starting state (everything checked, all time) is identical to no filter at all. `collectSolves`/`scanRecentSolves` (`extension/src/background/leetcode_backfill.ts`) take `langs: Set<string> | null` instead of a hardcoded constant; new shared `extension/src/leetcode_langs.ts` holds the language/window option lists for both the popup and the scan. 93 extension tests (up from 90).

- 2026-08-02 — **Companion could never reach the backend on Firefox; LeetCode import is now windowed + Python-only (D-053/054/059/060).** Root cause of a total data blackout — zero `companion_browser` and zero `PROBLEM_SOLVED` rows had EVER existed in prod: Firefox MV3 does not grant `host_permissions` at install, and `main.py`'s CORS allow-list is explicit origins, which a `moz-extension://<per-install-UUID>` origin can never join — so the host grant is the only thing that makes our own API reachable, and nothing ever requested it. Supabase kept working throughout (permissive CORS), which is exactly why sign-in looked healthy while nothing synced. Extension now requests `API_ORIGIN_PATTERN` from the consent-tier gesture (**both** tiers — `titles` uploads to the same API) and from the import button; new `extension/src/config.ts` is the single source for the API base. Separately, `dist-firefox/` was inheriting a developer `.env` and shipping `http://localhost:8000` — pinned via a committed `.env.firefox` + `vite build --mode firefox`. Import rewritten off `/api/problems/all/` (which carries neither language nor date) onto the paginated `/api/submissions/` log: accepted **Python** solves within a **60-day** window, earliest-in-window wins, stops paginating at the cutoff, rate-limit-aware `partial` flag surfaced in the popup. Backend `LeetCodeBackfillIn` gained `solved_at`. First-ever verified import: 136 slugs → **115** `PROBLEM_SOLVED` events across **38** nodes (21 dropped as unmapped — silent, see BACKLOG). 426 backend tests (up from 423), 90 extension tests (up from 76). Also: `/evidence` finally got a **nav link** (sidebar + mobile More menu, admin-gated) — the route had existed since Career Coach phase 1 but was reachable only by typing the URL, and it is the only surface showing companion sessions and imported solves (they write `learning_events`, not `activities`, so Home's rail and "Total captured" deliberately exclude them). New `backend/scripts/purge_leetcode_backfill.py` soft-deletes `backfilled: true` solves for one user and replays mastery, for undoing the pre-filter import.

- 2026-08-02 — **One provider seam, and the grader moves to `gemini-3.5-flash-lite` (D-058)** (`backend/app/services/llm.py`, new). Audit finding behind it: model **ids** were already centralised (5 settings in `config.py`, 1 stray literal in `embeddings.py`), but **provider dispatch was written three times** — `career_tree.py` branching `gemini*`/`claude*`/OpenAI-compat, `syllabus.py` its own `gemini*` check, `grader.py` hardwired to Groq plus an `openai/gpt-oss` branch for whether `reasoning_effort` was a legal param. `llm.call_json(model=...)` now derives the provider from the id; `grader.py` and `approach_inference.py` route through it and name no provider. `GROQ_MODEL` → **`GRADER_MODEL`** (`gemini-3.5-flash-lite`); a non-`gemini*` id routes back to Groq as an env flip, both keys stay wired. `embeddings.py`'s literal → `EMBEDDING_MODEL`. New invariant, test-enforced: **no model id may appear as a literal outside `config.py`** (`tests/test_llm_routing.py`). Supersedes D-056. Accepted risk recorded in D-058: grading strictness now rides on flash-lite's own thinking rather than an explicit `reasoning_effort="medium"`, and a lenient grader fails quietly into inflated FSRS intervals — watch the self-report-vs-LLM calibration gap. 423 backend tests green.

- 2026-08-02 — **Review questions are now written against the user's own solution, not the canonical one** (`backend/app/services/approach_inference.py`, new). Diagnosis: question generation was grounded only in the catalog's `role='primary'` node title/description + the problem title, so when asked for an implementation question the model filled the gap with the textbook solution — #567 solved with two dicts and a per-slide `==` comparison got quizzed on the 26-length count array and the `matches` counter. Right concept, wrong code. Fix: an optional "Your Solution" paste field in LeetCode mode; one lite-tier Gemini call at **log** time (not review time) resolves the code onto a **closed set** — the nodes already in `problem_concepts` for that problem, plus `"other"` — and returns 2-4 concrete facts about the implementation. New `activities.solution_code` / `approach_node_id` / `approach_confidence` / `approach_summary` (migration `b1c3e5a7d902`, **not applied to prod**). **`node_id` is untouched**: it stays the catalog primary and remains the sole mastery-routing key (`SPEC-leetcode-retention.md` §3.2.-1 — inference reframes questions, it does not move mastery); a high-confidence read additionally re-grounds the syllabus-topic block so the topic contract and the code facts can't describe two different approaches. Every failure path (no key, timeout, hallucinated node id, malformed response) returns `None` and the card degrades to exactly the previous behaviour. New `APPROACH_MODEL=gemini-3.5-flash-lite` / `APPROACH_CODE_MAX_CHARS=8000`; question generation and grading deliberately stay on `GROQ_MODEL` (D-055, D-056).
- 2026-07-30 — **Companion consent-tier switch fixed: `permissions.request()` is now the first awaited call whenever a Firefox user gesture is spent (D-052)** (`extension/src/consent.ts`). Choosing "cloud" tier in the popup threw `permissions.request may only be called from a user input handler` — `switchConsentTier` awaited `hasLlmOriginPermission()` (a `permissions.contains()` call) before ever reaching `permissions.request()`, spending the click's gesture context even though the popup's own click handler correctly avoided doing so. Fixed by reordering so `request()` runs unconditionally first when the tier needs origins (it's documented to no-op safely if already granted); the `contains()` check moved to the branch that never calls `request()`. Same root-cause class as D-051 (sign-in), same session — Firefox's gesture requirement bit twice at two different call sites. Regression test asserts call order via mock invocation order (jsdom can't observe real gesture state); verified as a real guard by confirming it fails against the pre-fix code, not just passes against the fix. 76 extension tests (up from 75). Shipped as v1.2.2 (AMO won't accept a re-upload of an already-signed version number).
- 2026-07-30 — **LeetCode `problem_concepts` importer now reconciles stale primaries on every run (D-050)** (`backend/scripts/import_leetcode_catalog.py`). Was upsert-only on `(problem_id, node_id)` — blind to a problem's other existing rows, so a primary that moved node, or vanished into `out_of_scope`, left its old row as a second/stale `role='primary'` forever. Fixed 427 affected rows in prod this way across three discovery passes (311 duplicate-primary, 34 stale-scaffold, 82 stale-legal — the last found only by comparing DB against the mapping file directly, not by invariant checks). `mapping.v1.json` merged to **v1.2** with `mapping.v3-tail.json`'s 797 pass-3 blinded-rerun decisions; imported and verified — prod `problem_concepts` primaries (2,828) now match the file exactly, both directions. Re-running the importer is now a confirmed no-op.
- 2026-07-30 — **Data Analyst career template added (data_analyst.v1.json)** (`docs/SPEC-career-templates-data-family.md`). 64 nodes across 8 subjects (`sql_core`, `sql_advanced`, `statistics`, `bi_dashboards` with Power BI depth, `spreadsheets`, `python_analysis` notebook-level, `data_modeling`, `analyst_craft`), 9 diagnostic probes spanning all `default_priority >= 4` subjects. Validated (0 errors, 0 warnings via `validate_career_templates.py`), synced to `backend/app/data/career_templates/`, research prompt addendum appended to `SPEC-career-templates-v2.md` §8. 35/35 `test_career.py` tests passing.
- 2026-07-28 — **A1 fixed: attached-roadmap metadata moves from a shared `node_meta` sidecar to the `career_goal_roadmaps` link (D-049)** (`docs/BUGFIX-career-coach-docs-review.md` §D). `node_meta.node_id` was globally unique with no user/goal column, so two users attaching the same catalog roadmap collided — one user's detach silently deleted sidecars a second user's `/today` plan depended on. Fix: attach/detach no longer touch `node_meta` at all; `career_goal_roadmaps` (`subject`/`default_priority`) is the sole per-attachment state, applied uniformly to every node in the roadmap. `/today`'s node query and its balance-window subject aggregation both now synthesize attached-node metadata from that link (Python-side `node_id -> subject` map) instead of an `INNER JOIN node_meta`, which attached nodes no longer have — a join that would have silently dropped every attached-roadmap minute from the balance window otherwise. New migration `a3f8c1d92b47` (data-only) deletes the 148 pre-fix `attached.%` rows already in prod — **written, not yet applied** (deploy code first, then apply, per this repo's convention). Severity was checked before fixing, not assumed: prod holds 1 attachment / 1 user / 0 roadmaps shared across users, so A1 had not yet fired — fixed ahead of any incident since the trigger is an ordinary action and the failure is silent. New regression test `test_two_users_attaching_the_same_catalog_roadmap_do_not_collide`; all 12 pre-existing attach/detach tests pass unmodified. 378/378 backend suite green. D-042 marked superseded (mechanism only — the feature is unchanged).
- 2026-07-28 — **Doc-sync pass, no code change** (`docs/BUGFIX-career-coach-docs-review.md` §C). Fixed six classes of staleness in this file, all of which had survived because the changelog got its line while the §-bodies it points at were never touched — the exact failure the CLAUDE.md routing rule exists to prevent. Corrected: §1 career-router count (15 → **19**, and the four attach routes are now listed); §1/§2 migration counts and heads, which were *mutually contradictory* (§1 said 32/`37714df921df`, §2 said 34/`b7b1755c4d22`; actual **39/`d5f1a7c3e284`**); §1 `models.py` table count (22 → **26 SQLModel / 27 physical**), which disagreed with §2's own heading; §1 `topic_mapping.py`, which still described the deleted Jaccard implementation and a `TRIAGE_THRESHOLD` of 0.55 (real: embeddings via the previously-unlisted `embeddings.py`, threshold **0.65**, and **nothing auto-maps**); §1 `CAREER_TREE_MODEL` (`gemini-3.5-flash` → **`gemini-3.6-flash`**, plus the new `COMPANION_LITE_MODEL`); §1 `tests/` row, which was two eras behind (171/171 @ 2026-07-19 → **378/378**, with the eight missing test files named). Added the missing `career_goal_roadmaps` §2 entry and corrected `node_meta`'s "rows exist only for career-tree nodes" claim, which attachments falsified. **Prod state re-verified via Supabase MCP, not assumed** (CLAUDE.md rule) — and it overturned two standing beliefs: prod is at `d5f1a7c3e284`, so **`problem_attempts` and `career_goal_roadmaps` are both applied to prod**, contradicting the "written but not applied" note carried since 2026-07-27. Also measured for the pending A1 fix: 148 orphan `attached.%` sidecars, and **1 attachment / 1 user / 0 shared roadmaps** — so A1 is a real defect that has not fired and cannot until a second user attaches the same catalog roadmap (correcting this doc's own earlier "genuine live multi-user bug" framing to "genuine, currently latent"). No code touched.
- 2026-07-27 — **YouTube chapter-based topic splitting (D-048)**, reusing D-047's fan-out machinery with no LLM call needed. New `extension/src/content/chapter_extract.ts` reads a video's own uploader-added chapter markers (public metadata, verified against a live 142-chapter freeCodeCamp tutorial — `ytd-macro-markers-list-item-renderer` → `h3.macro-markers`/`#time`) and a 15s sampler buckets watched seconds per chapter as segments emit. `Segment`/`Session`/`CompanionSessionIn` gained a `chapters` field alongside D-047's `content` — but unlike `content` (a repeated full snapshot, rides only a closed session) `chapters` is a per-emit DELTA, correctly summed across segments and safe on every sync including a checkpoint flush. Backend needed no new Gemini call: `companion.py`'s per-topic classify-and-record helper is now fed by either chat topics or chapters through one shared fan-out list, so chapters just had to produce a `(title, duration)` list D-047 already knew how to record. Same cost guard pattern as chat content: `MAX_CHAPTER_SESSIONS_PER_BATCH=3`, `MAX_CHAPTERS_PER_SESSION=15` (falls back to whole-video single-event for a video with more chapters than that watched in one sitting). Not consent-gated — chapters are public video metadata, not user-authored or AI-derived text, so none of D-047's privacy-tier logic applies. Two real defects caught by live verification before shipping (same discipline D-047 established): a guessed title selector matched nothing (found the real one by dumping every leaf node inside one chapter item); and a jsdom/nwsapi limitation with scoped `#id` lookups when an id is duplicated (YouTube's own hover-preview clone reuses `id="time"` inside every chapter item) — fixed by switching **production** code to `querySelectorAll('#time')[0]`, not just the test, since relying on id-uniqueness the live page itself violates is fragile regardless of engine. Not implemented: caption/transcript reading for the (large minority of) chapterless videos — deferred as meaningfully riskier than chapters, not guessed at. 378 backend tests (`test_companion.py` 24, up from 17), 64 extension vitest tests (up from 54).
- 2026-07-27 — **Companion "Smart tracking" reads AI-chat text and splits one session into per-topic evidence (D-047)** (`docs/IMPLEMENTATION-companion-chat-content.md`). New `extension/src/content/chat_extract.ts` (ChatGPT/Claude/Gemini DOM readers, live consent-tier gated, capped 60 turns/12k chars) and `backend/app/services/topic_segmentation.py` (one Gemini call → ≤5 model-written topic labels). `POST /api/companion/sessions` fans a content-bearing session into N `LearningEvent` rows (one per topic, duration split by share) — every row stays `TIME_BLOCK`/`T3_observed`/weight-0 regardless of fan-out (`evidence_weights.TIME_BLOCK_WEIGHT` is unconditionally 0.0), so more topics can never mean more mastery. `content` travels as a transport-only sibling of `CompanionSessionIn.payload`, never nested inside it — `payload`'s `extra="forbid"` invariant (D-043) is untouched. Daily rate limit switched from counting `LearningEvent` rows to a dedicated `companion_session_tracked` metric event, so a 5-topic fan-out still costs 1 against the 50/day cap. Consent gained copy-versioning (`CONSENT_COPY_VERSION`, `v2`) — a tier stored under stale copy re-prompts rather than silently carrying old consent forward, since a v1 "cloud" consent described materially less processing than the tier does now. Found and fixed in the same pass: `switchConsentTier`'s origin-grant/revoke decision now queries `hasLlmOriginPermission()` live instead of trusting the caller's cached tier — the old version would have left a v1 user's LLM host permission dangling after a copy-version-triggered downgrade. **Gemini billing CONFIRMED by the owner 2026-07-27** — `GEMINI_API_KEY` is on a billed Google Cloud project, which is what makes `PrivacyCompanion.jsx`'s "not used to train models" claim true now that it covers actual chat text, not just page titles. Standing condition, not a closed checkbox: if that project's billing lapses the published policy silently becomes false, with no code change to signal it. `manifest.json` `data_collection_permissions` checked against Mozilla's current category framework and needed **no change** — chat text fits the existing `optional: ["personallyIdentifyingInfo"]` declaration. Also removed: the `nano` (on-device) consent tier — no on-device classification path was ever implemented, it was a relabelled `titles` requesting permissions it never used. Extension also gained a manual pause/resume toggle, a Notion tracked surface, split inactivity constants (15-min checkpoint sync vs. 20-min kill, replacing one 15-min constant that did both jobs), and a real popup UI (design-bible tokens, live recording/sync state) replacing the placeholder Vite-template popup. **Two bugs found by a post-implementation verification pass, both fixed:** (1) `chat_extract.ts` snapshots the whole visible conversation each time (not a delta), so a long chat checkpointed repeatedly shipped cumulative snapshots and the backend segmented + recorded the same early turns once per checkpoint — content now rides only a genuinely CLOSED session, never a checkpoint flush; (2) **root cause, and a latent bug in the 15/20 split itself, independent of content** — `CHECKPOINT_MS` (15m) is shorter than `INACTIVITY_KILL_MS` (20m), so "has been running ≥15 min" alone fired on a session whose activity had already stopped, clearing the buffer and destroying the trailing segments *before* the kill window could recognize them as closed; that session then never closed at all. Checkpointing now additionally requires recent activity (`CHECKPOINT_ACTIVITY_RECENCY_MS`, 6m — one 5-min emit interval plus margin). The decision moved out of `service_worker.ts` (untestable in isolation: module-level alarm registration + supabase client) into a new pure `stitcher.planSync()`, pinned by a 70-minute simulation test asserting exactly one content-bearing sync carrying the fullest snapshot. Also hardened: the sync-queue write is now try/caught and retries after stripping content — chat content took a queued session from ~200 bytes to as much as 16KB, so a 300-session offline backlog can approach Chrome's 10MB `storage.local` quota, and an unhandled over-quota rejection would have silently stopped syncing forever. 371 backend tests (`test_companion.py` 17, up from 10), 40 extension vitest tests (up from 25; `jsdom` added as a devDependency, scoped to the one new DOM-dependent test file via a per-file pragma).
- 2026-07-27 — **AMO submission prep — unlisted self-host path (`docs/IMPLEMENTATION-amo-submission.md`).** `extension/REVIEWER-BUILD.md` added (build reproduction instructions for AMO's mandatory source-code review — bundled extension, so `dist-firefox/` alone would be rejected); verified `npm ci && npm run build:firefox` reproduces it from a clean `node_modules`. New public route **`/privacy/companion`** (`PrivacyCompanion.jsx`, see Frontend layout above) — the required privacy policy, live before submission. Manifest `data_collection_permissions` gained `"optional": ["personallyIdentifyingInfo"]` alongside the existing `required: ["browsingActivity"]` — verified against Mozilla's current category definitions (extensionworkshop.com) that the LeetCode reflection "biggest mistake" field (`src/content/leetcode.ts`, unconstrained free text, always skippable) doesn't fit `browsingActivity`'s "which sites you visit" definition and needs its own, opt-in-only declaration; `technicalAndInteraction` was considered and rejected (Mozilla only permits that key under `optional`, never `required`, and it's meant for device/crash telemetry, not user-typed notes). `manifest.test.ts` gained a regression assertion for the new `optional` array. `frontend/vercel.json` gained a `headers` rule (`/downloads/*.xpi` → `Content-Type: application/x-xpinstall`) so a future self-hosted signed `.xpi` won't hit Firefox's "downloads instead of installs" MIME bug — no `.xpi` exists yet; that file drops into `frontend/public/downloads/` once downloaded from AMO. Popup's "never used to train models" claim (`index.html`) was **re-verified, not changed** — it's accurate per D-043's confirmed Gemini paid-tier terms, and the new privacy policy repeats it on that basis. **Not done in this session (needs the user's own accounts/actions):** creating/using an AMO developer account, the actual `web-ext sign`/unlisted submission, downloading the signed `.xpi`, the dedicated reviewer account, and dropping the `.xpi` into `frontend/public/downloads/`.
- 2026-07-27 — **LeetCode problems on DSA nodes + manual capture (D-046)** (`docs/IMPLEMENTATION-problem-capture.md`). New `Problems (N)` section on each DSA-roadmap node's detail panel (`RoadmapDetail.jsx`, gated on `contentKey === 'dsa'` — the only roadmap `problem_concepts.node_id` actually maps into), backed by new `GET/POST/DELETE /api/problems/*` routes and a new `problem_attempts` table. **The governing rule (§1 of the doc): a manual click is a completion checkbox, never evidence** — it writes only `problem_attempts`, and is structurally incapable of writing `learning_events` or moving `node_mastery` (pinned by `test_marking_writes_no_evidence`, the load-bearing test). Only the browser extension's verified-submission path (`POST /api/evidence/leetcode/solve`) reaches the evidence spine, at `T1_verified_external`. `T4_claimed` was considered and rejected (still writes an event, still moves mastery, just less) — the product's own thesis ("track what you remember, not what you complete") reads cleaner with manual capture entirely outside the spine, and the difference is a truthful reason to install the extension. Frontend: sort control (difficulty default / number), primary shown open, supporting/alternative collapsed groups, `paid_only` labelled, optimistic mark/unmark reverted on error, fetch-failure tracked separately from genuine emptiness (`Home.jsx`'s post-2026-07-24 pattern). Migration `e2c4a8b1f960` **written, not applied to prod** (RLS, no policies) — verified live against real prod `problems`/`problem_concepts` data (2026-07-27) by temporarily disabling this environment's local `DEV_AUTH_BYPASS` so an unauthenticated GET wouldn't touch the not-yet-existing table; reverted after. Confirmed live: correct primary→supporting→alternative + difficulty→number ordering against the real 184-primary/254-supporting/20-alternative "Hash tables" node (§9.2's flat, unpaginated 184-row list is legitimately hard to scan — see BACKLOG), the honest fetch-error state (hit for real, before the DEV_AUTH_BYPASS workaround, as `relation "problem_attempts" does not exist`), and optimistic-revert-on-error (a real 401 round-trip against a stale browser session). Mark/unmark's actual write path is verified only by the backend test suite (SQLite, 9 new tests) — not live, since the table doesn't exist in prod yet. `ProblemConcept.role`'s docstring was stale (said only `'primary' | 'supporting'`; prod has a third value, `'alternative'`, confirmed via direct query — 422 rows) and is corrected. 364 backend tests (355 baseline + 9 new); frontend build clean.
- 2026-07-26 — **Firefox port of the Companion extension — two targets, one source tree (D-044)** (`docs/IMPLEMENTATION-companion-firefox.md`). Same `extension/src` now ships to both stores; no fork. `manifest.json` gained `background.scripts` alongside the existing `service_worker` (Firefox reads `scripts`, Chrome reads `service_worker` — confirmed both browsers ignore the key they don't recognize) and `browser_specific_settings.gecko` (`id: companion@retainhq.app`, `strict_min_version: 140.0`, `data_collection_permissions.required: ["browsingActivity"]`) plus `gecko_android.strict_min_version: 142.0`. New `extension/src/browser_api.ts`: the ~15 call sites that `await chrome.*` (storage.local get/set, permissions request/remove/contains, runtime.sendMessage) now resolve through it — Chrome's `chrome.*` is promise-native when called without a callback, but Firefox's is a callback-only compat alias, so the same awaited call silently resolved to `undefined` on Firefox instead of the real value. The shim picks Firefox's native `browser.*` global when present, else falls back to `chrome.*`; fire-and-forget calls (badges, alarms, `onMessage.addListener`) were left untouched since they don't depend on the return value. `@crxjs/vite-plugin` (confirmed, same class of bug as D-043) drops an unrecognized `background.scripts` key from its own output entirely rather than emitting a usable Firefox manifest — so `npm run build:firefox` (`extension/scripts/build-firefox.mjs`) reuses the Chrome build's bundled JS byte-for-byte and only patches the manifest's `background` key into `dist-firefox/`. `web-ext lint` on that output: 0 errors, 2 pre-existing warnings (an `innerHTML` template literal in `leetcode.ts` and a bundled-dependency dynamic `import()`, neither touched by this port). **Doc correction discovered mid-implementation, not anticipated by IMPLEMENTATION-companion-firefox.md**: Firefox now mandates `data_collection_permissions` on every new extension's manifest (policy effective 2025-11-03, `browsingActivity` declared here — titles/domains/durations, no page content), and that key requires Firefox ≥140 (Android ≥142) — one full major version above the doc's `strict_min_version: 128.0`, which was set only for the (older) `optional_host_permissions` floor. `strict_min_version` raised to `140.0` to satisfy both. Real-browser verification (build order step 4 — install unpacked, sign in, confirm a session syncs, confirm the consent screen actually grants an origin) was **not performed**: this environment's browser tooling can't drive Firefox or read `about:debugging`. The Firefox OAuth redirect URL (differs from Chrome's, derived from the extension/add-on id) is logged on first sign-in attempt rather than guessed — needs a real Firefox load to read the value before it can be added to Supabase's allowed-redirect list. 355 backend tests unaffected (no backend change in this task); 25 extension vitest tests (20 baseline + a manifest-shape regression test + a regex guard against a bare `await chrome.` reappearing outside `browser_api.ts`).
- 2026-07-26 — **Companion consent, tiers & Chrome Web Store readiness (D-043)** (`docs/SPEC-companion-phase1.md` §6, `docs/IMPLEMENTATION-companion-consent.md`). Chat-content classification stays OFF by default (metadata/titles stay ON, unchanged) until a one-time three-tier choice screen — `cloud` (Recommended, Gemini paid-tier — confirmed via Google's current terms that paid-tier prompts/responses are not used for training, unlike the free tier), `nano` (on-device, plumbing only — no Prompt API spike yet), `titles` (no permission grant at all). Consent lands as `metric_events`/`companion_consent` (`{tier, copy_version, surface}`, allowlisted in `CLIENT_EVENT_TYPES`); `services/metrics.get_latest_consent_tier()` derives the current tier from the newest such event, no new column. **`[DECISION]`, deviates from the parent doc's §2/§5**: the doc specified moving `chatgpt.com`/`claude.ai`/`gemini.google.com` to `optional_host_permissions` and registering the LLM content script dynamically via `chrome.scripting.registerContentScripts` after the grant. Implemented, then reverted the *mechanism* (kept the *goal*): `@crxjs/vite-plugin` only bundles content scripts referenced from a static manifest `content_scripts` entry — a script referenced solely from a `registerContentScripts()` call is silently dropped from the build (`llm_metadata.ts` was confirmed absent from `dist/` after building the dynamic-registration version). Shipped instead as a **static** `content_scripts` entry matching the LLM origins, with those origins under `optional_host_permissions` (not `host_permissions`) — Chrome gates actual injection on the runtime grant either way (confirmed against Chrome's own documented reload-after-grant behavior), so the enforcement guarantee ("no grant, no injection, enforced by the browser") is identical, achieved with less manifest surface (no `scripting` permission needed) and a build that actually works. Extension manifest also gained the 4 previously-missing PNG icons (rasterized from `public/favicon.svg` — its `color(display-p3 …)` fill overrides render solid black under `resvg`, worked around by stripping them and keeping the plain `fill="#hex"`), dropped the unused `tabs` permission (zero `chrome.tabs.*` call sites), and reworded `description` to state the single purpose plainly. Backend: `CompanionSessionPayload`'s `extra="forbid"` was **already present** (legacy class-based `Config`, functionally equivalent under Pydantic 2.12 — modernized to `model_config = ConfigDict(...)` for consistency, not because the invariant was missing) and the school-audience 403 on `POST /api/companion/sessions` was **already implemented** before this session. 354 backend tests passing (345 baseline + 9 new), 20 extension vitest tests (12 stitcher + 8 new consent tests). Live install-and-click-through verification (build order step 4) **not performed** — this environment's browser tooling is sandboxed from `chrome://` URLs, so an unpacked-extension load/permission-grant walkthrough isn't reachable here; the enforcement mechanism instead rests on Chrome's documented behavior + the test suite above (§1, §2).
- 2026-07-26 — **Attachable roadmaps on a career goal (D-042).** New `career_goal_roadmaps` table (migration `c3e8a1f47b92`, RLS) + 4 routes on the `career` router (`GET/POST /api/career/roadmaps/`, `GET /roadmaps/available`, `DELETE /roadmaps/{id}`). A goal now plans from its generated tree **plus** any official-catalog or personal roadmap the user attaches — previously the 126-node DSA roadmap and every syllabus roadmap were invisible to the planner. Attaching creates `node_meta` sidecar rows (the planner INNER JOINs it and cannot score a node without subject/priority/effort) and never copies `roadmap_nodes`. One click: subject defaults to the roadmap slug, priority to 3. `get_today_plan`'s node query now spans `[goal.roadmap_id, *attached]`, so attached nodes also feed the balance window. Detach removes only the sidecars it created (never a `user_edited` one) and leaves `learning_events`/`node_mastery` untouched. Cap 5 per goal; the syllabus lifetime limit is unaffected. The four roadmap routes guard on `_get_active_goal` (active goal, tree or not) rather than `_get_active_goal_with_tree` — the picker renders in CareerOnboarding's tree step too, which is where users discover the gap that makes them want it; `GET /today` keeps the stricter guard. Shared UI: `frontend/src/AttachRoadmaps.jsx` (§1, §2).
- 2026-07-26 — **Career Coach Phase 3 — Scheduler & Today screen** (`docs/SPEC-career-coach-phase3.md`, `docs/IMPLEMENTATION-career-coach-phase3.md`, steps 1-5 of the build order; step 6 constant tuning explicitly skipped — no dogfood evidence survives D-039). New pure core `services/planner.py` (`build_plan()`: reviews-first, frontier/prereq gating, priority×urgency×unlock-weight scoring, sprint pin, a balance nudge gated by a not-in-the-parent-spec evidence floor `BALANCE_MIN_WINDOW_MINUTES=120`, MAX_STUDY_ITEMS=5 truncation+proportion) verified by a 20-scenario golden suite (`tests/test_planner.py`, G1-G10, 2 property tests × 50 seeds, 3 committed exact-output snapshots). New migration `b7b1755c4d22` (`career_goals.daily_minutes`, applied to prod). 3 new routes: `GET/PATCH /api/career/goals/active`-adjacent `GET /api/career/today`, `PATCH /api/career/goals/active`, `POST /api/career/today/feedback` (7 route tests incl. IDOR + lazy sprint-expiry). Frontend: `TodayCard` + `BalanceStrip` in `CareerTree.jsx` — no new page/route. Backend suite: 325/325 (198 baseline + 127 new). Verified live against the owner's real "Backend Engineer" goal (§2) — no active goal was created or archived for this; the daily-minutes round-trip and node deep-link were exercised and reverted. See `DECISIONS.md` for the sprint-vs-balance precedence call and the urgency-formula finding.
- 2026-07-26 — **Nightly encrypted DB backup + data-loss canary (D-040).** The 2026-07-26 incident was unrecoverable because the Supabase project is on the free plan: **no provider backups, no PITR, nothing to restore from.** New `.github/workflows/backup-db.yml` runs a nightly `pg_dump` of the `public` schema (plus `auth.users` data-only for reconciliation), verifies the archive with `pg_restore --list`, GPG-encrypts it (AES-256) and keeps it as a 90-day private artifact. The same job doubles as a canary: if any of `activities`/`reviews`/`user_progress`/`roadmap_nodes`/`roadmaps` reads zero rows it fails the run, converting silent data loss into a notification within 24h. New secrets required: `BACKUP_DATABASE_URL` (**session** pooler `:5432` — the app's transaction pooler `:6543` cannot run `pg_dump`, and the direct host is IPv6-only/unreachable from Actions; the workflow hard-fails on both) and `BACKUP_PASSPHRASE`. Full procedure, known gaps and the recovery playbook: `docs/RUNBOOK-backup-restore.md`. **Unset secrets = no backup**; restore path is untested until the drill in §4 is run.
- 2026-07-26 — **Data-loss incident + `TRUNCATE` guard on user-data tables (D-039).** Every user's `activities` and `reviews` rows were emptied (all users, not just the owner) by a `TRUNCATE … CASCADE` on the LeetCode catalog tables — not by a migration and not by a DELETE (`pg_stat_user_tables` showed `n_live_tup=0` with `n_tup_del` unchanged at its historical 7/8, the signature only TRUNCATE produces). The `activities.concept_card_id` FK added by `37714df921de` was the bridge: `problems → concept_cards → activities → reviews/question_sets`. Its `ON DELETE NO ACTION` gave false safety — `TRUNCATE … CASCADE` ignores referential actions and empties every referencing table. New migration `a9d4f7c2e618` adds `BEFORE TRUNCATE` statement triggers (which fire on cascaded truncates) to 10 user-data tables, so the same mistake now aborts loudly instead of draining history. Surviving tables (`user_progress`, `test_attempts`, `metric_events`, `learning_events`) are outside that FK closure — which is why the Roadmaps section of Home still rendered while every other stat read zero (§2).
- 2026-07-20 — **LLM provider consolidated on Gemini (D-037).** `SYLLABUS_MODEL` and `CAREER_TREE_MODEL` now default to `gemini-3.5-flash` (was `claude-opus-4-8`), so `GEMINI_API_KEY` is the only LLM key syllabus extraction and career-tree generation need; `ANTHROPIC_API_KEY` becomes optional. The Anthropic code path is left wired — provider is routed by model-id prefix, so a single env override routes back with no code change. Verified end-to-end against the live API: `gemini-3.5-flash` returns a schema-valid 70-node/10-subject tree through the nested Pydantic `response_schema`. **Render env vars must be updated by hand** — an explicit `SYLLABUS_MODEL=claude-opus-4-8` there wins over the new default.

One line per system-state change, newest first: `YYYY-MM-DD — what changed (sections touched)`.

- 2026-07-21 — LeetCode Retention phase (Prod Actions): 3 new tables added (`problems`, `problem_concepts`, `concept_cards`), updated migration chain and applied `add_paid_only` to prod via manual `alembic upgrade`. Two new Evidence endpoints (`POST /api/evidence/leetcode/solve` and `POST /api/evidence/leetcode/backfill`) implemented to record `PROBLEM_SOLVED` and `evidence_unmapped` metrics, tested successfully up to database routing layer but failing locally due to connection refused. Seeded 16 advanced nodes to the DSA roadmap (`seed_dsa.py`) and ran against prod database via a background task. Updated system documentation (§1, §2).

- 2026-07-20 — Career templates v2 quality pass (`docs/SPEC-career-templates-v2.md`, all 7 steps): fixed the subject-blind diagnostic probe selection (flat priority sort + Python's stable tie-break by file order meant the AI Engineer diagnostic asked 8 math/classical-ML questions and **zero LLM/GenAI questions**; now round-robin across subjects in `_select_diagnostic_probes`, deterministic, +3 tests). Validator gained a probe-spread warning (`default_priority ≥4` subject with no probes → zero diagnostic coverage) which immediately caught backend v1's probe-less `system_design` subject. Three new immutable template files: `ai_engineer.v2.json` 70 nodes (new 5-node `llm_apis` subject — the "working with LLM APIs" layer every 2026 JD calls baseline and the in-repo ai-engineering roadmap already teaches; +reranking/hybrid-retrieval +fine-tune-vs-RAG-vs-prompt; paid for by removing kmeans/PCA/eigenvalues/feature-stores/RNN), `sde_generalist.v2.json` 62 nodes (+the four highest-frequency interview patterns: two-pointers, sliding-window, binary-search, heaps; +greedy), `backend.v2.json` 66 nodes (+heaps/greedy/intervals, +caching probe). v1 files frozen; committed v1 trees unaffected (nothing re-reads a template post-commit). Deliberate deviation from the spec's own step 7: skipped the live throwaway-goal check because creating one would **archive the owner's real Backend Engineer goal** (one-active-goal index, no un-archive endpoint) — verified against the synced runtime artifact via direct function call instead. Suite: 176/176. Market evidence + future-role research prompt in the spec's §8.
- 2026-07-19 — Career Coach Phase 2 — Goal, Tree & Topic Mapping (`docs/SPEC-career-coach-phase2.md`, build order §10, steps 1-10 of 11; docs are step 11): 2 new tables (`career_goals`, `node_meta` sidecar; migration `b6f2d8a1c934`, RLS, **applied to prod**), new `career` router (12 routes: templates, goal CRUD, diagnostic get/submit, tree generate/commit, node patch/delete, unmapped list/assign, sprint), new `services/career_tree.py` (provider-routed generation adapting a role template, §3.3 hard-rule validation + one retry + unmodified-template fallback — never an error screen) and `services/topic_mapping.py` (Jaccard word-overlap Tier-2 match, `AUTO_MAP_THRESHOLD`/`TRIAGE_THRESHOLD`, shared `backfill_activity`), 3 role templates (`content/career-templates/*.v1.json`: backend 63 nodes, ai_engineer 68, sde_generalist 57) each validated by a new `content/validate_career_templates.py`. New `.github/workflows/validate-content.yml`: **the "existing content gate" both phase specs assumed was already wired into CI didn't exist before this** — now gates both validators. Frontend: `/coach` (`CareerCoach.jsx` router → `CareerOnboarding.jsx` 4-step wizard, `CareerTree.jsx` tree view + unmapped triage, reusing phase 1's per-node evidence-log drill-down as the trust feature it was always meant to become). New `backfill_career_mapping.py` (one-shot, mirrors `backfill_evidence.py`'s convention) and `sync_career_templates.py` (manual — see deploy-topology note below). New `test_career.py`: 23 tests (the spec's 19 plus extras, incl. the sprint endpoint). Backend suite: 171/171 passing.
  - **Live end-to-end verification** (not just pytest against SQLite) walked the real onboarding flow in-browser and caught two real bugs SQLite's non-enforced FKs couldn't: `commit_tree` needs staged `flush()` calls between `Roadmap` → `RoadmapNode` → `{NodeMeta, RoadmapNodePrerequisite, CareerGoal update}`, because none of those mapped classes have an ORM `relationship()` to each other, so the unit-of-work has no dependency edge ordering their writes — a single `add_all()`-then-`flush()` could (and, against real Postgres, did) send a child INSERT or the goal's UPDATE before its parent row existed. Recorded in `DECISIONS.md`. Also fixed a frontend crash (`CareerTree.jsx`'s `UnmappedTriage`: `useEffect(load, [])` passed a function that implicitly returned a Promise, which React mistook for a cleanup function).
  - **Deploy-topology gap found and worked around, not fixed at the root**: the spec puts role templates under `content/career-templates/` (matching the existing content pipeline), but Render's backend service deploys with root=`backend/` (§1), so that directory is outside the Docker build context and unreadable at runtime. Worked around with a backend-owned synced copy (`backend/app/data/career_templates/`, `sync_career_templates.py`, manual invocation) rather than changing deploy topology. Two copies now need to be kept in sync by hand when a template changes — flagged, not silently absorbed.
  - **Real-history backfill** (`backfill_career_mapping.py` against the owner's real account): of the 6 activities phase 1 flagged unmapped, **0 auto-mapped, 6 landed below the 0.55 triage floor** — real activity topics (e.g. "Mutable and Immutable Objects") share no vocabulary at all with the Backend Engineer tree's node titles. Honest result, not a bug: the Jaccard-overlap placeholder needs a real embedding call once mapping volume justifies it (already noted in `topic_mapping.py`'s docstring as the upgrade trigger).
  - **A real career tree now exists in the owner's own account** (not test junk — kept on request): "Backend Engineer" goal, 63 nodes across 8 subjects, committed during verification.
  - Missing from the spec's own API table until this pass: `POST /api/career/sprint` was speced (§6) but never implemented in earlier steps — added here with its own test.
- 2026-07-19 — Career Coach Phase 1 — Evidence Spine (`docs/SPEC-career-coach-phase1.md`, build order §9, steps 1-9): 2 new tables (`learning_events` append-only log, `node_mastery` derived cache; migration `a5e9c3d7f102`, RLS, **applied to prod**), new `evidence` router (6 routes: manual log, per-node mastery, per-node event trace, summary, recompute, soft-delete), new pure `services/evidence_weights.py` (weight table + `w()`) and `services/evidence.py` (`fold_events` + DB layer + read-side derivations), Producer A wired into `reviews.py`'s `complete_review` (emits `RECALL_GRADED` post-commit, failure-isolated via a SAVEPOINT — a bare `db.rollback()` was tried first and would have expired the already-eager-loaded `review.activity`, crashing the next line with `MissingGreenlet`; caught by the test suite, not by inspection), dev-only `/evidence` page (founder-gated). Real-history backfill (one-off `backfill_evidence.py`, entity_id-deduped so it can't double-count against future organic completions): of the owner's 31 completed reviews, only 1 was node-linked (30 = 97% unmapped, tracked via `evidence_unmapped` metric events) — that one event was a recall miss, so `m_learned=0` (honest, not inflated). New `test_evidence.py`: 52 tests (the spec's 18 plus extra threshold/property coverage). Backend suite: 148/148 passing. One spec deviation, recorded in `DECISIONS.md`: the fold tracks two independent running totals (capped vs. uncapped evidence, combined by `max()`) rather than the single shared running total the spec's §4.2 pseudocode shows — the single-total version isn't actually order-independent when a capped event lands before the uncapped push that dominates it (§1, §2).
- 2026-07-17 — Teacher dashboard Phase 1, partial (frontend-only — the 3 backend endpoints already existed from Phase 0 but had zero UI): new `TeachStudentDetail.jsx` (`/teach/:id/students/:memberId` — retention stats, 8-week recall trend bar chart, weakest topics, per-node mastery grid, calling the already-built `GET .../students/{member_id}`); `TeachClassroom.jsx` Students tab gained inline rename (pencil → edit → `PATCH .../members/{id}`) and remove (trash → confirm → `DELETE .../members/{id}`) row actions, roster names now link to the drill-down; classroom header gained a join-code regeneration button (`PATCH .../{id}` with `regenerate_join_code`). Verified via client-side `fetch` mock (no prod writes — the one real classroom has 0 students) + one live check against real data (regen button renders/wired, not exercised — it's a real destructive action on a real, possibly-shared join code). Still open from spec Phase 1: join-attempt throttle (explicit `NOTE:` deferral in `classrooms.py`), DPDP one-pager, weekly teacher digest email (§1).
- 2026-07-17 — Agentic-AI batch specced end-to-end (research → critique → contract): `content/research/agentic-ai-curriculum.json` (26 lessons, 5 phases, every docs URL fetch-verified; round-2 patch record in `agentic-ai-round2.json`), `backend/seed_ai_engineering.py` +26 nodes (62 total — **prod reseed pending**, founder-run), PROMPT-engineering.md gained the AGENTIC BATCH rules, `content/HANDOFF-agentic-ai.md` = Antigravity mission (MCP phase first for SEO). No schema/endpoint change (§1 catalog counts change on reseed).
- 2026-07-17 — School science-viz spec (`docs/SPEC-school-science-viz.md`) + M0 executed: `physics-9-10` roadmap (128 lessons) split into `physics-9` (77) + `physics-10` (51), each its own `content/roadmaps/` directory and DB `Roadmap` row — the class-first IA prep for Chemistry/11-12 (D-029/D-030). `SchoolRoadmaps.jsx` (§ frontend layout) needed no changes — it already groups any roadmap's nodes into a Class→Subject→Chapter browser by parsing `"Class N · Chapter"` phase strings, so the split alone fixes Physics previously double-bucketing under both classes. `backend/seed_physics_school.py`/`_prereqs.py` rewritten to seed two roadmaps (132 same-class prereq edges kept; 8 cross-year edges dropped — the roadmaps API has no cross-roadmap prerequisite resolution); **not yet run against prod** (manual hand-off, wipes `user_progress`). `frontend/vercel.json` gained 129 permanent redirects for the old lesson/roadmap URLs. Build verified: 640 static pages (unchanged), 12 hub pages (was 11), title-parity re-confirmed at the new URLs (§1, §2).
- 2026-07-16 — Review-feedback fixes + "Key areas to focus": new `GET /api/dashboard/focus-areas` (recalled=False completions, 14-day window, per-card grouping, cap 5, latest `ai_feedback` per topic) + `FocusAreasCard` on Home (amber accent, renders only with data); Review.jsx "Missed" fixed in both places (per-question chip grey→house alert red `#ba1a1a`; outcome buttons' inline styles bypass the html.dark remap layer, so OUTCOMES gained theme-aware `darkColor`/`darkBorder` resolved via `useTheme` — 'Missed it' `#334155` was invisible on dark); sidebar now open by default on Home only (spacer widens to 240px so the panel is structural there, hover-rail everywhere else). No schema change (§1, §2).
- 2026-07-16 — Teacher dashboard Phase 0 (`docs/SPEC-teacher-dashboard.md`, build-first scope): 3 new tables (`classrooms`/`classroom_members`/`classroom_roadmaps`, migration `431279836d74`, RLS, **not yet applied to prod**), new `classrooms` router (12 routes: roster/lifecycle + overview/gap-map/students/drill-down analytics) behind a fresh `get_owned_classroom` IDOR wall, new pure `services/mastery.py` (node-mastery + at-risk + root-cause logic, unit-tested), `services/learner_stats.py` extracted from `dashboard.py` (review-metrics/heatmap/node-accuracy now reusable with an optional roadmap scope — self-serve behavior unchanged, verified against the existing analytics test suite), new `test_classrooms.py` (IDOR + the privacy-boundary rule tested explicitly) and `test_mastery.py`. Frontend: `/teach`, `/teach/:id`, `/join/:code` (`Teach.jsx`/`TeachClassroom.jsx`/`JoinClassroom.jsx`), Profile.jsx entry points, conditional "Teach" nav item — builds clean. `seed_classroom_demo.py` written and validated (pitch-demo classroom) but **not run against prod**. Backend suite: 96/96 passing. Remaining before this is pitch-ready: apply the migration to prod, run the seed script, and a live browser smoke-test of the new UI (§1, §2, §4).
- 2026-07-13 — Sentry + Web Push/PWA + deeper analytics (`docs/HANDOFF-sentry-push-analytics.md`, 13 commits, all gated-off/no-op until env vars are set): **Sentry** — backend `sentry_sdk.init` + `logging.basicConfig`, frontend `lib/errors.js` + custom `ErrorBoundary.jsx`, sourcemap upload+delete via `sentryVitePlugin` (verified: zero `.map` files ship without `SENTRY_AUTH_TOKEN`). **Web Push/PWA** — `manifest.webmanifest` + icons, push-only `sw.js` (no fetch handler; caught+fixed a cross-origin bug — a static SW file can't read `import.meta.env`, so `push.js` stashes the API origin via Cache Storage), new `push_subscriptions` table (migration `d4f6a8c1e3b7`), `services/push.py` (pywebpush), `/api/push/*` routes, Profile toggle + Review done-screen prompt, reminder fan-out to push under the same `reminder_log` claim. **Analytics** — `reviews.duration_ms` (migration `e7a9c2f4b6d8`, clamped (0,1_800_000]), generic `metric_events` table (migration `f1b3d5e7a9c0`, first producer: syllabus's `extraction_edit_delta`), 5 new `/api/dashboard/*` aggregation endpoints (each with its own `enough_data` floor) + Analytics.jsx UI, PostHog event audit (`pwa_installed`/`syllabus_committed`/`review_skipped`/`feedback_sent` — the last one found a second, previously-untracked call site in `KnowledgeVault.jsx`). Live-Postgres verification (not just the SQLite test harness) caught and fixed a real bug: `source-retention`'s `GROUP BY func.coalesce(...)` used two separately-compiled expressions that SQLite tolerated but Postgres correctly rejected. New env vars in §4; alembic head now `f1b3d5e7a9c0` (3 migrations pending in prod, auto-apply on next deploy). Backend test suite: 45/45 passing (§1, §2, §4, §6).
- 2026-07-13 — Analytics build-out (AARRR + North Star; plan in `docs/analytics-plan.md`). Frontend `lib/analytics.js` event vocab expanded (activation funnel: `signup_started`/`signed_up`/`onboarding_started`/`onboarding_completed`/`first_capture_shown`/`activated`; retention: `reviews_due_shown`/`review_queue_empty`/`reminder_clicked`; engagement: `lesson_completed`/`card_created_from_lesson`; friction: `api_error`) + `trackOnce` helper + `capture_pageleave:true` (Web Analytics bounce/duration). New backend `services/analytics.py` (server-side PostHog) emits `review_scheduled` (activities + reviews) and `reminder_sent` (reminders, link tagged `?src=reminder`). New env `POSTHOG_API_KEY`/`POSTHOG_HOST`; `posthog>=3.7.0` dep; flush on app shutdown (§1 topology/services/frontend). **No schema change.**
- 2026-07-12 — Syllabus commits capped at **3 per user lifetime** (`SYLLABUS_LIFETIME_LIMIT`; new `user_prefs.custom_roadmaps_created` counter, migration `c8e2a7f5d1b9`, atomic claim, delete ≠ refund) + `GET /api/syllabus/quota`; Home gains a "Customize your own" tile → `/roadmaps/new`; upload page shows remaining quota and blocks at 0. Content plan doc added: `docs/PLAN-lesson-generation.md` (Sonnet-drafted, founder to review) (§1–2).
- 2026-07-12 — Question mode goes persistent + topic-grounded: new `question_sets` table (migration `b3d9f1a4c6e2`, 13th table; RLS) — LLM sets generated with per-question `reference_answer`s, reused ≥2 review sessions shuffled, then regenerated; node-linked cards get topic-grounded generation with a user-facing `depth` choice ('main'|'deep', Review.jsx toggle); `/grade-questions` injects stored references server-side. Superseded key_memory-only `generate_questions` removed from `grader.py`. New tests `test_question_sets.py` (§1–2).
- 2026-07-12 — Syllabus: added `POST /api/syllabus/extract-text` (pasted text ≤40K chars, token-cheap default; shares the daily limit) + paste-text tab as the primary input in `SyllabusUpload.jsx`; `services/syllabus.py` grew `extract_roadmap_from_text` with shared `_finalize` guardrails (§1).
- 2026-07-11 — Backend deploy host moved **Railway → Render** (Railway free tier ended); env vars (incl. `ANTHROPIC_API_KEY`/`GEMINI_API_KEY`/`SYLLABUS_MODEL`) re-set on Render. Docs updated across SYSTEM-OVERVIEW §1/§4/§6, CLAUDE.md, README, BACKLOG. **Render region unverified** — keep near Supabase Mumbai (Singapore) for latency (§6, watch-list).
- 2026-07-11 — Syllabus → personal roadmap feature: `syllabus` router (extract/commit/delete, review-before-commit), `services/syllabus.py` (**provider-routed by `SYLLABUS_MODEL`**: Anthropic `claude-opus-4-8` or Google Gemini via `google-genai`; new `ANTHROPIC_API_KEY`/`GEMINI_API_KEY`/`SYLLABUS_*` env), migration `a1c5e8f2d7b3` `roadmaps.user_id` (**applied to prod 2026-07-11; file committed same day after it crash-looped the deploy while uncommitted**), roadmap visibility = catalog-by-audience + own, `/roadmaps/new` UI (§1, §2).
- 2026-07-11 — Agent audit vs code + prod DB: prod re-verified (head `f2b7d3a9c8e4`, RLS ×12, 30 roadmaps/1689 nodes); fixed validate.py kind list (was 5, actually 9 branched + base); DSA renderer-feed status precised (4 unfed, backtracking feeds Tree/Grid). Backend §1–2 verified clean.
- 2026-07-11 — Frontend layout: documented `SchoolRoadmaps.jsx` Class→Subject→Chapter browser + roadmap search (shipped in commit `b94d233` on 07-10 without a doc update; rationale in `DECISIONS.md` D-006).
- 2026-07-10 — Docs-layer restructure: CLAUDE.md slimmed to rules + update-routing (old version → `docs/CLAUDE-ARCHIVE-2026-07.md`); added `docs/claude-code-workflow.md`, `docs/DECISIONS.md` (D-001), `docs/BACKLOG.md` (Appendix updated).
- 2026-07-10 — Document created from a full code + prod-DB verification pass; repo cleanup (one-off scripts purged, `.gitignore` hardened); living-doc convention added to CLAUDE.md.
- 2026-07-30 � **LeetCode Log Capture UI & Full Grader Integration** (docs/IMPLEMENTATION-leetcode-log-capture.md). Fully implemented the "LeetCode" manual logging tab in LogActivity.jsx. Combines a combobox problem search (using pg_trgm via GET /api/activities/search-problems and GET /api/activities/suggest-problems) with a node selector that uses problem_concepts mappings. The frontend submits to POST /api/activities/ which was extended to accept problem_id alongside 
ode_id. This problem_id flows down into the ctivities table. The review loop (ackend/app/api/routes/reviews.py and ackend/app/services/grader.py) now dynamically looks up the problem's title, the primary mapped node, and alternative nodes via problem_concepts, passing them to the LLM grader as PROBLEM CONTEXT in _QGEN_SET_SYSTEM_PROMPT. This ensures that review questions focus on the underlying concepts (the node) applied to the specific problem, rather than generically reciting the problem statement. As per D-052, manual capture only creates an ctivities row and a problem_attempts row; it does NOT directly emit learning_events (which are reserved for the actual recall reviews and extension captures). Tests were updated in 	est_activities.py to cover search, suggestion, and submission with problem_id, alongside frontend updates to rontend/src/pages/LogActivity.jsx and rontend/src/api/activities.js.
